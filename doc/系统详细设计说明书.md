# 系统详细设计说明书

> 项目名称：基于 LangChain 的 RAG 企业内部知识库问答 Agent 系统
>
> 文档版本：v1.0
>
> 日期：2026-06-03
>
> 文档状态：已评审

***

## 开发阶段总览

> 本文档中所有类和方法均标注了所属开发阶段。三个阶段与《业务需求说明书 v2.0》第 9 章对齐。

```
阶段标记图例:
  [P1] = 第一阶段 MVP（核心问答 + 基础权限 + 文档管理 + Dashboard）
  [P2] = 第二阶段增强（混合检索 + 流式输出 + 多源接入 + ABAC 属性权限 + ETL）
  [P3] = 第三阶段全面（全业务域 + 语义路由 + CQRS 读写分离 + 智能运维）

依赖关系:
  P2 依赖 P1 的全部产出
  P3 依赖 P1+P2 的全部产出
  同阶段内的类按照标记数字顺序开发
```

### 三阶段全量类映射表

| Maven 模块                           | 阶段一 (P1)                                                                                                                                                                                                   | 阶段二 (P2)                                                                                                                                                                                                                          | 阶段三 (P3)                                              |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| **agent-qr-common** (公共)           | Result, GlobalExceptionHandler, BusinessException, CorsConfig, MybatisPlusConfig, SpringContextUtil, 5个Event                                                                                               | TraceIdFilter, MdcTaskDecorator, CaffeineConfig, DeadLetterQueue, DlqRetryScheduler, DlqMessage, DocumentDeleteRequestedEvent ★                                                                                                      | ReadWriteRoutingDataSource, ReadWriteDataSourceAspect |
| **agent-qr-auth** (认证)             | AuthController, AuthService, PasswordUtil, JwtUtil(基础), JwtAuthFilter(基础), SecurityConfig(基础)                                                                                                              | UserPrincipal, AbacEvaluator, JwtUtil(ABAC扩展), JwtAuthFilter(ABAC扩展), SecurityConfig(@PreAuthorize), AbacAccessDeniedHandler, RefreshTokenService, TokenRefresh                                                                   | —                                                     |
| **agent-qr-user** (用户)             | AdminController(基础CRUD), SysUserMapper, SysUser(基础字段), MyMetaObjectHandler                                                                                                                                 | SysUser(ABAC字段扩展)                                                                                                                                                                                                                 | —                                                     |
| **agent-qr-knowledge** (知识库)       | KnowledgeController, DocumentCommandService(基础), DocumentQueryService, FileStorageService, DocumentParserService, PdfParser(基础), DocxParser(基础), TextParser, TextSplitter, DocumentStatus, Document, Chunk | DocumentCommandService(软删除v2), PdfParser(流式+表格+OCR), DocxParser(表格Markdown), Document(domain/sensitivity字段)                                                                                                                       | —                                                     |
| **agent-qr-rag** (RAG)             | ChatController(基础ask), ChatQueryService(基础sync), ProviderFactory, DeepSeekLLMProvider, DeepSeekEmbeddingProvider, ChromaRetriever(基础), PromptTemplate, RetrievedDocument, Conversation, Message            | ChatQueryService(SSE流式), OllamaLLMProvider, OllamaEmbeddingProvider, HybridRetriever, BM25Retriever, RerankerService, LLMCircuitBreaker, StructuredFilterService, ChunkStructuredMapper, BatchEmbeddingService, DomainRouter(关键词) | DomainRouter(Embedding语义), EmbeddingDimensionManager  |
| **agent-qr-statistics** (统计)       | StatisticsQueryService(基础Dashboard), DailyStats, StatisticsController, StatisticsUpdateListener                                                                                                            | StatisticsQueryService(含满意度), FeedbackService                                                                                                                                                                                     | —                                                     |
| **agent-qr-web** (启动)              | AgentQrApplication, GlobalExceptionHandler, CorsConfig, SecurityConfig(基础), AsyncConfig(单池), application.yml                                                                                               | SecurityConfig(ABAC扩展), AsyncConfigV2(四池隔离), TraceIdFilter, DlqRetryScheduler ★, application-p2.yml                                                                                                                                    | CQRS配置, application-p3.yml                            |
| **agent-qr-compensation** (补偿) ★   | —                                                                                                                                                                                                          | OrphanVectorScanner, DocumentDeleteListener ★, DocumentDeleteServiceV2, DeleteTask                                                                                                                                                | —                                                     |
| **agent-qr-datasource** (数据接入) ★   | —                                                                                                                                                                                                          | DataSourceConnector, JdbcConnector, RestApiConnector, S3Connector, SyncScheduler, DataSourceService, DataSourceConfig                                                                                                             | —                                                     |
| **agent-qr-data-quality** (数据质量) ★ | —                                                                                                                                                                                                          | DataQualityChecker, QualityRule, CompletenessRule, EncodingRule, FormatRule, QualityReport                                                                                                                                        | —                                                     |
| **agent-qr-etl** (ETL) ★           | —                                                                                                                                                                                                          | DataNormalizer, StructuredDataConverter, FieldMappingEngine, CanonicalRecord                                                                                                                                                      | —                                                     |
| **agent-qr-catalog** (知识目录) ★      | —                                                                                                                                                                                                          | KnowledgeCatalogService, DomainRouter(关键词)                                                                                                                                                                                        | DomainRouter(Embedding语义)                             |

> ★ = P2 阶段新增 Maven 模块，详见对应章节的包结构设计

\| 时序图 | 文档上传处理、RAG 问答(basic) | RAG 问答(混合+流式)、多源接入ETL | — |
\| 算法 | 递归字符分割 | RRF融合, DLQ退避, 熔断状态机, 字符集检测, 结构化转换, 批量攒批 | Provider切换决策 |

***

## 目录

1. [引言](#1-引言)
2. [公共模块详细设计 (agent-qr-common)](#2-公共模块详细设计-agent-qr-common)
3. [认证授权模块详细设计 (agent-qr-auth)](#3-认证授权模块详细设计-agent-qr-auth)
4. [用户管理模块详细设计 (agent-qr-user)](#4-用户管理模块详细设计-agent-qr-user)
5. [知识库模块详细设计 (agent-qr-knowledge)](#5-知识库模块详细设计-agent-qr-knowledge)
6. [RAG 问答模块详细设计 (agent-qr-rag)](#6-rag-问答模块详细设计-agent-qr-rag)
7. [事件处理模块详细设计](#7-事件处理模块详细设计)
8. [统计模块详细设计 (agent-qr-statistics)](#8-统计模块详细设计-agent-qr-statistics)
9. [Web 启动模块详细设计 (agent-qr-web)](#9-web-启动模块详细设计-agent-qr-web)
10. [数据一致性补偿模块详细设计 (agent-qr-compensation) ★](#10-数据一致性补偿模块详细设计-agent-qr-compensation)
11. [多源数据接入模块详细设计 (agent-qr-datasource) ★](#11-多源数据接入模块详细设计-agent-qr-datasource)
12. [数据质量检查模块详细设计 (agent-qr-data-quality) ★](#12-数据质量检查模块详细设计-agent-qr-data-quality)
13. [ETL 标准化管道模块详细设计 (agent-qr-etl) ★](#13-etl-标准化管道模块详细设计-agent-qr-etl)
14. [知识目录模块详细设计 (agent-qr-catalog) ★](#14-知识目录模块详细设计-agent-qr-catalog)
15. [核心时序图](#15-核心时序图)
16. [异常处理设计](#16-异常处理设计)
17. [关键算法说明](#17-关键算法说明)

> ★ = P2 阶段新增 Maven 模块

***

## 1. 引言

### 1.1 编写目的

本《系统详细设计说明书》对每个模块进行类级别设计，明确类的属性、方法、依赖关系和协作流程，指导编码实现。

### 1.2 适用范围

后端开发工程师、前端开发工程师、测试工程师。

### 1.3 参考文档

| 编号   | 文档名称           |
| ---- | -------------- |
| \[1] | 系统概要设计说明书 v1.0 |
| \[2] | 软件需求规格说明书 v1.0 |

### 1.4 命名规范

| 层级         | 后缀/前缀              | 示例                         |
| ---------- | ------------------ | -------------------------- |
| Controller | `XxxController`    | `AuthController`           |
| Service 接口 | `XxxService`       | `AuthService`              |
| Service 实现 | `XxxServiceImpl`   | `AuthServiceImpl`          |
| Mapper     | `XxxMapper`        | `SysUserMapper`            |
| Entity     | 表名转驼峰              | `SysUser`                  |
| DTO        | `XxxDTO` / `XxxVO` | `LoginDTO` / `DashboardVO` |
| 事件类        | `XxxEvent`         | `DocumentUploadedEvent`    |
| 监听器        | `XxxListener`      | `DocumentParseListener`    |
| 工具类        | `XxxUtil`          | `PasswordUtil`             |
| 配置类        | `XxxConfig`        | `AsyncConfig`              |
| 枚举         | `XxxEnum` 或不加后缀    | `DocumentStatus`           |

***

## 2. 公共模块详细设计 (agent-qr-common)

> Maven 模块: `agent-qr-common`
> 基础包路径: `org.example.agent_qr.common`

### 2.1 包结构（P1 / P2 / P3 全量）

```
agent-qr-common/
└── src/main/java/org/example/agent_qr/common/
    ├── [P1] Result.java                       // 统一响应类
    ├── [P1] BusinessException.java            // 业务异常
    ├── config/
    │   ├── [P1] MybatisPlusConfig.java        // MyBatis-Plus 分页插件配置
    │   └── [P2] CaffeineConfig.java           // Caffeine 本地缓存配置（LLM响应缓存）
    ├── event/
    │   ├── [P1] DocumentUploadedEvent.java    // 文档上传事件
    │   ├── [P1] DocumentParsedEvent.java      // 文档解析完成事件
    │   ├── [P1] ChunksCreatedEvent.java       // 文本切片完成事件
    │   ├── [P1] EmbeddingCompletedEvent.java  // 向量化完成事件
    │   ├── [P1] AnswerGeneratedEvent.java     // AI回答生成事件
    │   └── [P2] DocumentDeleteRequestedEvent.java // ★ 文档删除请求事件（事件驱动解耦 knowledge↔compensation）
    ├── util/
    │   └── [P1] SpringContextUtil.java        // Spring 上下文工具
    ├── filter/
    │   └── [P2] TraceIdFilter.java            // 全链路追踪 TraceId 过滤器
    ├── executor/
    │   └── [P2] MdcTaskDecorator.java         // 异步线程 MDC 上下文传递装饰器
    ├── dlq/
    │   ├── [P2] DeadLetterQueue.java          // 死信队列管理器（指数退避重试，位于 common）
    │   └── entity/
    │       └── [P2] DlqMessage.java           // 死信消息实体（位于 common）
    └── datasource/
        ├── [P3] ReadWriteRoutingDataSource.java    // CQRS 读写分离数据源路由
        └── [P3] ReadWriteDataSourceAspect.java     // CQRS 读写分离切面（@Transactional）

agent-qr-common/pom.xml 依赖:
  [P1] spring-boot-starter-web, mybatis-plus-spring-boot3-starter, lombok
  [P2] spring-boot-starter-aop (为 CaffeineConfig/TraceIdFilter)
  [P3] (无新增依赖，ReadWriteRoutingDataSource 基于 Spring 内置 AbstractRoutingDataSource)
```

> **设计约束**: `agent-qr-common` 被所有业务模块依赖，禁止反向依赖任何业务模块。新增 P2/P3 类时不得引入对 `agent-qr-auth`、`agent-qr-user`、`agent-qr-knowledge`、`agent-qr-rag`、`agent-qr-statistics` 等模块的依赖。

### 2.2 统一响应类 Result

```java
/**
 * 统一 API 响应结果封装
 * @param <T> 响应数据类型
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class Result<T> {

    /** 状态码 */
    private Integer code;

    /** 提示信息 */
    private String message;

    /** 响应数据 */
    private T data;

    /** 时间戳 */
    private Long timestamp;

    /**
     * 成功响应（无数据）
     */
    public static <T> Result<T> success() {
        return new Result<>(200, "操作成功", null, System.currentTimeMillis());
    }

    /**
     * 成功响应（带数据）
     * @param data 响应数据
     */
    public static <T> Result<T> success(T data) {
        return new Result<>(200, "操作成功", data, System.currentTimeMillis());
    }

    /**
     * 成功响应（自定义消息）
     * @param message 提示信息
     * @param data 响应数据
     */
    public static <T> Result<T> success(String message, T data) {
        return new Result<>(200, message, data, System.currentTimeMillis());
    }

    /**
     * 失败响应
     * @param code 错误码
     * @param message 错误信息
     */
    public static <T> Result<T> error(Integer code, String message) {
        return new Result<>(code, message, null, System.currentTimeMillis());
    }
}
```

### 2.3 全局异常处理 GlobalExceptionHandler

```java
/**
 * 全局异常处理器
 * 统一处理 Controller 层抛出的异常，返回标准 JSON 响应
 */
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    /**
     * 处理业务异常
     */
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusinessException(BusinessException e) {
        log.warn("业务异常: {}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }

    /**
     * 处理参数校验异常
     */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValidationException(MethodArgumentNotValidException e) {
        String msg = e.getBindingResult().getFieldErrors().stream()
                .map(FieldError::getDefaultMessage)
                .collect(Collectors.joining(", "));
        return Result.error(400, msg);
    }

    /**
     * 处理访问拒绝异常（权限不足）
     */
    @ExceptionHandler(AccessDeniedException.class)
    public Result<Void> handleAccessDeniedException(AccessDeniedException e) {
        return Result.error(403, "权限不足，无法访问该资源");
    }

    /**
     * 处理未知异常
     */
    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error(500, "服务器内部错误，请稍后重试");
    }
}
```

### 2.4 业务异常 BusinessException

```java
/**
 * 业务异常类
 * 用于在 Service 层抛出可预期的业务错误
 */
public class BusinessException extends RuntimeException {

    /** 异常状态码 */
    private Integer code;

    public BusinessException(String message) {
        super(message);
        this.code = 400;
    }

    public BusinessException(Integer code, String message) {
        super(message);
        this.code = code;
    }

    public Integer getCode() {
        return code;
    }
}
```

***

## 3. 认证授权模块详细设计 (agent-qr-auth)

> Maven 模块: `agent-qr-auth`
> 基础包路径: `org.example.agent_qr.auth`
> 模块依赖: `agent-qr-common`、`agent-qr-user`

### 3.0 包结构（P1 / P2 全量）

```
agent-qr-auth/
└── src/main/java/org/example/agent_qr/auth/
    ├── controller/
    │   └── [P1] AuthController.java               // POST /api/auth/login, register; GET /api/auth/info
    ├── service/
    │   ├── [P1] AuthService.java                   // 认证服务接口
    │   ├── [P2] RefreshTokenService.java           // 双Token刷新服务（Access 30min + Refresh 7day）
    │   └── impl/
    │       └── [P1] AuthServiceImpl.java           // 认证服务实现（登录/注册/获取当前用户）
    ├── dto/
    │   ├── [P1] LoginDTO.java                      // 登录请求 DTO（username + password）
    │   ├── [P1] RegisterDTO.java                   // 注册请求 DTO
    │   └── [P1] LoginVO.java                       // 登录响应 VO（token + userId + username + role）
    ├── util/
    │   ├── [P1] PasswordUtil.java                  // BCrypt 密码加密/验证（strength=12）
    │   └── [P1→P2] JwtUtil.java                   // [P1] 基础 JWT 生成/解析 [P2] ABAC属性编码 + 双Token
    ├── filter/
    │   └── [P1→P2] JwtAuthenticationFilter.java   // [P1] Bearer Token 提取+验证 [P2] ABAC UserPrincipal 注入
    ├── principal/
    │   └── [P2] UserPrincipal.java                 // ABAC 扩展用户主体（department/clearanceLevel/allowedDomains/title）
    ├── evaluator/
    │   └── [P2] AbacEvaluator.java                 // ABAC 属性访问控制评估器（@PreAuthorize SpEL 调用）
    ├── handler/
    │   └── [P2] AbacAccessDeniedHandler.java       // ABAC 权限拒绝全局异常处理（结构化审计日志 JSON）
    └── entity/
        └── [P2] TokenRefresh.java                  // Refresh Token 持久化实体

agent-qr-auth/pom.xml 依赖:
  [P1] agent-qr-common, agent-qr-user, spring-boot-starter-security, jjwt-api/jjwt-impl/jjwt-jackson
  [P2] (无新增依赖，ABAC 为纯内存属性比较)
```

### 3.1 类图

```
┌─────────────────────┐
│   AuthController     │
├─────────────────────┤
│ - authService        │
├─────────────────────┤
│ + login(LoginDTO)   │
│ + register(RegDTO)  │
│ + getUserInfo()     │
└──────────┬──────────┘
           │ 依赖
           ▼
┌─────────────────────┐        ┌──────────────────┐
│    AuthService       │───────→│  SysUserMapper   │
├─────────────────────┤        └──────────────────┘
│ + login(username,pwd)│
│ + register(dto)     │        ┌──────────────────┐
│ + getCurrentUser()  │───────→│  PasswordUtil    │
└─────────────────────┘        └──────────────────┘
                                       │
                              ┌────────┴────────┐
                              │    JwtUtil      │
                              ├─────────────────┤
                              │ + generateToken │
                              │ + parseToken    │
                              │ + validateToken │
                              └─────────────────┘

┌─────────────────────────┐
│ JwtAuthenticationFilter │
├─────────────────────────┤
│ - jwtUtil               │
├─────────────────────────┤
│ + doFilterInternal()    │
└─────────────────────────┘
```

### 3.2 类详细设计

#### 3.2.1 AuthController

| 属性/方法                      | 说明                |
| -------------------------- | ----------------- |
| `authService: AuthService` | 认证服务              |
| `POST /api/auth/login`     | 用户登录，返回 JWT       |
| `POST /api/auth/register`  | 用户注册，默认 role=user |
| `GET /api/auth/info`       | 获取当前登录用户信息        |

#### 3.2.2 AuthService

| 方法               | 参数                                     | 返回                                       | 说明                        |
| ---------------- | -------------------------------------- | ---------------------------------------- | ------------------------- |
| `login`          | `LoginDTO(username, password)`         | `LoginVO(token, userId, username, role)` | BCrypt 验证密码后生成 JWT        |
| `register`       | `RegisterDTO(username, password, ...)` | `void`                                   | 校验用户名唯一性，BCrypt 加密密码，保存用户 |
| `getCurrentUser` | -                                      | `SysUser`                                | 从 SecurityContext 获取当前用户  |

**登录流程**：

```
1. 根据 username 查询 sys_user
2. 用户不存在 → 抛出 BusinessException("用户名或密码错误")
3. 用户 status=0 → 抛出 BusinessException("账号已被禁用")
4. BCrypt.matches(password, dbPassword) 验证密码
5. 不匹配 → 抛出 BusinessException("用户名或密码错误")
6. 生成 JWT Token（sub=username, userId, role, exp=24h）
7. 返回 LoginVO
```

#### 3.2.3 PasswordUtil（BCrypt 密码工具） `[P1]`

```java
/**
 * BCrypt 密码加密验证工具类
 * 使用 BCrypt 自适应哈希算法替代不安全的 MD5
 * strength=12 提供约 2^12 轮迭代，有效抵御 GPU 暴力破解和彩虹表攻击
 */
@Component
public class PasswordUtil {

    private final BCryptPasswordEncoder encoder = new BCryptPasswordEncoder(12);

    /**
     * 对明文密码进行 BCrypt 加密
     * @param rawPassword 明文密码
     * @return BCrypt 密文（60 字符，含自动生成的随机盐）
     */
    public String encode(String rawPassword) {
        return encoder.encode(rawPassword);
    }

    /**
     * 验证明文密码与 BCrypt 密文是否匹配
     * @param rawPassword 明文密码
     * @param encodedPassword BCrypt 密文
     * @return 是否匹配
     */
    public boolean matches(String rawPassword, String encodedPassword) {
        return encoder.matches(rawPassword, encodedPassword);
    }
}
```

#### 3.2.4 JwtUtil `[P1]`

```java
/**
 * JWT 令牌工具类
 */
@Component
public class JwtUtil {

    @Value("${jwt.secret}")
    private String secret;

    @Value("${jwt.expiration:86400000}")
    private Long expiration; // 默认24小时

    /**
     * 生成 JWT 令牌
     * @param userId 用户ID
     * @param username 用户名
     * @param role 角色
     * @return JWT 字符串
     */
    public String generateToken(Long userId, String username, String role) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + expiration);
        return Jwts.builder()
                .setSubject(username)
                .claim("userId", userId)
                .claim("role", role)
                .setIssuedAt(now)
                .setExpiration(expiryDate)
                .signWith(SignatureAlgorithm.HS256, secret)
                .compact();
    }

    /**
     * 从令牌中解析用户名
     * @param token JWT 令牌
     * @return 用户名
     */
    public String getUsernameFromToken(String token) {
        return getClaimsFromToken(token).getSubject();
    }

    /**
     * 验证令牌是否有效
     * @param token JWT 令牌
     * @return 是否有效
     */
    public boolean validateToken(String token) {
        try {
            getClaimsFromToken(token);
            return true;
        } catch (Exception e) {
            return false;
        }
    }

    /**
     * 解析令牌 Claims
     */
    private Claims getClaimsFromToken(String token) {
        return Jwts.parser()
                .setSigningKey(secret)
                .parseClaimsJws(token)
                .getBody();
    }
}
```

#### 3.2.5 JwtAuthenticationFilter `[P1]`

```java
/**
 * JWT 认证过滤器
 * 在 Spring Security 过滤器链中拦截请求，解析 JWT 并注入 SecurityContext
 */
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    @Autowired
    private JwtUtil jwtUtil;

    @Autowired
    private SysUserMapper sysUserMapper;

    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                     HttpServletResponse response,
                                     FilterChain filterChain)
            throws ServletException, IOException {

        // 1. 从请求头获取 Token
        String token = extractToken(request);
        if (token == null) {
            filterChain.doFilter(request, response);
            return;
        }

        // 2. 验证 Token 有效性
        if (!jwtUtil.validateToken(token)) {
            filterChain.doFilter(request, response);
            return;
        }

        // 3. 解析用户名，查询用户
        String username = jwtUtil.getUsernameFromToken(token);
        SysUser user = sysUserMapper.selectByUsername(username);
        if (user == null) {
            filterChain.doFilter(request, response);
            return;
        }

        // 4. 构建认证对象，注入 SecurityContext
        UsernamePasswordAuthenticationToken authentication =
                new UsernamePasswordAuthenticationToken(
                        user, null,
                        List.of(new SimpleGrantedAuthority("ROLE_" + user.getRole().toUpperCase()))
                );
        SecurityContextHolder.getContext().setAuthentication(authentication);

        filterChain.doFilter(request, response);
    }

    /**
     * 从 Authorization 头中提取 Bearer Token
     */
    private String extractToken(HttpServletRequest request) {
        String header = request.getHeader("Authorization");
        if (header != null && header.startsWith("Bearer ")) {
            return header.substring(7);
        }
        return null;
    }
}
```

#### 3.2.6 SecurityConfig `[P1]`

```java
/**
 * Spring Security 安全配置
 */
@Configuration
@EnableWebSecurity
@EnableMethodSecurity
public class SecurityConfig {

    @Autowired
    private JwtAuthenticationFilter jwtFilter;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(AbstractHttpConfigurer::disable)
            .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/login", "/api/auth/register").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .requestMatchers("/api/knowledge/**").hasRole("ADMIN")
                .requestMatchers("/api/chat/**").authenticated()
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        // 使用 BCrypt 自适应哈希编码器（strength=12），密码在 Service 层加密
        return new BCryptPasswordEncoder(12);
    }
}
```

#### 3.2.7 UserPrincipal（ABAC 扩展用户主体） `[P2]`

```java
/**
 * ★ ABAC 扩展用户主体
 * 从 JWT Payload 中解析 ABAC 属性，注入 SecurityContext
 * 替代原有的简单角色判断，支持部门/密级/域/职级多维度属性
 */
@Data
public class UserPrincipal {

    private Long userId;
    private String username;
    private String role;
    /** ★ 所属部门 */
    private String department;
    /** ★ 数据密级等级 */
    private Integer clearanceLevel;
    /** ★ 允许访问的业务域列表 */
    private List<String> allowedDomains;
    /** ★ 职级 */
    private String title;

    // 从 JWT Claims 构建
    public static UserPrincipal fromClaims(Claims claims) {
        UserPrincipal user = new UserPrincipal();
        user.setUserId(claims.get("userId", Long.class));
        user.setUsername(claims.getSubject());
        user.setRole(claims.get("role", String.class));
        user.setDepartment(claims.get("department", String.class));
        user.setClearanceLevel(claims.get("clearanceLevel", Integer.class));
        // allowedDomains 在 JWT 中存为逗号分隔字符串
        String domains = claims.get("allowedDomains", String.class);
        user.setAllowedDomains(domains != null
            ? Arrays.asList(domains.split(","))
            : Collections.emptyList());
        user.setTitle(claims.get("title", String.class));
        return user;
    }

    /** 是否为管理员（绕过 ABAC 属性检查） */
    public boolean isAdmin() {
        return "admin".equals(this.role);
    }

    /** 是否有权访问指定业务域 */
    public boolean hasDomainAccess(String domain) {
        if (domain == null) return true;
        return this.allowedDomains.contains(domain)
            || domain.equals(this.department);
    }

    /** 密级是否足够访问指定资源 */
    public boolean hasClearance(int resourceLevel) {
        return this.clearanceLevel >= resourceLevel;
    }
}
```

#### 3.2.8 AbacEvaluator（ABAC 属性策略评估器） `[P2]`

```java
/**
 * ★ ABAC 属性访问控制评估器
 * 所有权限策略集中在此类中，通过 SpEL @PreAuthorize 表达式调用
 *
 * 设计原则：
 * 1. Admin 全权限（绕过所有属性检查）
 * 2. 默认拒绝（方法返回值只有 true/false，无匹配规则时返回 false）
 * 3. 策略集中（所有规则在一个类中，便于审计和维护）
 * 4. 延迟 < 1ms（纯内存属性比较，无外部调用）
 */
@Component("abac")
@Slf4j
public class AbacEvaluator {

    @Autowired
    private DocumentMapper documentMapper;

    // ==================== 域权限 ====================

    /**
     * 用户能否在指定域内执行查询操作
     * 规则：
     *   1. admin → 允许
     *   2. user.allowedDomains 包含 domain → 允许
     *   3. user.department == domain → 允许
     *   4. 其他 → 拒绝
     *
     * 使用方式: @PreAuthorize("@abac.canQueryDomain(principal, #domain)")
     */
    public boolean canQueryDomain(UserPrincipal user, String domain) {
        if (user.isAdmin()) return true;
        if (domain == null) return true; // 未指定域，不限制

        boolean allowed = user.hasDomainAccess(domain);
        if (!allowed) {
            log.warn("ABAC 拒绝: 用户 {} (部门={}) 无法访问 {} 域",
                user.getUsername(), user.getDepartment(), domain);
        }
        return allowed;
    }

    // ==================== 文档权限 ====================

    /**
     * 用户能否访问指定文档
     * 规则：
     *   1. admin → 允许
     *   2. 用户密级 >= 文档密级 AND 用户有文档域的权限
     *   3. 其他 → 拒绝
     *
     * 使用方式: @PreAuthorize("@abac.canAccessDocument(principal, #documentId)")
     */
    public boolean canAccessDocument(UserPrincipal user, Long documentId) {
        if (user.isAdmin()) return true;

        Document doc = documentMapper.selectById(documentId);
        if (doc == null) return false;

        // ★ 密级检查
        if (!user.hasClearance(doc.getSensitivityLevel())) {
            log.warn("ABAC 拒绝: 用户 {} 密级 {} 无法访问密级 {} 的文档 {}",
                user.getUsername(), user.getClearanceLevel(),
                doc.getSensitivityLevel(), documentId);
            return false;
        }

        // ★ 域检查
        return canQueryDomain(user, doc.getDomain());
    }

    /**
     * 用户能否上传文档到指定域
     * 额外要求：职级 ≥ manager
     *
     * 使用方式: @PreAuthorize("@abac.canUploadToDomain(principal, #domain)")
     */
    public boolean canUploadToDomain(UserPrincipal user, String domain) {
        if (user.isAdmin()) return true;
        if (!canQueryDomain(user, domain)) return false;

        boolean isManager = "manager".equals(user.getTitle())
                         || "director".equals(user.getTitle());
        if (!isManager) {
            log.warn("ABAC 拒绝: 用户 {} 职级 {} 无法上传文档 (需要 manager+)",
                user.getUsername(), user.getTitle());
        }
        return isManager;
    }

    /**
     * 用户能否删除指定文档
     * 额外要求：职级 == director
     */
    public boolean canDeleteDocument(UserPrincipal user, Long documentId) {
        if (user.isAdmin()) return true;
        if (!canAccessDocument(user, documentId)) return false;

        boolean isDirector = "director".equals(user.getTitle());
        if (!isDirector) {
            log.warn("ABAC 拒绝: 用户 {} 职级 {} 无法删除文档 (需要 director)",
                user.getUsername(), user.getTitle());
        }
        return isDirector;
    }

    // ==================== 用户管理权限 ====================

    /**
     * 用户能否修改用户信息
     * admin → 允许所有；普通用户 → 只能改自己的非敏感字段
     */
    public boolean canModifyUser(UserPrincipal user, Long targetUserId) {
        if (user.isAdmin()) return true;
        if (user.getUserId().equals(targetUserId)) return true;
        log.warn("ABAC 拒绝: 用户 {} 无法修改用户 {} 的信息",
            user.getUsername(), targetUserId);
        return false;
    }

    // ==================== 数据源管理 ====================

    /**
     * 只有 admin 可以管理数据源
     */
    public boolean canManageDatasource(UserPrincipal user) {
        return user.isAdmin();
    }
}
```

#### 3.2.9 JwtUtil 扩展（ABAC 属性编码） `[P2]`

```java
/**
 * JWT 工具类 v2 —— 支持 ABAC 属性
 */
@Component
public class JwtUtil {

    @Value("${jwt.secret}") private String secret;
    @Value("${jwt.access-expiration:1800}") private Long accessExpiration;     // 30min
    @Value("${jwt.refresh-expiration:604800}") private Long refreshExpiration; // 7day

    /**
     * 生成 Access Token（含 ABAC 属性）
     */
    public String generateAccessToken(SysUser user) {
        Date now = new Date();
        return Jwts.builder()
            .setSubject(user.getUsername())
            .claim("userId", user.getId())
            .claim("role", user.getRole())
            .claim("department", user.getDepartment())         // ★ ABAC
            .claim("clearanceLevel", user.getClearanceLevel()) // ★ ABAC
            .claim("allowedDomains", user.getAllowedDomains()) // ★ ABAC
            .claim("title", user.getTitle())                   // ★ ABAC
            .setIssuedAt(now)
            .setExpiration(new Date(now.getTime() + accessExpiration * 1000))
            .signWith(SignatureAlgorithm.HS256, secret)
            .compact();
    }

    /**
     * 生成 Refresh Token（不含 ABAC 属性，仅用于刷新）
     */
    public String generateRefreshToken(SysUser user) {
        Date now = new Date();
        return Jwts.builder()
            .setSubject(user.getUsername())
            .claim("userId", user.getId())
            .claim("tokenType", "refresh")
            .setIssuedAt(now)
            .setExpiration(new Date(now.getTime() + refreshExpiration * 1000))
            .signWith(SignatureAlgorithm.HS256, secret)
            .compact();
    }

    /** 从 Token 解析 ABAC 用户主体 */
    public UserPrincipal parseUserPrincipal(String token) {
        Claims claims = getClaimsFromToken(token);
        return UserPrincipal.fromClaims(claims);
    }
}
```

#### 3.2.10 JwtAuthenticationFilter 扩展（注入 ABAC UserPrincipal） `[P2]`

```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    @Autowired private JwtUtil jwtUtil;

    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                     HttpServletResponse response,
                                     FilterChain chain)
            throws ServletException, IOException {

        String token = extractToken(request);
        if (token == null || !jwtUtil.validateToken(token)) {
            chain.doFilter(request, response);
            return;
        }

        // ★ 解析 ABAC 扩展用户主体
        UserPrincipal user = jwtUtil.parseUserPrincipal(token);

        // ★ 构建包含 ABAC 属性的认证对象
        List<GrantedAuthority> authorities = new ArrayList<>();
        authorities.add(new SimpleGrantedAuthority("ROLE_" + user.getRole().toUpperCase()));
        // 将 ABAC 属性也编码为 GrantedAuthority（可选，用于 hasAuthority 匹配）
        user.getAllowedDomains().forEach(d ->
            authorities.add(new SimpleGrantedAuthority("DOMAIN_" + d)));

        UsernamePasswordAuthenticationToken authentication =
            new UsernamePasswordAuthenticationToken(user, null, authorities);
        SecurityContextHolder.getContext().setAuthentication(authentication);

        chain.doFilter(request, response);
    }
}
```

#### 3.2.11 SecurityConfig 改造（ABAC 方法级安全 + 角色路由） `[P2]`

```java
@Configuration
@EnableWebSecurity
@EnableMethodSecurity(prePostEnabled = true)  // ★ 开启 @PreAuthorize
public class SecurityConfig {

    @Autowired
    private JwtAuthenticationFilter jwtFilter;

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(AbstractHttpConfigurer::disable)
            .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/login", "/api/auth/register",
                                 "/api/auth/refresh").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .requestMatchers("/api/knowledge/**", "/api/chat/**",
                                 "/api/catalog/**").authenticated()
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder(12);
    }
}
```

#### 3.2.12 Controller 层 ABAC 注解示例 `[P2]`

```java
// ========== ChatController ==========
@RestController
@RequestMapping("/api/chat")
public class ChatController {

    /** ★ RAG 问答：检查用户是否有权访问请求的业务域 */
    @PostMapping("/ask")
    @PreAuthorize("@abac.canQueryDomain(principal, #request.domain)")
    public SseEmitter ask(@RequestBody ChatRequest request) {
        UserPrincipal user = (UserPrincipal) SecurityContextHolder
            .getContext().getAuthentication().getPrincipal();
        return chatQueryService.askStream(request, user);
    }
}

// ========== KnowledgeController ==========
@RestController
@RequestMapping("/api/knowledge")
public class KnowledgeController {

    /** ★ 查看文档：检查密级 + 域权限 */
    @GetMapping("/documents/{id}")
    @PreAuthorize("@abac.canAccessDocument(principal, #id)")
    public Result<DocumentVO> getDocument(@PathVariable Long id) { ... }

    /** ★ 上传文档：检查域权限 + 职级 (manager+) */
    @PostMapping("/upload")
    @PreAuthorize("@abac.canUploadToDomain(principal, #domain)")
    public Result<Document> upload(@RequestParam MultipartFile file,
                                   @RequestParam String domain,
                                   @RequestParam(defaultValue = "1") Integer sensitivityLevel) { ... }

    /** ★ 删除文档：检查 director 职级 */
    @DeleteMapping("/{id}")
    @PreAuthorize("@abac.canDeleteDocument(principal, #id)")
    public Result<Void> delete(@PathVariable Long id) { ... }
}

// ========== AdminController (用户管理) ==========
@RestController
@RequestMapping("/api/admin")
public class AdminController {

    /** ★ 修改用户：admin 或本人可修改 */
    @PutMapping("/users/{id}")
    @PreAuthorize("@abac.canModifyUser(principal, #id)")
    public Result<Void> updateUser(@PathVariable Long id, @RequestBody UpdateUserDTO dto) { ... }

    /** 数据源管理：仅 admin */
    @PostMapping("/datasource")
    @PreAuthorize("@abac.canManageDatasource(principal)")
    public Result<Void> createDatasource(@RequestBody DatasourceDTO dto) { ... }
}
```

#### 3.2.13 全局 ABAC 异常处理 `[P2]`

```java
/**
 * ABAC 权限拒绝的自定义异常处理
 * 替代 Spring Security 默认的 403 空白页，返回结构化 JSON
 */
@RestControllerAdvice
public class AbacAccessDeniedHandler {

    /**
     * 处理 @PreAuthorize 拒绝
     */
    @ExceptionHandler(AccessDeniedException.class)
    public ResponseEntity<Result<Void>> handleAccessDenied(AccessDeniedException e,
                                                            HttpServletRequest request) {
        Authentication auth = SecurityContextHolder.getContext().getAuthentication();
        UserPrincipal user = (auth != null && auth.getPrincipal() instanceof UserPrincipal)
            ? (UserPrincipal) auth.getPrincipal() : null;

        String username = user != null ? user.getUsername() : "anonymous";
        String traceId = MDC.get("traceId");

        // ★ 结构化审计日志
        Map<String, Object> auditLog = new HashMap<>();
        auditLog.put("user", username);
        auditLog.put("department", user != null ? user.getDepartment() : "N/A");
        auditLog.put("uri", request.getRequestURI());
        auditLog.put("method", request.getMethod());
        auditLog.put("traceId", traceId);
        auditLog.put("timestamp", LocalDateTime.now().toString());
        auditLog.put("reason", e.getMessage());

        log.warn("ABAC 权限拒绝: {}", JSON.toJSONString(auditLog));

        Result<Void> result = Result.error(403,
            String.format("权限不足: %s", e.getMessage()));
        return ResponseEntity.status(403).body(result);
    }
}
```

***

## 4. 用户管理模块详细设计 (agent-qr-user)

> Maven 模块: `agent-qr-user`
> 基础包路径: `org.example.agent_qr.user`
> 模块依赖: `agent-qr-common`

### 4.0 包结构（P1 / P2 全量）

```
agent-qr-user/
└── src/main/java/org/example/agent_qr/user/
    ├── controller/
    │   └── [P1] AdminController.java          // GET/POST/PUT /api/admin/users (CRUD + toggleStatus)
    ├── entity/
    │   └── [P1→P2] SysUser.java               // [P1] 基础字段 [P2] ABAC扩展字段(department/clearanceLevel/allowedDomains/title)
    ├── mapper/
    │   └── [P1] SysUserMapper.java            // selectByUsername, selectPage(分页关键词搜索)
    ├── handler/
    │   └── [P1] MyMetaObjectHandler.java      // MyBatis-Plus 自动填充 createTime/updateTime
    └── dto/
        ├── [P1] CreateUserDTO.java            // 新增用户 DTO
        └── [P1] UpdateUserDTO.java            // 编辑用户 DTO

agent-qr-user/pom.xml 依赖:
  [P1] agent-qr-common, mybatis-plus-spring-boot3-starter, mysql-connector-j
  [P2] (无新增依赖)
```

### 4.1 类设计

#### 4.1.1 AdminController `[P1 基础 + P2 ABAC注解]`

| 端点                                 | 方法                               | 说明       | 权限    |
| ---------------------------------- | -------------------------------- | -------- | ----- |
| `GET /api/admin/users`             | `listUsers(page, size, keyword)` | 分页查询用户列表 | ADMIN |
| `POST /api/admin/users`            | `createUser(CreateUserDTO)`      | 新增用户     | ADMIN |
| `PUT /api/admin/users/{id}`        | `updateUser(id, UpdateUserDTO)`  | 编辑用户信息   | ADMIN |
| `PUT /api/admin/users/{id}/status` | `toggleStatus(id, status)`       | 启用/禁用用户  | ADMIN |

#### 4.1.2 SysUserMapper `[P1]`

```java
/**
 * 系统用户数据访问层
 */
@Mapper
public interface SysUserMapper extends BaseMapper<SysUser> {

    /**
     * 根据用户名查询用户
     * @param username 用户名
     * @return 用户实体
     */
    @Select("SELECT * FROM sys_user WHERE username = #{username}")
    SysUser selectByUsername(@Param("username") String username);

    /**
     * 分页查询用户列表（支持关键词搜索）
     * @param page 分页参数
     * @param keyword 搜索关键词（匹配用户名/真实姓名）
     * @return 分页结果
     */
    @Select("<script>" +
            "SELECT * FROM sys_user " +
            "<where>" +
            "  <if test='keyword != null and keyword != \"\"'>" +
            "    AND (username LIKE CONCAT('%',#{keyword},'%') " +
            "    OR real_name LIKE CONCAT('%',#{keyword},'%'))" +
            "  </if>" +
            "</where>" +
            "ORDER BY create_time DESC" +
            "</script>")
    IPage<SysUser> selectPage(Page<SysUser> page, @Param("keyword") String keyword);
}
```

#### 4.1.3 SysUser 实体 `[P1 基础字段 + P2 ABAC字段]`

```java
/**
 * 系统用户实体
 */
@Data
@TableName("sys_user")
public class SysUser {

    /** 用户ID */
    @TableId(type = IdType.AUTO)
    private Long id;

    /** 用户名 */
    private String username;

    /** 密码（BCrypt 加密） */
    private String password;

    /** 真实姓名 */
    private String realName;

    /** 邮箱 */
    private String email;

    /** 手机号 */
    private String phone;

    /** 角色：admin/user */
    private String role;

    /** ★ ABAC: 所属部门 HR/FINANCE/RD/SALES/COMMON */
    private String department;

    /** ★ ABAC: 数据密级 0=公开 1=内部 2=机密 3=绝密 */
    private Integer clearanceLevel;

    /** ★ ABAC: 允许访问的业务域（逗号分隔） */
    private String allowedDomains;

    /** ★ ABAC: 职级 employee/manager/director */
    private String title;

    /** 状态：1-启用，0-禁用 */
    private Integer status;

    /** 创建时间 */
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;

    /** 更新时间 */
    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```

***

## 5. 知识库模块详细设计 (agent-qr-knowledge)

> Maven 模块: `agent-qr-knowledge`
> 基础包路径: `org.example.agent_qr.knowledge`
> 模块依赖: `agent-qr-common`、`agent-qr-rag`
> ★ 设计约束：**禁止依赖 `agent-qr-compensation`**。软删除通过发布 `DocumentDeleteRequestedEvent` 事件实现，由 compensation 模块的 `DocumentDeleteListener` 监听处理，避免循环依赖。

### 5.0 包结构（P1 / P2 全量）

```
agent-qr-knowledge/
└── src/main/java/org/example/agent_qr/knowledge/
    ├── controller/
    │   └── [P1] KnowledgeController.java          // POST upload, GET documents/{id}, DELETE, GET status/chunks
    ├── entity/
    │   ├── [P1→P2] Document.java                 // [P1] 基础字段 [P2] domain/sensitivityLevel/sensitivityLabel 字段
    │   └── [P1] Chunk.java                        // 文本切片实体（含 chromaId 关联）
    ├── mapper/
    │   ├── [P1→P2] DocumentMapper.java               // [P1] updateStatus [P2] softDelete, updateErrorMsg
    │   └── [P1→P2] ChunkMapper.java                  // [P1] deleteByDocumentId [P2] softDeleteByDocumentId, selectChromaIdsByDocumentId
    ├── enums/
    │   └── [P1] DocumentStatus.java              // UPLOADED→PARSING→CHUNKING→EMBEDDING→READY / FAILED / DELETING
    ├── service/
    │   ├── [P1→P2] DocumentCommandService.java    // [P1] 上传+删除 [P2] ★ 事件驱动软删除v2（发布DocumentDeleteRequestedEvent，不注入compensation类）
    │   ├── [P1] DocumentQueryService.java         // listDocuments(分页), getDocument, getStatus, getChunks
    │   └── [P1] FileStorageService.java           // store(按日期分目录+时间戳唯一名), delete
    ├── parser/
    │   ├── [P1] DocumentParserService.java        // 解析路由（switch fileType → Pdf/Docx/Text）
    │   ├── [P1→P2] PdfParser.java                // [P1] PDFBox 基础文本 [P2] 流式逐页+表格Markdown+OCR
    │   ├── [P1→P2] DocxParser.java               // [P1] POI 段落文本 [P2] 表格→Markdown 转换
    │   └── [P1] TextParser.java                   // TXT/MD 纯文本读取（UTF-8）
    ├── splitter/
    │   └── [P1] TextSplitter.java                 // 递归字符分割（chunkSize=500, overlap=50）
    └── listener/
        ├── [P1→P2] DocumentParseListener.java     // [P1] 监听 DocumentUploadedEvent [P2] DLQ 失败入队
        └── [P1→P2] ChunkEmbeddingListener.java    // [P1] 切片+向量化链 [P2] DLQ 集成+BatchEmbedding

agent-qr-knowledge/pom.xml 依赖:
  [P1] agent-qr-common, agent-qr-rag, pdfbox, poi-ooxml, spring-boot-starter-web
  [P2] poi-ooxml (表格识别增强), tika-parsers (PDF表格检测), tesseract (OCR可选)
  ★ 禁止依赖 agent-qr-compensation（软删除通过发布 common 中的 DocumentDeleteRequestedEvent 实现解耦）
```

### 5.1 类图

```
┌──────────────────────────┐
│  KnowledgeController      │
├──────────────────────────┤
│ - documentCommandService │
│ - documentQueryService   │
├──────────────────────────┤
│ + uploadDocument(file)   │
│ + listDocuments(page)    │
│ + getDocument(id)        │
│ + deleteDocument(id)     │
│ + getDocumentStatus(id)  │
│ + getChunks(docId)       │
└──────────┬───────────────┘
           │
    ┌──────┴──────────────────┐
    │                         │
    ▼                         ▼
┌───────────────────┐  ┌───────────────────┐
│DocumentCommandSvc │  │ DocumentQuerySvc   │
├───────────────────┤  ├───────────────────┤
│+ uploadDocument() │  │+ listDocuments()  │
│+ deleteDocument() │  │+ getDocument()    │
│                   │  │+ getStatus()      │
└────────┬──────────┘  │+ getChunks()      │
         │              └───────────────────┘
         │ 发布事件
         ▼
┌───────────────────┐
│ ApplicationEvent  │
│ Publisher         │
└───────────────────┘

依赖服务：
┌───────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│ FileStorageSvc    │  │ DocumentParserSvc │  │ TextSplitter     │
├───────────────────┤  ├──────────────────┤  ├──────────────────┤
│+ store(file):path │  │+ parse(path):str │  │+ split(text)     │
│+ delete(path)     │  │(PDF/DOCX/TXT/MD) │  │  : List<String>  │
└───────────────────┘  └──────────────────┘  └──────────────────┘
```

### 5.2 核心类详细设计

#### 5.2.1 DocumentCommandService `[P1 基础上传 + P2 软删除v2]`

```java
/**
 * 文档命令服务（写操作）
 * 处理文档上传、删除等写操作，上传后发布事件触发异步处理链
 */
@Service
@Slf4j
public class DocumentCommandService {

    @Autowired
    private DocumentMapper documentMapper;
    @Autowired
    private ChunkMapper chunkMapper;
    @Autowired
    private FileStorageService fileStorageService;
    @Autowired
    private ApplicationEventPublisher eventPublisher;

    /** 允许的文件类型 */
    private static final Set<String> ALLOWED_TYPES =
            Set.of("pdf", "docx", "txt", "md");

    /** 文件大小上限 */
    private static final long MAX_FILE_SIZE = 50 * 1024 * 1024; // 50MB

    /**
     * 上传文档（同步保存文件+创建记录，异步处理内容）
     * @param file 上传的文件
     * @param title 文档标题（可选）
     * @param userId 上传用户ID
     * @return 创建的文档记录
     */
    @Transactional
    public Document uploadDocument(MultipartFile file, String title, Long userId) {
        // 1. 校验文件类型
        String fileType = getFileExtension(file.getOriginalFilename());
        if (!ALLOWED_TYPES.contains(fileType)) {
            throw new BusinessException("不支持的文件格式，仅支持: pdf, docx, txt, md");
        }

        // 2. 校验文件大小
        if (file.getSize() > MAX_FILE_SIZE) {
            throw new BusinessException("文件大小不能超过 50MB");
        }

        // 3. 保存文件到服务器
        String filePath = fileStorageService.store(file);

        // 4. 创建文档记录
        Document doc = new Document();
        doc.setTitle(title != null ? title : file.getOriginalFilename());
        doc.setFileName(file.getOriginalFilename());
        doc.setFilePath(filePath);
        doc.setFileType(fileType);
        doc.setFileSize(file.getSize());
        doc.setStatus(DocumentStatus.UPLOADED.name());
        doc.setUploadUserId(userId);
        documentMapper.insert(doc);

        // 5. 发布文档上传事件（触发异步处理链）
        DocumentUploadedEvent event = new DocumentUploadedEvent();
        event.setDocumentId(doc.getId());
        event.setFilePath(filePath);
        event.setFileName(file.getOriginalFilename());
        event.setFileType(fileType);
        event.setUserId(userId);
        eventPublisher.publishEvent(event);

        log.info("文档上传成功: id={}, name={}", doc.getId(), doc.getFileName());
        return doc; // 立即返回，后续异步处理
    }

    /**
     * [P1] 删除文档及其关联数据（直接删除，P2 阶段废弃）
     * @deprecated P2 阶段由 {@link #requestDeleteDocument(Long)} 替代，
     *             采用事件驱动软删除策略，由 compensation 模块异步物理删除
     * @param documentId 文档ID
     */
    @Transactional
    @Deprecated
    public void deleteDocument(Long documentId) {
        Document doc = documentMapper.selectById(documentId);
        if (doc == null) {
            throw new BusinessException(404, "文档不存在");
        }

        // 1. 删除服务器文件
        fileStorageService.delete(doc.getFilePath());

        // 2. 删除切片记录
        chunkMapper.deleteByDocumentId(documentId);

        // 3. 删除 ChromaDB 向量（通过 chroma_id）
        chromaVectorStore.deleteByDocumentId(documentId);

        // 4. 删除文档记录
        documentMapper.deleteById(documentId);
        log.info("文档删除成功: id={}", documentId);
    }

    /**
     * ★ [P2] 事件驱动的软删除请求
     * 采用"先标记后清理"策略：标记 DELETING → 收集切片信息 → 发布事件 → 立即返回
     *
     * 关键设计：
     * 1. knowledge 模块只发布事件，不注入 compensation 的任何类 → 无循环依赖
     * 2. compensation 模块的 DocumentDeleteListener 通过 @TransactionalEventListener 接收事件
     * 3. 用户在 Controller 调用后立即得到 200 响应，物理删除在后台异步完成
     *
     * @param documentId 文档ID
     */
    @Transactional
    public void requestDeleteDocument(Long documentId) {
        Document doc = documentMapper.selectById(documentId);
        if (doc == null) {
            throw new BusinessException(404, "文档不存在");
        }
        if ("DELETING".equals(doc.getStatus())) {
            throw new BusinessException("文档正在删除中，请稍后重试");
        }

        // 1. 标记文档状态为 DELETING（防止并发删除）
        documentMapper.updateStatus(documentId, DocumentStatus.DELETING.name());

        // 2. 收集切片列表和 ChromaDB 向量 ID
        List<Chunk> chunks = chunkMapper.selectByDocumentId(documentId);
        List<String> chromaIds = chunkMapper.selectChromaIdsByDocumentId(documentId);

        // 3. ★ 发布 DocumentDeleteRequestedEvent 事件（定义在 agent-qr-common，无循环依赖）
        DocumentDeleteRequestedEvent event = new DocumentDeleteRequestedEvent();
        event.setDocumentId(documentId);
        event.setChunkIds(chunks.stream().map(Chunk::getId).collect(Collectors.toList()));
        event.setChromaIds(chromaIds);
        event.setFilePath(doc.getFilePath());
        eventPublisher.publishEvent(event);

        // 4. ★ 立即返回，不等待物理删除完成
        //    compensation 模块的 DocumentDeleteListener 在事务提交后异步执行：
        //    → MySQL 逻辑删除（chunkMapper.softDeleteByDocumentId + documentMapper.softDelete）
        //    → ChromaDB 物理删除（DocumentDeleteServiceV2.asyncPhysicalDelete）
        log.info("文档删除请求已发布: id={}, chromaIds={}", documentId, chromaIds.size());
    }

    /**
     * 获取文件扩展名
     */
    private String getFileExtension(String fileName) {
        if (fileName == null) return "";
        int dotIndex = fileName.lastIndexOf('.');
        return dotIndex == -1 ? "" : fileName.substring(dotIndex + 1).toLowerCase();
    }
}
```

#### 5.2.2 FileStorageService `[P1]`

```java
/**
 * 文件存储服务
 * 将上传文件保存到本地 ./uploads/ 目录，按日期分文件夹
 */
@Service
public class FileStorageService {

    @Value("${file.upload-dir:./uploads}")
    private String uploadDir;

    /**
     * 存储文件
     * @param file 上传文件
     * @return 文件存储路径（相对路径）
     */
    public String store(MultipartFile file) {
        try {
            // 按日期创建子目录
            String dateDir = LocalDate.now().format(DateTimeFormatter.ofPattern("yyyy/MM"));
            Path dirPath = Paths.get(uploadDir, dateDir);
            Files.createDirectories(dirPath);

            // 生成唯一文件名（时间戳 + 原始文件名）
            String uniqueName = System.currentTimeMillis() + "_" + file.getOriginalFilename();
            Path filePath = dirPath.resolve(uniqueName);

            // 写入文件
            file.transferTo(filePath.toFile());
            return filePath.toString();
        } catch (IOException e) {
            throw new BusinessException("文件存储失败: " + e.getMessage());
        }
    }

    /**
     * 删除文件
     * @param filePath 文件路径
     */
    public void delete(String filePath) {
        try {
            Files.deleteIfExists(Paths.get(filePath));
        } catch (IOException e) {
            log.warn("文件删除失败: {}", filePath, e);
        }
    }
}
```

#### 5.2.3 DocumentParserService `[P1 基础解析 + P2 表格/OCR]`

```java
/**
 * 文档解析服务
 * 根据文件类型调用不同的解析器
 */
@Service
public class DocumentParserService {

    @Autowired
    private PdfParser pdfParser;
    @Autowired
    private DocxParser docxParser;
    @Autowired
    private TextParser textParser; // 处理 txt 和 md

    /**
     * 解析文档内容为纯文本
     * @param filePath 文件路径
     * @param fileType 文件类型
     * @return 解析后的文本内容
     */
    public String parse(String filePath, String fileType) {
        return switch (fileType.toLowerCase()) {
            case "pdf" -> pdfParser.parse(filePath);
            case "docx" -> docxParser.parse(filePath);
            case "txt", "md" -> textParser.parse(filePath);
            default -> throw new BusinessException("不支持的文件类型: " + fileType);
        };
    }
}

/**
 * PDF 文档解析器（基于 Apache PDFBox + Tika 表格识别 + OCR）
 */
@Component
public class PdfParser {

    @Value("${parser.ocr.enabled:false}")
    private boolean ocrEnabled;  // OCR 开关（默认关闭以节省资源）

    /** 解析 PDF 为结构化文本（表格转为 Markdown 格式） */
    public String parse(String filePath) {
        try (PDDocument document = Loader.loadPDF(new File(filePath))) {
            StringBuilder result = new StringBuilder();

            // 1. 逐页提取文本
            PDFTextStripper stripper = new PDFTextStripper();
            stripper.setSortByPosition(true); // 按位置排序
            String text = stripper.getText(document);
            result.append(text);

            // 2. 使用 Tika 识别表格 → Markdown 格式
            try (InputStream is = new FileInputStream(filePath)) {
                ContentHandler handler = new ToXMLContentHandler();
                Metadata metadata = new Metadata();
                ParseContext context = new ParseContext();
                new PDFParser().parse(is, handler, metadata, context);
                // 将表格转为 Markdown 表格格式保留结构
                String tableMarkdown = extractTablesAsMarkdown(handler.toString());
                if (!tableMarkdown.isEmpty()) {
                    result.append("\n\n## 文档表格内容\n\n").append(tableMarkdown);
                }
            }

            // 3. OCR 识别扫描页（可选）
            if (ocrEnabled && isScannedPdf(document)) {
                String ocrText = performOcr(filePath);
                result.append("\n\n## OCR 识别内容\n\n").append(ocrText);
            }

            return result.toString();
        } catch (IOException e) {
            throw new BusinessException("PDF 解析失败: " + e.getMessage());
        }
    }

    /** 判断是否为扫描版 PDF（页数多但文本极少） */
    private boolean isScannedPdf(PDDocument document) {
        try {
            PDFTextStripper stripper = new PDFTextStripper();
            String text = stripper.getText(document);
            int pages = document.getNumberOfPages();
            // 平均每页字符数 < 50 判定为扫描件
            return pages > 0 && text.length() / pages < 50;
        } catch (Exception e) {
            return false;
        }
    }

    /** Tesseract OCR 识别 */
    private String performOcr(String filePath) { ... }

    /** 从 Tika 输出中提取表格并转为 Markdown */
    private String extractTablesAsMarkdown(String tikaXml) { ... }
}

/**
 * DOCX 文档解析器（基于 Apache POI + 表格识别）
 */
@Component
public class DocxParser {
    /** 解析 DOCX 为结构化文本（表格转为 Markdown 格式） */
    public String parse(String filePath) {
        try (FileInputStream fis = new FileInputStream(filePath);
             XWPFDocument document = new XWPFDocument(fis)) {
            StringBuilder sb = new StringBuilder();

            // 遍历所有元素（段落 + 表格）
            for (IBodyElement element : document.getBodyElements()) {
                if (element instanceof XWPFParagraph) {
                    sb.append(((XWPFParagraph) element).getText()).append("\n");
                } else if (element instanceof XWPFTable) {
                    // ★ 将表格转为 Markdown 格式
                    sb.append(tableToMarkdown((XWPFTable) element)).append("\n\n");
                }
            }
            return sb.toString();
        } catch (IOException e) {
            throw new BusinessException("DOCX 解析失败: " + e.getMessage());
        }
    }

    /** 将 XWPFTable 转为 Markdown 表格 */
    private String tableToMarkdown(XWPFTable table) {
        StringBuilder md = new StringBuilder();
        List<XWPFTableRow> rows = table.getRows();
        for (int i = 0; i < rows.size(); i++) {
            md.append("|");
            for (XWPFTableCell cell : rows.get(i).getTableCells()) {
                md.append(" ").append(cell.getText().replace("\n", " ")).append(" |");
            }
            md.append("\n");
            // 表头分隔线
            if (i == 0) {
                md.append("|");
                for (int j = 0; j < rows.get(i).getTableCells().size(); j++) {
                    md.append(" --- |");
                }
                md.append("\n");
            }
        }
        return md.toString();
    }
}

/**
 * 纯文本解析器（TXT / MD）
 */
@Component
public class TextParser {
    /** 读取文本文件内容 */
    public String parse(String filePath) {
        try {
            return Files.readString(Paths.get(filePath), StandardCharsets.UTF_8);
        } catch (IOException e) {
            throw new BusinessException("文本文件读取失败: " + e.getMessage());
        }
    }
}
```

#### 5.2.4 文本切片器 `[P1]`

```java
/**
 * 文本切片服务
 * 使用递归字符分割策略，将长文本切分为适合向量化的段落
 */
@Service
public class TextSplitter {

    @Value("${rag.chunk-size:500}")
    private int chunkSize;       // 切片大小（字符数）

    @Value("${rag.chunk-overlap:50}")
    private int chunkOverlap;    // 重叠大小

    /**
     * 将文本切分为多个片段
     * @param text 原始文本
     * @return 文本片段列表
     */
    public List<String> split(String text) {
        // 1. 按段落初步分割
        List<String> paragraphs = Arrays.asList(text.split("\n\n"));

        // 2. 对于超长段落，递归分割
        List<String> chunks = new ArrayList<>();
        for (String paragraph : paragraphs) {
            if (paragraph.trim().isEmpty()) continue;

            if (paragraph.length() <= chunkSize) {
                chunks.add(paragraph.trim());
            } else {
                // 递归分割长段落
                chunks.addAll(splitLongText(paragraph.trim()));
            }
        }

        // 3. 合并过短的片段
        return mergeShortChunks(chunks);
    }

    /**
     * 递归分割长文本
     */
    private List<String> splitLongText(String text) {
        List<String> result = new ArrayList<>();
        int start = 0;
        while (start < text.length()) {
            int end = Math.min(start + chunkSize, text.length());
            // 尝试在标点/空格处断开
            if (end < text.length()) {
                end = findBreakPoint(text, end);
            }
            result.add(text.substring(start, end).trim());
            start = end - chunkOverlap; // 重叠
            if (start >= text.length()) break;
        }
        return result;
    }

    /**
     * 寻找合适的断点（句号、换行等）
     */
    private int findBreakPoint(String text, int end) {
        int searchStart = Math.max(end - 50, 0);
        String sub = text.substring(searchStart, Math.min(end + 1, text.length()));
        // 优先在句号处断开
        for (char sep : new char[]{'。', '\n', '！', '？', ';', '；', '，', ' '}) {
            int idx = sub.lastIndexOf(sep);
            if (idx >= 0) return searchStart + idx + 1;
        }
        return end;
    }

    /**
     * 合并过短片段到前一个片段
     */
    private List<String> mergeShortChunks(List<String> chunks) {
        List<String> result = new ArrayList<>();
        for (String chunk : chunks) {
            if (!result.isEmpty() && chunk.length() < 100) {
                result.set(result.size() - 1, result.get(result.size() - 1) + "\n" + chunk);
            } else {
                result.add(chunk);
            }
        }
        return result;
    }
}
```

#### 5.2.5 DocumentStatus 枚举

```java
/**
 * 文档处理状态枚举
 */
public enum DocumentStatus {

    /** 已上传，等待处理 */
    UPLOADED("已上传"),

    /** 正在解析文档 */
    PARSING("解析中"),

    /** 正在切片 */
    CHUNKING("切片中"),

    /** 正在生成向量 */
    EMBEDDING("向量化中"),

    /** 处理完成，可检索 */
    READY("就绪"),

    /** 处理失败 */
    FAILED("失败"),

    /** 正在删除 */
    DELETING("删除中");

    private final String description;

    DocumentStatus(String description) {
        this.description = description;
    }

    public String getDescription() {
        return description;
    }
}
```

***

## 6. RAG 问答模块详细设计 (agent-qr-rag)

> Maven 模块: `agent-qr-rag`
> 基础包路径: `org.example.agent_qr.rag`
> 模块依赖: `agent-qr-common`

### 6.0 包结构（P1 / P2 / P3 全量）

```
agent-qr-rag/
└── src/main/java/org/example/agent_qr/rag/
    ├── controller/
    │   └── [P1] ChatController.java                  // POST /api/chat/ask, GET conversations, GET/DELETE messages
    ├── service/
    │   ├── [P1→P2] ChatQueryService.java             // [P1] 同步 ask() [P2] SSE 流式 askStream()
    │   └── [P1] ConversationService.java             // 会话管理（createConversation/listConversations/delete）
    ├── provider/
    │   ├── [P1] LLMProvider.java                     // LLM 策略接口：generate() + generateStream()
    │   ├── [P1] EmbeddingProvider.java               // Embedding 策略接口：embed() + embedBatch()
    │   ├── [P1] ProviderFactory.java                 // Provider 工厂（根据 yml 配置选择实现）
    │   ├── deepseek/
    │   │   ├── [P1] DeepSeekLLMProvider.java         // DeepSeek Chat API (OpenAI 兼容)
    │   │   └── [P1] DeepSeekEmbeddingProvider.java   // DeepSeek Embedding API
    │   └── ollama/
    │       ├── [P2] OllamaLLMProvider.java           // Ollama 本地 LLM (正式部署选项)
    │       └── [P2] OllamaEmbeddingProvider.java     // Ollama 本地 Embedding
    ├── retriever/
    │   ├── [P1] ChromaRetriever.java                 // ChromaDB 语义向量检索器
    │   ├── [P2] HybridRetriever.java                 // 混合检索器（语义+BM25+RRF+Rerank）
    │   ├── [P2] BM25Retriever.java                   // Lucene 内存索引 BM25 关键词检索
    │   └── [P2] RerankerService.java                 // bge-reranker-v2-m3 交叉编码器精排
    ├── filter/
    │   ├── [P2] StructuredFilterService.java         // MySQL B+树前置结构化过滤（数值/日期/枚举）
    │   └── mapper/
    │       └── [P2] ChunkStructuredMapper.java       // 结构化过滤 Mapper（kb_chunk_structured 表）
    ├── circuitbreaker/
    │   └── [P2] LLMCircuitBreaker.java               // LLM 熔断器（CLOSED→OPEN→HALF_OPEN 状态机）
    ├── router/
    │   ├── [P2] DomainRouter.java                    // 业务域路由器（关键词匹配方式）
    │   └── [P3] DomainRouterV2.java                  // 业务域路由器（Embedding 语义匹配方式）
    ├── embedding/
    │   ├── [P2] BatchEmbeddingService.java           // 批量向量化攒批服务（BlockingQueue 生产者-消费者）
    │   └── [P3] EmbeddingDimensionManager.java       // Collection 维度检测与模型隔离
    ├── prompt/
    │   └── [P1] PromptTemplate.java                  // RAG Prompt 模板构建器
    ├── entity/
    │   ├── [P1] Conversation.java                    // 会话实体（chat_conversation 表）
    │   ├── [P1] Message.java                         // 消息实体（chat_message 表）
    │   └── [P1] RetrievedDocument.java              // 检索结果文档模型
    └── mapper/
        ├── [P1] ConversationMapper.java              // selectByUserId, incrementMessageCount
        └── [P1] MessageMapper.java                   // selectByConversationId, updateFeedback

agent-qr-rag/pom.xml 依赖:
  [P1] agent-qr-common, langchain4j, langchain4j-open-ai, langchain4j-chroma, spring-boot-starter-webflux
  [P2] lucene-core, lucene-queryparser, lucene-analyzers-smartcn (BM25), spring-boot-starter-webflux (SSE)
  [P3] (无新增依赖)
```

### 6.1 类图

```
┌──────────────────────┐
│   ChatController      │
├──────────────────────┤
│ - chatQueryService   │
│ - conversationSvc    │
├──────────────────────┤
│ + askStream(ChatReq) │  ← SSE 流式端点
│ + conversations()    │
│ + messages(convId)   │
│ + deleteConv(id)     │
│ + feedback(msgId, fb)│  ← 答案反馈评价
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────┐
│     ChatQueryService          │
├──────────────────────────────┤
│ - providerFactory            │
│ - hybridRetriever            │  ← ★ 混合检索器
│ - promptTemplate             │
│ - circuitBreaker             │  ← ★ LLM 熔断器
│ - conversationService        │
│ - messageMapper              │
│ - eventPublisher             │
├──────────────────────────────┤
│ + askStream(q, cId, uId,     │
│            emitter): void    │
│ + submitFeedback(msgId, fb)  │
└──────────┬───────────────────┘
           │
    ┌──────┼────────────────────────────┐
    │      │              │             │
    ▼      ▼              ▼             ▼
┌────────┐┌──────────────┐┌──────────┐┌──────────────┐
│Provider││HybridRetriever││LLMCircuit││PromptTemplate│
│Factory ││(语义+BM25+    ││Breaker   ││              │
│        ││ Rerank)      ││          ││              │
└────────┘└──────────────┘└──────────┘└──────────────┘
```

### 6.2 核心类详细设计

#### 6.2.1 ChatQueryService（RAG 问答核心） `[P1 sync + P2 SSE流式]`

```java
/**
 * 问答查询服务（CQRS 读端）
 * 实现完整的 RAG 检索增强生成流程
 */
@Service
@Slf4j
public class ChatQueryService {

    @Autowired
    private ProviderFactory providerFactory;

    @Autowired
    private HybridRetriever hybridRetriever;       // ★ 混合检索器

    @Autowired
    private PromptTemplate promptTemplate;

    @Autowired
    private ConversationService conversationService;

    @Autowired
    private MessageMapper messageMapper;

    @Autowired
    private ApplicationEventPublisher eventPublisher;

    @Autowired
    private LLMCircuitBreaker circuitBreaker;      // ★ LLM 熔断器

    /**
     * 基于 RAG 的智能问答（流式 SSE）
     *
     * @param query 用户问题
     * @param conversationId 会话ID（null则新建）
     * @param userId 用户ID
     * @param responseEmitter SSE 响应发射器
     */
    public void askStream(String query, Long conversationId, Long userId,
                          SseEmitter responseEmitter) {
        // ===== 1. 会话管理 =====
        if (conversationId == null) {
            conversationId = conversationService.createConversation(userId, query);
        }

        // 保存用户提问
        Message userMsg = saveMessage(conversationId, "user", query, null);

        // ===== 2. 混合检索 =====
        List<RetrievedDocument> retrievedDocs;
        try {
            EmbeddingProvider embedProvider = providerFactory.getEmbeddingProvider();
            float[] queryEmbedding = embedProvider.embed(query);
            // ★ 混合检索：语义 + BM25 → RRF 融合 → Rerank 精排
            retrievedDocs = hybridRetriever.hybridSearch(query, queryEmbedding);
        } catch (Exception e) {
            log.error("文档检索失败", e);
            sendSseEvent(responseEmitter, "error", "知识库检索暂时不可用，请稍后重试");
            responseEmitter.complete();
            return;
        }

        if (retrievedDocs.isEmpty()) {
            sendSseEvent(responseEmitter, "error",
                "知识库中暂无相关信息，请联系管理员上传相关文档");
            responseEmitter.complete();
            return;
        }

        // ===== 3. 构建 Prompt =====
        String contextText = retrievedDocs.stream()
                .map(doc -> "【来源：" + doc.getDocumentTitle() + "】\n" + doc.getContent())
                .collect(Collectors.joining("\n\n---\n\n"));
        String prompt = promptTemplate.build(query, contextText);

        // ===== 4. LLM 熔断器保护 + 流式生成 =====
        List<Message> messages = List.of(
            Message.system("你是一个企业知识库助手，请根据参考资料准确回答用户问题。"),
            Message.user(prompt)
        );

        LLMProvider llmProvider = circuitBreaker.getActiveProvider();
        StringBuilder fullAnswer = new StringBuilder();

        try {
            Flux<String> tokenStream = llmProvider.generateStream(messages);
            tokenStream.subscribe(
                token -> {
                    // ★ 逐 token 通过 SSE 推送到前端
                    fullAnswer.append(token);
                    sendSseEvent(responseEmitter, "token", token);
                },
                error -> {
                    // 流式生成异常
                    log.error("LLM 流式生成失败", error);
                    circuitBreaker.recordFailure();  // 通知熔断器记录失败
                    sendSseEvent(responseEmitter, "error", "AI 回答生成失败");
                    responseEmitter.complete();
                },
                () -> {
                    // 流式生成完成
                    circuitBreaker.recordSuccess();  // 通知熔断器记录成功

                    // 构建来源引用
                    List<SourceVO> sources = retrievedDocs.stream()
                            .map(SourceVO::from)
                            .collect(Collectors.toList());

                    // 保存完整 AI 回答
                    saveMessage(conversationId, "assistant",
                            fullAnswer.toString(), sources);

                    // 发送完成信号（含会话ID和引用来源）
                    Map<String, Object> doneData = new HashMap<>();
                    doneData.put("conversationId", conversationId);
                    doneData.put("sources", sources);
                    sendSseEvent(responseEmitter, "done", doneData);
                    responseEmitter.complete();

                    // 发布统计事件
                    publishStatEvent(userId, conversationId);
                }
            );
        } catch (Exception e) {
            log.error("LLM 调用失败", e);
            sendSseEvent(responseEmitter, "error", "AI 服务暂时不可用，请稍后重试");
            responseEmitter.complete();
        }
    }

    /**
     * 发送 SSE 事件
     */
    private void sendSseEvent(SseEmitter emitter, String eventName, Object data) {
        try {
            emitter.send(SseEmitter.event().name(eventName).data(data));
        } catch (IOException e) {
            log.error("SSE 推送失败", e);
        }
    }

    /**
     * 保存消息到数据库
     */
    private Message saveMessage(Long conversationId, String role,
                                 String content, List<SourceVO> sources) {
        Message msg = new Message();
        msg.setConversationId(conversationId);
        msg.setRole(role);
        msg.setContent(content);
        if (sources != null) {
            msg.setSources(JSON.toJSONString(sources));
        }
        messageMapper.insert(msg);
        // 更新会话消息计数
        conversationService.incrementMessageCount(conversationId);
        return msg;
    }
}
```

#### 6.2.2 ProviderFactory（LLM/Embedding 策略管理） `[P1]`

```java
/**
 * LLM/Embedding Provider 工厂
 * 根据 application.yml 配置决定使用哪个 Provider 实现
 *
 * 切换方式：
 *   DeepSeek 为默认 Provider（当前测试环境使用）
 *   Ollama 为正式部署环境选项
 *   无需修改任何业务代码
 */
@Component
public class ProviderFactory {

    @Value("${llm.provider:deepseek}")
    private String llmProviderType;

    @Value("${embedding.provider:deepseek}")
    private String embeddingProviderType;

    /** DeepSeek Provider（默认，当前测试环境使用） */
    @Autowired(required = false)
    private DeepSeekLLMProvider deepSeekLLMProvider;
    @Autowired(required = false)
    private DeepSeekEmbeddingProvider deepSeekEmbeddingProvider;

    /** Ollama Provider（正式部署环境选项） */
    @Autowired(required = false)
    private OllamaLLMProvider ollamaLLMProvider;
    @Autowired(required = false)
    private OllamaEmbeddingProvider ollamaEmbeddingProvider;

    /**
     * 获取 LLM Provider 实例
     * @return 当前配置的 LLM 实现
     */
    public LLMProvider getLLMProvider() {
        return switch (llmProviderType) {
            case "ollama" -> {
                if (ollamaLLMProvider == null) {
                    throw new BusinessException("Ollama LLM Provider 未配置");
                }
                yield ollamaLLMProvider;
            }
            default -> {
                if (deepSeekLLMProvider == null) {
                    throw new BusinessException("DeepSeek LLM Provider 未配置");
                }
                yield deepSeekLLMProvider;
            }
        };
    }

    /**
     * 获取 Embedding Provider 实例
     * @return 当前配置的 Embedding 实现
     */
    public EmbeddingProvider getEmbeddingProvider() {
        return switch (embeddingProviderType) {
            case "ollama" -> {
                if (ollamaEmbeddingProvider == null) {
                    throw new BusinessException("Ollama Embedding Provider 未配置");
                }
                yield ollamaEmbeddingProvider;
            }
            default -> {
                if (deepSeekEmbeddingProvider == null) {
                    throw new BusinessException("DeepSeek Embedding Provider 未配置");
                }
                yield deepSeekEmbeddingProvider;
            }
        };
    }
}
```

#### 6.2.3 DeepSeekLLMProvider（DeepSeek API 实现） `[P1]`

```java
/**
 * DeepSeek LLM Provider 实现
 * 通过 OpenAI 兼容接口调用 DeepSeek API
 */
@Component
@Slf4j
public class DeepSeekLLMProvider implements LLMProvider {

    @Value("${llm.deepseek.api-key}")
    private String apiKey;

    @Value("${llm.deepseek.base-url:https://api.deepseek.com}")
    private String baseUrl;

    @Value("${llm.deepseek.model:deepseek-chat}")
    private String model;

    @Value("${llm.deepseek.temperature:0.7}")
    private Double temperature;

    @Value("${llm.deepseek.max-tokens:2048}")
    private Integer maxTokens;

    private OpenAiChatModel chatModel;

    @PostConstruct
    public void init() {
        this.chatModel = OpenAiChatModel.builder()
                .baseUrl(baseUrl)
                .apiKey(apiKey)
                .modelName(model)
                .temperature(temperature)
                .maxTokens(maxTokens)
                .build();
    }

    @Override
    public String generate(List<Message> messages) {
        try {
            ChatResponse response = chatModel.chat(messages);
            return response.aiMessage().text();
        } catch (Exception e) {
            log.error("DeepSeek API 调用失败", e);
            throw new BusinessException("AI 服务暂时不可用: " + e.getMessage());
        }
    }

    @Override
    public Flux<String> generateStream(List<Message> messages) {
        // 流式响应（可选实现）
        return chatModel.chatStream(messages);
    }
}
```

#### 6.2.4 DeepSeekEmbeddingProvider（DeepSeek Embedding 实现） `[P1]`

```java
/**
 * DeepSeek Embedding Provider 实现
 * 通过 OpenAI 兼容接口调用 DeepSeek Embedding API
 */
@Component
@Slf4j
public class DeepSeekEmbeddingProvider implements EmbeddingProvider {

    @Value("${embedding.deepseek.api-key}")
    private String apiKey;

    @Value("${embedding.deepseek.base-url:https://api.deepseek.com}")
    private String baseUrl;

    @Value("${embedding.deepseek.model:deepseek-embedding}")
    private String model;

    private OpenAiEmbeddingModel embeddingModel;

    @PostConstruct
    public void init() {
        // 使用 LangChain4j OpenAI 兼容客户端
        this.embeddingModel = OpenAiEmbeddingModel.builder()
                .baseUrl(baseUrl)
                .apiKey(apiKey)
                .modelName(model)
                .build();
    }

    @Override
    public float[] embed(String text) {
        try {
            Embedding embedding = embeddingModel.embed(text);
            return embedding.content().vector();
        } catch (Exception e) {
            log.error("DeepSeek Embedding 调用失败", e);
            throw new BusinessException("向量化服务暂时不可用: " + e.getMessage());
        }
    }

    @Override
    public List<float[]> embedBatch(List<String> texts) {
        List<float[]> result = new ArrayList<>();
        for (String text : texts) {
            result.add(embed(text));
        }
        return result;
    }
}
```

#### 6.2.5 HybridRetriever（混合检索器）★ `[P2]`

```java
/**
 * 混合检索器 —— 语义向量检索 + BM25 关键词检索 + Rerank 重排序
 * 双路召回 → RRF 加权融合 → Rerank 精排 → Top-K 输出
 */
@Component
@Slf4j
public class HybridRetriever {

    @Autowired
    private ChromaRetriever chromaRetriever;    // 语义向量检索

    @Autowired
    private BM25Retriever bm25Retriever;        // 关键词检索

    @Autowired
    private RerankerService rerankerService;    // Rerank 重排序

    @Value("${rag.hybrid.semantic-weight:0.6}")
    private double semanticWeight;

    @Value("${rag.hybrid.keyword-weight:0.4}")
    private double keywordWeight;

    @Value("${rag.retrieval.wide-top-k:20}")
    private int wideTopK;  // 宽召回数量

    @Value("${rag.retrieval.final-top-k:5}")
    private int finalTopK; // 最终返回数量

    /**
     * 混合检索：语义 + 关键词 → 融合 → Rerank → Top-K
     * @param query 用户原始问题
     * @param queryEmbedding 查询向量
     * @return 重排序后的检索结果
     */
    public List<RetrievedDocument> hybridSearch(String query, float[] queryEmbedding) {
        // ===== 1. 双路宽召回 =====
        // 语义检索 Top-K=20
        List<RetrievedDocument> semanticResults =
                chromaRetriever.similaritySearch(queryEmbedding, wideTopK);

        // BM25 关键词检索 Top-K=20
        List<RetrievedDocument> bm25Results =
                bm25Retriever.keywordSearch(query, wideTopK);

        // ===== 2. RRF (Reciprocal Rank Fusion) 加权融合去重 =====
        List<RetrievedDocument> fusedResults = rrfFusion(
                semanticResults, bm25Results, semanticWeight, keywordWeight);

        // ===== 3. Rerank 精排 =====
        List<RetrievedDocument> rerankedResults =
                rerankerService.rerank(query, fusedResults, finalTopK);

        log.info("混合检索完成: 语义={}, BM25={}, 融合后={}, Rerank后={}",
                semanticResults.size(), bm25Results.size(),
                fusedResults.size(), rerankedResults.size());

        return rerankedResults;
    }

    /**
     * RRF 加权融合算法
     * score(doc) = w1 / (k + rank_semantic) + w2 / (k + rank_bm25)
     * 对两路结果按 RRF 分数重新排序，相同内容取最高分
     */
    private List<RetrievedDocument> rrfFusion(
            List<RetrievedDocument> semanticResults,
            List<RetrievedDocument> bm25Results,
            double w1, double w2) {

        final double k = 60.0; // RRF 平滑常数
        Map<String, Double> scoreMap = new HashMap<>();
        Map<String, RetrievedDocument> docMap = new HashMap<>();

        // 语义检索结果计分
        for (int i = 0; i < semanticResults.size(); i++) {
            String key = hashKey(semanticResults.get(i));
            double score = w1 / (k + i + 1);
            scoreMap.merge(key, score, Double::sum);
            docMap.putIfAbsent(key, semanticResults.get(i));
        }

        // BM25 结果计分
        for (int i = 0; i < bm25Results.size(); i++) {
            String key = hashKey(bm25Results.get(i));
            double score = w2 / (k + i + 1);
            scoreMap.merge(key, score, Double::sum);
            docMap.putIfAbsent(key, bm25Results.get(i));
        }

        // 按 RRF 分数降序排列
        return scoreMap.entrySet().stream()
                .sorted(Map.Entry.<String, Double>comparingByValue().reversed())
                .map(e -> docMap.get(e.getKey()))
                .collect(Collectors.toList());
    }

    private String hashKey(RetrievedDocument doc) {
        return String.valueOf(doc.getContent().hashCode());
    }
}
```

#### 6.2.5.1 ChromaRetriever（语义向量检索器） `[P1]`

```java
/**
 * ChromaDB 语义检索器 —— 只负责向量相似度检索
 */
@Component
@Slf4j
public class ChromaRetriever {

    @Value("${langchain4j.chroma.collection-name:enterprise_knowledge}")
    private String collectionName;

    @Autowired
    private ChromaVectorStore chromaVectorStore;

    public List<RetrievedDocument> similaritySearch(float[] queryEmbedding, int topK) { ... }
    public void deleteByDocumentId(Long documentId) { ... }
}
```

#### 6.2.5.2 BM25Retriever（关键词检索器） `[P2]`

```java
/**
 * BM25 关键词检索器
 * 基于 Lucene 内存索引，解决企业术语（产品代号、项目编号等）
 * 在语义检索中容易丢失的问题
 */
@Component
@Slf4j
public class BM25Retriever {

    /** 内存索引管理器 */
    private volatile IndexSearcherManager indexManager;

    @Autowired
    private ChunkMapper chunkMapper;

    /**
     * 应用启动时从 MySQL 加载全量切片构建 Lucene 内存索引
     * 文档变更时通过事件触发增量更新
     */
    @PostConstruct
    public void buildIndex() {
        List<Chunk> allChunks = chunkMapper.selectAllReadyChunks();
        rebuildIndex(allChunks);
        log.info("BM25 索引构建完成，共 {} 条记录", allChunks.size());
    }

    /**
     * BM25 关键词检索
     * @param query 用户原始查询文本
     * @param topK 返回数量
     * @return 关键词匹配结果
     */
    public List<RetrievedDocument> keywordSearch(String query, int topK) {
        try {
            IndexSearcher searcher = indexManager.acquire();
            // 使用中文分词器（IK Analyzer）构建查询
            QueryParser parser = new QueryParser("content", new IKAnalyzer());
            Query luceneQuery = parser.parse(query);
            TopDocs topDocs = searcher.search(luceneQuery, topK);

            List<RetrievedDocument> results = new ArrayList<>();
            for (ScoreDoc scoreDoc : topDocs.scoreDocs) {
                Document doc = searcher.doc(scoreDoc.doc);
                RetrievedDocument result = new RetrievedDocument();
                result.setContent(doc.get("content"));
                result.setDocumentTitle(doc.get("document_title"));
                result.setSimilarity((double) scoreDoc.score);
                results.add(result);
            }
            return results;
        } catch (Exception e) {
            log.error("BM25 检索失败", e);
            return Collections.emptyList();
        }
    }

    /** 增量添加切片到索引 */
    public void addToIndex(Chunk chunk) { ... }
    /** 从索引中移除切片 */
    public void removeFromIndex(Long chunkId) { ... }
}
```

#### 6.2.5.3 RerankerService（重排序服务） `[P2]`

```java
/**
 * Rerank 重排序服务
 * 使用 bge-reranker-v2-m3 交叉编码器对候选文档精细排序
 * 解决简单向量检索中相似度≠相关性的问题
 */
@Component
@Slf4j
public class RerankerService {

    @Value("${rag.reranker.model:bge-reranker-v2-m3}")
    private String modelName;

    @Value("${rag.reranker.provider:ollama}")
    private String provider; // ollama 或 deepseek

    /**
     * 对候选文档列表进行 Rerank 重排序
     * @param query 用户原始问题
     * @param candidates 候选文档列表（宽召回+融合后）
     * @param topK 最终返回数量
     * @return 按相关性精确排序后的文档列表
     */
    public List<RetrievedDocument> rerank(String query,
                                           List<RetrievedDocument> candidates,
                                           int topK) {
        if (candidates.size() <= topK) {
            return candidates; // 候选数不足，无需重排
        }

        // 构建 [query, doc] 对，调用 Reranker 模型打分
        List<Pair<RetrievedDocument, Double>> scoredDocs = new ArrayList<>();
        for (RetrievedDocument doc : candidates) {
            double score = computeRelevanceScore(query, doc.getContent());
            scoredDocs.add(Pair.of(doc, score));
        }

        // 按相关性分数降序排列，取 Top-K
        return scoredDocs.stream()
                .sorted((a, b) -> Double.compare(b.getValue(), a.getValue()))
                .limit(topK)
                .map(Pair::getKey)
                .peek(doc -> doc.setSimilarity(
                    scoredDocs.stream()
                        .filter(p -> p.getKey().equals(doc))
                        .findFirst().map(Pair::getValue).orElse(0.0)))
                .collect(Collectors.toList());
    }

    /**
     * 调用 Reranker 模型计算 query-doc 相关性分数
     */
    private double computeRelevanceScore(String query, String document) {
        // 通过 Ollama API 调用 bge-reranker-v2-m3 模型
        // 返回 [0, 1] 之间的相关性分数
        ...
    }
}
```

#### 6.2.6 PromptTemplate `[P1]`

```java
/**
 * Prompt 模板构建器
 * 负责组装 RAG 检索上下文和用户问题为 LLM 可理解的格式
 */
@Component
public class PromptTemplate {

    /**
     * 构建 RAG 问答 Prompt
     *
     * @param query 用户原始问题
     * @param context 检索到的上下文文档
     * @return 完整的 Prompt 文本
     */
    public String build(String query, String context) {
        return String.format("""
            你是一个企业知识库助手，请根据以下参考资料回答用户问题。
            如果参考资料中没有直接相关信息，请如实告知用户"知识库中暂无相关记录"。

            参考资料：
            %s

            用户问题：%s

            请用专业、准确的语言回答，并在答案末尾注明引用的来源文档：
            """, context, query);
    }
}
```

#### 6.2.7 LLMCircuitBreaker（LLM 熔断器） `[P2]`

```java
/**
 * LLM 服务熔断器
 * 状态机：CLOSED → OPEN → HALF_OPEN → CLOSED
 *
 * 当 Ollama 连续失败 ≥ 3 次时自动熔断，切换到 DeepSeek API 降级链路
 * 熔断 30 秒后进入半开状态，允许探测请求验证 Ollama 是否恢复
 */
@Component
@Slf4j
public class LLMCircuitBreaker {

    @Autowired
    private ProviderFactory providerFactory;

    /** 熔断状态 */
    private enum State { CLOSED, OPEN, HALF_OPEN }

    /** 当前状态 */
    private volatile State state = State.CLOSED;

    /** 连续失败计数器 */
    private final AtomicInteger failureCount = new AtomicInteger(0);

    /** 熔断开始时间 */
    private volatile long openTimestamp = 0;

    @Value("${circuit-breaker.failure-threshold:3}")
    private int failureThreshold;     // 失败阈值

    @Value("${circuit-breaker.open-duration-ms:30000}")
    private long openDurationMs;      // 熔断时长（毫秒）

    /**
     * 获取当前可用的 LLM Provider
     * 根据熔断状态决定返回 Ollama 还是降级的 DeepSeek
     */
    public LLMProvider getActiveProvider() {
        switch (state) {
            case CLOSED:
                return providerFactory.getLLMProvider(); // 使用配置的默认 Provider

            case OPEN:
                if (System.currentTimeMillis() - openTimestamp > openDurationMs) {
                    // 熔断时间到，进入半开状态
                    state = State.HALF_OPEN;
                    log.info("熔断器: OPEN → HALF_OPEN，尝试探测 Ollama");
                    return providerFactory.getLLMProvider(); // 半开时尝试主 Provider
                }
                // 仍在熔断期，返回降级 Provider
                log.warn("熔断器 OPEN: 使用 DeepSeek 降级链路");
                return providerFactory.getFallbackLLMProvider();

            case HALF_OPEN:
                // 半开状态允许一个探测请求通过
                return providerFactory.getLLMProvider();

            default:
                return providerFactory.getLLMProvider();
        }
    }

    /** 记录调用成功 */
    public void recordSuccess() {
        if (state == State.HALF_OPEN) {
            state = State.CLOSED;
            failureCount.set(0);
            log.info("熔断器: HALF_OPEN → CLOSED，Ollama 已恢复");
        }
        failureCount.set(0); // CLOSED 状态下重置
    }

    /** 记录调用失败 */
    public void recordFailure() {
        int count = failureCount.incrementAndGet();
        if (state == State.CLOSED && count >= failureThreshold) {
            state = State.OPEN;
            openTimestamp = System.currentTimeMillis();
            log.error("熔断器: CLOSED → OPEN，连续失败 {} 次，切换到 DeepSeek 降级", count);
        } else if (state == State.HALF_OPEN) {
            state = State.OPEN;
            openTimestamp = System.currentTimeMillis();
            log.error("熔断器: HALF_OPEN → OPEN，Ollama 探测失败，继续熔断");
        }
    }
}
```

***

## 7. 事件处理模块详细设计

> 事件处理为**跨模块横切关注点**，涉及以下模块:
>
> - `agent-qr-common` — 事件类定义 + DLQ/重试基础设施 \[P2]
> - `agent-qr-knowledge` — DocumentParseListener + ChunkEmbeddingListener
> - `agent-qr-statistics` — StatisticsUpdateListener
> - `agent-qr-web` — AsyncConfig 线程池配置

### 7.0 包结构（事件处理横切关注点）

```
事件类（位于 agent-qr-common）:
  agent-qr-common/src/main/java/org/example/agent_qr/common/event/
  ├── [P1] DocumentUploadedEvent.java
  ├── [P1] DocumentParsedEvent.java
  ├── [P1] ChunksCreatedEvent.java
  ├── [P1] EmbeddingCompletedEvent.java
  ├── [P1] AnswerGeneratedEvent.java
  └── [P2] DocumentDeleteRequestedEvent.java  // ★ 文档删除请求事件（解耦 knowledge↔compensation 循环依赖）

DLQ 基础设施:
  agent-qr-common/src/main/java/org/example/agent_qr/common/dlq/
  ├── [P2] DeadLetterQueue.java               // 死信队列管理器（写入+重试状态管理）
  └── entity/
      └── [P2] DlqMessage.java                // 死信消息实体

  agent-qr-web/src/main/java/org/example/agent_qr/web/scheduler/
  └── [P2] DlqRetryScheduler.java             // ★ @Scheduled 定时重试调度器（位于 web，跨模块编排）

监听器实现（位于各自业务模块）:
  agent-qr-knowledge/.../listener/
  ├── [P1→P2] DocumentParseListener.java      // @Async("parseExecutor") → 解析+DLQ
  └── [P1→P2] ChunkEmbeddingListener.java     // @Async("chunkExecutor") → 切片+向量化+DLQ

  agent-qr-compensation/.../listener/          // ★ compensation 模块监听器
  └── [P2] DocumentDeleteListener.java        // @Async("deleteExecutor") + @TransactionalEventListener → 逻辑删+物理删

  agent-qr-statistics/.../listener/
  └── [P1] StatisticsUpdateListener.java      // @Async("statExecutor") → 日统计更新

线程池配置（位于 agent-qr-web）:
  agent-qr-web/.../web/config/
  ├── [P1] AsyncConfig.java                   // [P1] docProcessExecutor + statExecutor 双池
  └── [P2] AsyncConfigV2.java                // [P2] parseExecutor/chunkExecutor/embedExecutor/deleteExecutor 四池隔离
```

### 7.1 死信队列（DLQ）与指数退避重试 `[P2]`

#### 7.0.1 DeadLetterQueue

```java
/**
 * 死信队列管理器
 * 事件处理失败时写入 dlq_message 表，支持后续重试
 */
@Component
@Slf4j
public class DeadLetterQueue {

    @Autowired
    private DlqMessageMapper dlqMessageMapper;

    /** 最大重试次数 */
    @Value("${dlq.max-retries:4}")
    private int maxRetries;

    /** 指数退避基数（秒） */
    @Value("${dlq.backoff-base:3}")
    private int backoffBase;

    /**
     * 将失败事件写入死信队列
     * @param eventType 事件类型
     * @param documentId 关联文档ID
     * @param payload 事件原始载荷
     * @param error 错误信息
     */
    public void enqueue(String eventType, Long documentId,
                        String payload, String error) {
        DlqMessage msg = new DlqMessage();
        msg.setEventType(eventType);
        msg.setDocumentId(documentId);
        msg.setPayload(payload);
        msg.setErrorMsg(error);
        msg.setRetryCount(0);
        // 首次重试延迟 = backoffBase^1 = 3 秒
        msg.setNextRetryAt(LocalDateTime.now().plusSeconds(backoffBase));
        msg.setStatus("PENDING");
        dlqMessageMapper.insert(msg);
        log.warn("事件进入死信队列: type={}, docId={}, 首次重试延迟={}s",
                eventType, documentId, backoffBase);
    }

    /**
     * 计算指数退避时间
     * retry 0 → 3s, retry 1 → 9s, retry 2 → 27s, retry 3 → 81s
     */
    private long calcBackoffSeconds(int retryCount) {
        return (long) Math.pow(backoffBase, retryCount + 1);
    }

    /**
     * 更新重试状态
     * @param msgId 消息ID
     * @param success 是否成功
     * @param error 失败时的错误信息
     */
    public void updateRetryResult(Long msgId, boolean success, String error) {
        DlqMessage msg = dlqMessageMapper.selectById(msgId);
        if (success) {
            dlqMessageMapper.deleteById(msgId);
            log.info("DLQ 重试成功: msgId={}", msgId);
        } else {
            int newCount = msg.getRetryCount() + 1;
            if (newCount >= maxRetries) {
                dlqMessageMapper.updateStatus(msgId, "DEAD", error);
                log.error("DLQ 重试耗尽: msgId={}, 最终标记为 DEAD", msgId);
                // TODO: 发送告警通知管理员
            } else {
                long nextDelay = calcBackoffSeconds(newCount);
                dlqMessageMapper.updateRetry(msgId, newCount,
                        LocalDateTime.now().plusSeconds(nextDelay), error);
                log.warn("DLQ 重试失败: msgId={}, 第{}次, 下次重试延迟={}s",
                        msgId, newCount, nextDelay);
            }
        }
    }
}
```

#### 7.0.2 DlqRetryScheduler（定时重试调度器）★

> ★ **模块归属变更**：`DlqRetryScheduler` 位于 `agent-qr-web`（而非 `agent-qr-common`），
> 因为重试执行需要注入各业务模块的 Service/Listener（`DocumentParserService`、`ChunkEmbeddingListener`、`DocumentDeleteServiceV2`），
> 而 `agent-qr-common` 不能反向依赖业务模块。`agent-qr-web` 依赖全部模块，是放置跨模块调度逻辑的唯一合理位置。

```java
/**
 * DLQ 重试定时调度器 ★ 位于 agent-qr-web 模块
 *
 * 模块归属说明：
 * - DeadLetterQueue / DlqMessageMapper / DlqMessage → agent-qr-common（纯基础设施）
 * - DlqRetryScheduler → agent-qr-web（跨模块编排，需要注入各业务模块的类）
 * - 各模块也可注册自己的 @Scheduled 重试方法作为补充
 */
@Component
@Slf4j
public class DlqRetryScheduler {

    @Autowired
    private DeadLetterQueue deadLetterQueue;
    @Autowired
    private DlqMessageMapper dlqMessageMapper;

    // ★ 以下依赖来自各业务模块，仅 agent-qr-web 可同时注入
    @Autowired(required = false)
    private DocumentParserService parserService;          // 来自 agent-qr-knowledge
    @Autowired(required = false)
    private ChunkEmbeddingListener chunkEmbeddingListener; // 来自 agent-qr-knowledge
    @Autowired(required = false)
    private DocumentDeleteServiceV2 documentDeleteServiceV2; // ★ 来自 agent-qr-compensation

    /**
     * 每 30 秒扫描一次 DLQ，对到期记录重试
     */
    @Scheduled(fixedDelay = 30000)
    public void retryDeadLetters() {
        List<DlqMessage> pendingMessages =
                dlqMessageMapper.selectPendingRetries(LocalDateTime.now());

        for (DlqMessage msg : pendingMessages) {
            try {
                log.info("DLQ 重试: msgId={}, type={}, retry={}",
                        msg.getId(), msg.getEventType(), msg.getRetryCount());

                // 根据事件类型重新触发处理
                switch (msg.getEventType()) {
                    case "PARSE" -> retryParse(msg);
                    case "CHUNK" -> retryChunk(msg);
                    case "EMBED" -> retryEmbed(msg);
                    case "DELETE" -> retryDelete(msg);  // ★ compensation 模块的物理删除重试
                }

                deadLetterQueue.updateRetryResult(msg.getId(), true, null);
            } catch (Exception e) {
                deadLetterQueue.updateRetryResult(msg.getId(), false, e.getMessage());
            }
        }
    }

    private void retryParse(DlqMessage msg) {
        // 重新发布 DocumentUploadedEvent 触发解析
        DocumentUploadedEvent event = JSON.parseObject(msg.getPayload(), DocumentUploadedEvent.class);
        parserService.parse(event.getFilePath(), event.getFileType());
    }

    private void retryChunk(DlqMessage msg) { /* 类似 retryParse */ }
    private void retryEmbed(DlqMessage msg) { /* 类似 retryParse */ }

    /** ★ DELETE 重试：重新调用 compensation 模块的物理删除 */
    private void retryDelete(DlqMessage msg) {
        List<String> chromaIds = JSON.parseArray(msg.getPayload(), String.class);
        documentDeleteServiceV2.asyncPhysicalDelete(msg.getDocumentId(), chromaIds);
    }
}
```

> **DLQ 依赖关系总结**：
> ```
> agent-qr-common          → DeadLetterQueue, DlqMessage, DlqMessageMapper（纯基础设施）
> agent-qr-web             → DlqRetryScheduler（跨模块编排，依赖全部业务模块）
> agent-qr-compensation    → 也可注册自己的 @Scheduled deleteRetry() 作为补充
> ```

### 7.2 事件类设计

| 事件类                       | 阶段 | 触发时机      | 关键字段                                   |
| ------------------------- | -- | --------- | -------------------------------------- |
| `DocumentUploadedEvent`   | P1 | 文档上传完成    | documentId, filePath, fileType, userId |
| `DocumentParsedEvent`     | P1 | 文档内容解析完成  | documentId, content（解析后的全文）            |
| `ChunksCreatedEvent`      | P1 | 文本切片完成    | documentId, chunks（切片列表）               |
| `EmbeddingCompletedEvent` | P1 | 向量化入库完成   | documentId, chunkCount（成功数量）           |
| `AnswerGeneratedEvent`    | P1 | AI 回答生成完成 | userId, conversationId                 |
| `DocumentDeleteRequestedEvent` | P2 | ★ 文档删除请求（knowledge 发布，compensation 监听） | documentId, chunkIds, chromaIds, filePath |

> ★ `DocumentDeleteRequestedEvent` 是解耦 `agent-qr-knowledge` 与 `agent-qr-compensation` 循环依赖的关键：
> - 定义在 `agent-qr-common`，两个模块都依赖 common，不产生直接依赖
> - knowledge 模块的 `DocumentCommandService.requestDeleteDocument()` 发布此事件
> - compensation 模块的 `DocumentDeleteListener` 通过 `@TransactionalEventListener(phase = AFTER_COMMIT)` 监听
> - 依赖链：`compensation → knowledge → common ← compensation`（通过 common 事件类解耦，无循环）

#### 7.2.6 DocumentDeleteRequestedEvent `[P2]`

```java
/**
 * ★ 文档删除请求事件 [P2]
 * 由 knowledge 模块发布，compensation 模块监听处理
 *
 * 设计目的：解耦 knowledge ↔ compensation 的循环依赖
 * - knowledge 只发布事件，不注入 compensation 的任何类
 * - compensation 通过 @TransactionalEventListener 监听，单向依赖 knowledge
 * - 事件类定义在 agent-qr-common，双方都依赖 common，不产生直接依赖
 */
@Data
public class DocumentDeleteRequestedEvent {

    /** 待删除的文档ID */
    private Long documentId;

    /** 文档所有切片ID列表（用于逻辑删除） */
    private List<Long> chunkIds;

    /** ChromaDB 中的向量ID列表（用于物理删除） */
    private List<String> chromaIds;

    /** 文档服务器文件路径（用于清理上传文件） */
    private String filePath;
}
```

### 7.3 监听器设计

#### 7.2.1 DocumentParseListener `[P1 基础 + P2 DLQ集成]`

```java
/**
 * 文档解析监听器
 * 监听 DocumentUploadedEvent，异步解析文档内容
 */
@Component
@Slf4j
public class DocumentParseListener {

    @Autowired
    private DocumentMapper documentMapper;
    @Autowired
    private DocumentParserService parserService;
    @Autowired
    private ApplicationEventPublisher eventPublisher;
    @Autowired
    private DeadLetterQueue deadLetterQueue;  // ★ DLQ

    /**
     * 异步处理文档解析
     * @param event 文档上传事件
     */
    @EventListener
    @Async("docProcessExecutor")
    public void handleDocumentUploaded(DocumentUploadedEvent event) {
        log.info("开始解析文档: id={}, name={}", event.getDocumentId(), event.getFileName());

        // 1. 更新状态为 PARSING
        updateStatus(event.getDocumentId(), DocumentStatus.PARSING);

        try {
            // 2. 解析文档（含表格提取 + OCR）
            String content = parserService.parse(event.getFilePath(), event.getFileType());

            // 3. 发布解析完成事件
            DocumentParsedEvent parsedEvent = new DocumentParsedEvent();
            parsedEvent.setDocumentId(event.getDocumentId());
            parsedEvent.setContent(content);
            eventPublisher.publishEvent(parsedEvent);

            log.info("文档解析完成: id={}", event.getDocumentId());
        } catch (Exception e) {
            // 4. 解析失败 → 进入死信队列，不直接标记 FAILED
            log.error("文档解析失败: id={}, 进入DLQ", event.getDocumentId(), e);
            documentMapper.updateStatus(event.getDocumentId(), DocumentStatus.FAILED.name());
            documentMapper.updateErrorMsg(event.getDocumentId(), "解析失败: " + e.getMessage());
            // ★ 写入死信队列等待重试
            deadLetterQueue.enqueue("PARSE", event.getDocumentId(),
                    JSON.toJSONString(event), e.getMessage());
        }
    }

    private void updateStatus(Long documentId, DocumentStatus status) {
        documentMapper.updateStatus(documentId, status.name());
    }
}
```

#### 7.2.2 ChunkEmbeddingListener `[P1 基础 + P2 DLQ/批量集成]`

```java
/**
 * 切片与向量化监听器
 * 监听 DocumentParsedEvent，异步执行切片 → 向量化 → 存入 ChromaDB
 */
@Component
@Slf4j
public class ChunkEmbeddingListener {

    @Autowired
    private DocumentMapper documentMapper;
    @Autowired
    private ChunkMapper chunkMapper;
    @Autowired
    private TextSplitter textSplitter;
    @Autowired
    private ProviderFactory providerFactory;
    @Autowired
    private ChromaVectorStore chromaVectorStore;
    @Autowired
    private ApplicationEventPublisher eventPublisher;
    @Autowired
    private DeadLetterQueue deadLetterQueue;  // ★ DLQ

    @EventListener
    @Async("docProcessExecutor")
    public void handleDocumentParsed(DocumentParsedEvent event) {
        log.info("开始切片文档: id={}", event.getDocumentId());

        try {
            // ... 切片和向量化逻辑（同原设计）...

            log.info("文档处理完成: id={}, 成功切片数={}", event.getDocumentId(), successCount);
        } catch (Exception e) {
            // ★ 失败进入死信队列
            log.error("文档切片/向量化失败: id={}, 进入DLQ", event.getDocumentId(), e);
            updateDocumentStatus(event.getDocumentId(), DocumentStatus.FAILED);
            documentMapper.updateErrorMsg(event.getDocumentId(), "处理失败: " + e.getMessage());
            deadLetterQueue.enqueue("CHUNK", event.getDocumentId(),
                    JSON.toJSONString(event), e.getMessage());
        }
    }

    private void updateDocumentStatus(Long docId, DocumentStatus status) {
        documentMapper.updateStatus(docId, status.name());
    }
}
```

#### 7.2.2b DocumentDeleteListener ★ `[P2]` — compensation 模块监听器

> ★ 本监听器位于 `agent-qr-compensation` 模块（`org.example.agent_qr.compensation.listener`），
> 是解耦 knowledge ↔ compensation 循环依赖的核心组件。

```java
/**
 * ★ 文档删除事件监听器 [P2]
 * 位于 agent-qr-compensation 模块，单向依赖 agent-qr-knowledge（访问 Mapper）
 *
 * 设计原则：
 * 1. knowledge 模块只发布 DocumentDeleteRequestedEvent，不注入 compensation 的任何类
 * 2. compensation 模块使用 @TransactionalEventListener(phase = AFTER_COMMIT)
 *    确保在 knowledge 模块的事务提交成功后才执行
 * 3. 异步执行（@Async("deleteExecutor")），不阻塞 Controller 响应
 * 4. 异常进入 DLQ 重试，保证最终一致性
 *
 * 依赖链：compensation → knowledge → common ← compensation（无循环）
 */
@Component
@Slf4j
public class DocumentDeleteListener {

    @Autowired
    private DocumentMapper documentMapper;          // 来自 agent-qr-knowledge
    @Autowired
    private ChunkMapper chunkMapper;                // 来自 agent-qr-knowledge
    @Autowired
    private DocumentDeleteServiceV2 documentDeleteServiceV2;  // 本模块
    @Autowired
    private DeadLetterQueue deadLetterQueue;         // 来自 agent-qr-common

    /**
     * ★ 处理文档删除请求事件
     * @TransactionalEventListener(phase = AFTER_COMMIT) 确保：
     * - knowledge 模块的 DocumentCommandService.requestDeleteDocument() 事务已提交
     * - 文档状态已标记为 DELETING
     * - 此时执行逻辑删除+物理删除是安全的
     *
     * @Async("deleteExecutor") 确保：
     * - 不阻塞 Controller 的 HTTP 响应
     * - 使用独立的删除线程池，与解析/切片/向量化线程池隔离
     */
    @TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)
    @Async("deleteExecutor")
    public void handleDocumentDeleteRequested(DocumentDeleteRequestedEvent event) {
        log.info("收到文档删除事件: docId={}, chromaIds={}",
            event.getDocumentId(), event.getChromaIds() != null ? event.getChromaIds().size() : 0);

        try {
            // ===== 第 1 步：MySQL 逻辑删除（B+树索引瞬间生效，检索立即过滤） =====
            // 切片逻辑删除
            chunkMapper.softDeleteByDocumentId(event.getDocumentId());
            // 文档主记录逻辑删除
            documentMapper.softDelete(event.getDocumentId());

            log.info("MySQL 逻辑删除完成: docId={}", event.getDocumentId());

            // ===== 第 2 步：ChromaDB 异步物理删除（带重试+DLQ兜底） =====
            if (event.getChromaIds() != null && !event.getChromaIds().isEmpty()) {
                documentDeleteServiceV2.asyncPhysicalDelete(
                    event.getDocumentId(), event.getChromaIds());
            }

        } catch (Exception e) {
            // ===== 异常处理：写入 DLQ 等待重试 =====
            log.error("文档删除监听器执行失败: docId={}, 进入DLQ",
                event.getDocumentId(), e);
            deadLetterQueue.enqueue("DELETE", event.getDocumentId(),
                JSON.toJSONString(event), e.getMessage());
        }
    }
}
```

#### 7.2.3 StatisticsUpdateListener `[P1]`

```java
/**
 * 统计更新监听器
 * 监听 EmbeddingCompletedEvent 和 AnswerGeneratedEvent，异步更新日统计
 */
@Component
@Slf4j
public class StatisticsUpdateListener {

    @Autowired
    private DailyStatsMapper dailyStatsMapper;

    /**
     * 文档上传统计更新
     */
    @EventListener
    @Async("statExecutor")
    public void handleEmbeddingCompleted(EmbeddingCompletedEvent event) {
        LocalDate today = LocalDate.now();
        DailyStats stats = dailyStatsMapper.selectByDate(today);
        if (stats == null) {
            stats = new DailyStats();
            stats.setStatDate(today);
            stats.setDocUploadCount(1);
            dailyStatsMapper.insert(stats);
        } else {
            dailyStatsMapper.incrementDocUploadCount(today);
        }
    }

    /**
     * 问答统计更新
     */
    @EventListener
    @Async("statExecutor")
    public void handleAnswerGenerated(AnswerGeneratedEvent event) {
        LocalDate today = LocalDate.now();
        DailyStats stats = dailyStatsMapper.selectByDate(today);
        if (stats == null) {
            stats = new DailyStats();
            stats.setStatDate(today);
            stats.setQaCount(1);
            stats.setUserQuestionCount(1);
            stats.setActiveUserCount(1);
            dailyStatsMapper.insert(stats);
        } else {
            dailyStatsMapper.incrementQaCount(today, event.getUserId());
        }
    }
}
```

### 7.4 异步线程池配置 `[P1 双池 + P2 四池隔离]`

```java
/**
 * 异步任务线程池配置 [P1 双池 → P2 四池隔离]
 *
 * P2 四池隔离原因：
 * - parseExecutor: PDF/DOCX 解析是 CPU 密集型 + 大内存，独立池防止 OOM 影响其他任务
 * - chunkExecutor: 文本切片是 CPU 密集型，与解析隔离
 * - embedExecutor: 向量化是 IO 密集型（调用外部 API），独立池防止慢 IO 阻塞 CPU 任务
 * - deleteExecutor: ★ 删除是 IO 密集型（ChromaDB API + MySQL），独立池防止慢删除阻塞正常处理
 *   使用 MdcTaskDecorator 确保 TraceId 在线程切换时不丢失
 */
@Configuration
@EnableAsync
public class AsyncConfigV2 implements AsyncConfigurer {

    /** ★ 文档解析线程池 — PDF/DOCX 解析（CPU + 大内存） */
    @Bean("parseExecutor")
    public Executor parseExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(2);
        executor.setMaxPoolSize(4);
        executor.setQueueCapacity(50);
        executor.setThreadNamePrefix("parse-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.setTaskDecorator(new MdcTaskDecorator()); // ★ TraceId 传递
        executor.initialize();
        return executor;
    }

    /** ★ 文本切片线程池 — TextSplitter（CPU 密集型） */
    @Bean("chunkExecutor")
    public Executor chunkExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(2);
        executor.setMaxPoolSize(4);
        executor.setQueueCapacity(100);
        executor.setThreadNamePrefix("chunk-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.setTaskDecorator(new MdcTaskDecorator());
        executor.initialize();
        return executor;
    }

    /** ★ 向量化线程池 — Embedding API 调用（IO 密集型） */
    @Bean("embedExecutor")
    public Executor embedExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(4);
        executor.setMaxPoolSize(8);
        executor.setQueueCapacity(200);
        executor.setThreadNamePrefix("embed-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.setTaskDecorator(new MdcTaskDecorator());
        executor.initialize();
        return executor;
    }

    /** ★ 删除线程池 — MySQL 逻辑删除 + ChromaDB 物理删除（IO 密集型，独立隔离） */
    @Bean("deleteExecutor")
    public Executor deleteExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(2);
        executor.setMaxPoolSize(4);
        executor.setQueueCapacity(50);
        executor.setThreadNamePrefix("delete-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.setTaskDecorator(new MdcTaskDecorator());
        executor.initialize();
        return executor;
    }

    /** 统计更新线程池 */
    @Bean("statExecutor")
    public Executor statExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(2);
        executor.setMaxPoolSize(4);
        executor.setQueueCapacity(50);
        executor.setThreadNamePrefix("stat-");
        executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
        executor.initialize();
        return executor;
    }
}
```

***

## 8. 统计模块详细设计 (agent-qr-statistics)

> Maven 模块: `agent-qr-statistics`
> 基础包路径: `org.example.agent_qr.statistics`
> 模块依赖: `agent-qr-common`、`agent-qr-knowledge`、`agent-qr-user`

### 8.0 包结构（P1 / P2 全量）

```
agent-qr-statistics/
└── src/main/java/org/example/agent_qr/statistics/
    ├── controller/
    │   └── [P1] StatisticsController.java       // GET /api/statistics/dashboard
    ├── entity/
    │   └── [P1] DailyStats.java                 // 日统计实体（stat_daily 表）
    ├── mapper/
    │   └── [P1] DailyStatsMapper.java           // selectByDate, incrementDocUploadCount, incrementQaCount, selectWeeklyTrend
    ├── dto/
    │   └── [P1] DashboardVO.java                // 仪表盘视图对象（todayQA/totalDocuments/weeklyTrend/docTypeDistribution）
    ├── service/
    │   ├── [P1→P2] StatisticsQueryService.java  // [P1] 基础Dashboard [P2] 含满意度/点赞点踩统计
    │   └── [P2] FeedbackService.java            // 答案反馈评价（submitFeedback: positive/negative）
    └── listener/
        └── [P1] StatisticsUpdateListener.java   // @Async("statExecutor") 监听 EmbeddingCompleted+AnswerGenerated

agent-qr-statistics/pom.xml 依赖:
  [P1] agent-qr-common, agent-qr-knowledge, agent-qr-user, mybatis-plus-spring-boot3-starter
  [P2] (无新增依赖)
```

### 8.1 StatisticsQueryService

```java
/**
 * 统计查询服务
 */
@Service
public class StatisticsQueryService {

    @Autowired
    private DailyStatsMapper dailyStatsMapper;
    @Autowired
    private DocumentMapper documentMapper;
    @Autowired
    private SysUserMapper sysUserMapper;

    /**
     * 获取管理后台仪表盘数据
     */
    public DashboardVO getDashboard() {
        DashboardVO vo = new DashboardVO();
        LocalDate today = LocalDate.now();

        // 今日统计
        DailyStats todayStats = dailyStatsMapper.selectByDate(today);
        if (todayStats != null) {
            vo.setTodayQA(todayStats.getQaCount());
            vo.setTodayNewUsers(sysUserMapper.countByDate(today));
        }

        // 知识库概览
        vo.setTotalDocuments(documentMapper.selectCount(null));
        vo.setTotalChunks(chunkMapper.selectCount(null));
        vo.setTotalUsers(sysUserMapper.selectCount(null));

        // 近7天问答趋势
        vo.setWeeklyTrend(dailyStatsMapper.selectWeeklyTrend(today));

        // 文档类型分布
        vo.setDocTypeDistribution(documentMapper.selectTypeDistribution());

        return vo;
    }
}
```

***

## 8.5 数据一致性补偿模块 `[P2]`

> ⚠️ **本小节内容已完整迁移至 [§10. 数据一致性补偿模块详细设计](#10-数据一致性补偿模块详细设计-agent-qr-compensation)**，含：
> - [§10.1 依赖设计分析：循环依赖解耦方案](#101-依赖设计分析循环依赖解耦方案) ★
> - [§10.2 OrphanVectorScanner（孤儿向量扫描清理）](#102-orphanvectorscanner孤儿向量扫描清理-p2)
> - [§10.3 DocumentDeleteListener（文档删除事件监听器）](#103-documentdeletelistener文档删除事件监听器-p2)
> - [§10.4 DocumentDeleteServiceV2（物理删除服务）](#104-documentdeleteservicev2物理删除服务-p2)
>
> **核心设计决策**：`agent-qr-knowledge` 与 `agent-qr-compensation` 之间通过事件驱动（`DocumentDeleteRequestedEvent`）解耦：
> - knowledge 只发布事件 → 不依赖 compensation
> - compensation 单向依赖 knowledge → 访问 ChunkMapper/DocumentMapper
> - 依赖链：`compensation → knowledge → common ← compensation`（无循环依赖）
>
> 事件驱动架构详图见 [§10.1 依赖设计分析](#101-依赖设计分析循环依赖解耦方案)。

***

## 8.6 答案反馈评价模块 `[P2]`

```java
/**
 * 答案反馈评价服务
 * 用户点赞/点踩后更新消息记录和统计数据
 */
@Service
@Slf4j
public class FeedbackService {

    @Autowired
    private MessageMapper messageMapper;
    @Autowired
    private DailyStatsMapper dailyStatsMapper;

    /**
     * 提交答案反馈
     * @param messageId 消息ID
     * @param feedback positive/negative
     * @param reason 负面原因（可选）
     * @param userId 用户ID
     */
    public void submitFeedback(Long messageId, String feedback,
                                String reason, Long userId) {
        // 1. 校验消息存在且为 AI 回答
        Message msg = messageMapper.selectById(messageId);
        if (msg == null || !"assistant".equals(msg.getRole())) {
            throw new BusinessException("无效的消息或消息类型不正确");
        }

        // 2. 更新消息反馈字段（覆盖前次评价）
        messageMapper.updateFeedback(messageId, feedback, reason);

        // 3. 更新当日统计
        LocalDate today = LocalDate.now();
        if ("positive".equals(feedback)) {
            dailyStatsMapper.incrementPositiveCount(today);
        } else if ("negative".equals(feedback)) {
            dailyStatsMapper.incrementNegativeCount(today);
        }

        log.info("用户反馈已记录: msgId={}, feedback={}, reason={}", messageId, feedback, reason);
    }
}
```

***

***

## 8.7 多源数据接入模块详细设计（方案一：统一接入网关） `[P2]`

> ⚠️ 本模块在 P2 阶段独立为 Maven 模块 `agent-qr-datasource`，完整包结构见 [11. 多源数据接入模块详细设计](#11-多源数据接入模块详细设计-agent-qr-datasource)
>
> 以下为详细类设计，类文件位于 `org.example.agent_qr.datasource` 包路径下。

### 8.7.1 DataSourceConnector 策略接口

```java
/**
 * 数据源连接器策略接口
 * 所有数据源类型（JDBC/REST/S3）统一实现此接口，
 * 新增数据源类型无需修改任何核心逻辑
 */
public interface DataSourceConnector {

    /** 连接器类型标识 */
    String getType();

    /**
     * 测试数据源连通性
     * @param config 连接配置（URL、凭据等）
     * @return 连接测试结果（含延迟、数据量预览）
     */
    ConnectionTestResult testConnection(Map<String, Object> config);

    /**
     * 全量同步：拉取数据源全部数据
     * @param context 同步上下文（数据源ID、批次号等）
     * @return 同步结果（含原始数据列表、游标信息）
     */
    SyncResult fullSync(SyncContext context);

    /**
     * 增量同步：仅拉取自上次同步后变更的数据
     * @param context 同步上下文
     * @param lastCursor 上次同步的游标位置
     * @return 增量数据 + 新游标位置
     */
    SyncResult incrementalSync(SyncContext context, String lastCursor);
}
```

### 8.7.2 JdbcConnector（JDBC 数据库连接器）

```java
/**
 * JDBC 数据库连接器
 * 支持 MySQL、PostgreSQL、Oracle、SQL Server
 * 增量策略：基于时间戳游标或 Debezium CDC 日志
 */
@Component
@Slf4j
public class JdbcConnector implements DataSourceConnector {

    @Override
    public String getType() { return "JDBC"; }

    @Override
    public ConnectionTestResult testConnection(Map<String, Object> config) {
        String url = (String) config.get("url");
        String username = (String) config.get("username");
        String password = (String) config.get("password");

        try (Connection conn = DriverManager.getConnection(url, username, password)) {
            long start = System.currentTimeMillis();
            DatabaseMetaData meta = conn.getMetaData();
            long latency = System.currentTimeMillis() - start;

            ConnectionTestResult result = new ConnectionTestResult();
            result.setSuccess(true);
            result.setLatencyMs(latency);
            result.setDbProduct(meta.getDatabaseProductName());
            result.setDbVersion(meta.getDatabaseProductVersion());
            return result;
        } catch (SQLException e) {
            ConnectionTestResult result = new ConnectionTestResult();
            result.setSuccess(false);
            result.setErrorMsg(e.getMessage());
            return result;
        }
    }

    @Override
    public SyncResult fullSync(SyncContext context) {
        DataSourceConfig config = context.getConfig();
        List<Map<String, Object>> allRows = new ArrayList<>();

        for (String tableName : config.getTableNames()) {
            String sql = String.format("SELECT * FROM %s", tableName);
            try (Connection conn = getConnection(config);
                 PreparedStatement stmt = conn.prepareStatement(sql);
                 ResultSet rs = stmt.executeQuery()) {

                ResultSetMetaData meta = rs.getMetaData();
                int colCount = meta.getColumnCount();
                while (rs.next()) {
                    Map<String, Object> row = new LinkedHashMap<>();
                    for (int i = 1; i <= colCount; i++) {
                        row.put(meta.getColumnName(i), rs.getObject(i));
                    }
                    allRows.add(row);
                }
            } catch (SQLException e) {
                log.error("全量同步失败: table={}", tableName, e);
                throw new BusinessException("数据库全量同步失败: " + e.getMessage());
            }
        }

        SyncResult result = new SyncResult();
        result.setTotalRows(allRows.size());
        result.setRawData(allRows);
        result.setNextCursor(null); // 全量同步不设游标
        return result;
    }

    @Override
    public SyncResult incrementalSync(SyncContext context, String lastCursor) {
        DataSourceConfig config = context.getConfig();
        String cursorField = config.getCursorField(); // e.g. "update_time"
        List<Map<String, Object>> changedRows = new ArrayList<>();
        String newCursor = lastCursor;

        for (String tableName : config.getTableNames()) {
            String sql = String.format(
                "SELECT * FROM %s WHERE %s > ? ORDER BY %s ASC",
                tableName, cursorField, cursorField);

            try (Connection conn = getConnection(config);
                 PreparedStatement stmt = conn.prepareStatement(sql)) {
                stmt.setString(1, lastCursor);
                try (ResultSet rs = stmt.executeQuery()) {
                    ResultSetMetaData meta = rs.getMetaData();
                    int colCount = meta.getColumnCount();
                    while (rs.next()) {
                        Map<String, Object> row = new LinkedHashMap<>();
                        for (int i = 1; i <= colCount; i++) {
                            row.put(meta.getColumnName(i), rs.getObject(i));
                        }
                        changedRows.add(row);
                        // 更新游标到最后一条记录的 cursor_field 值
                        newCursor = rs.getString(cursorField);
                    }
                }
            } catch (SQLException e) {
                log.error("增量同步失败: table={}", tableName, e);
                throw new BusinessException("数据库增量同步失败: " + e.getMessage());
            }
        }

        SyncResult result = new SyncResult();
        result.setTotalRows(changedRows.size());
        result.setRawData(changedRows);
        result.setNextCursor(newCursor); // 新游标
        return result;
    }
}
```

### 8.7.3 RestApiConnector（REST API 连接器）

```java
/**
 * REST API 数据源连接器
 * 支持分页拉取和 Webhook 推送两种模式
 * 增量策略：基于响应头 X-Next-Cursor 分页 / Webhook 事件回调
 */
@Component
@Slf4j
public class RestApiConnector implements DataSourceConnector {

    @Override
    public String getType() { return "REST"; }

    @Override
    public SyncResult fullSync(SyncContext context) {
        DataSourceConfig config = context.getConfig();
        RestTemplate restTemplate = createRestTemplate(config);
        List<Map<String, Object>> allRecords = new ArrayList<>();

        String nextCursor = null;
        boolean hasMore = true;
        while (hasMore) {
            // 构建请求 URL（带分页参数）
            String url = buildPageUrl(config.getBaseUrl(), nextCursor);
            ResponseEntity<List<Map<String, Object>>> response =
                restTemplate.exchange(url, HttpMethod.GET, null,
                    new ParameterizedTypeReference<>() {});

            List<Map<String, Object>> page = response.getBody();
            if (page != null && !page.isEmpty()) {
                allRecords.addAll(page);
            }

            // 从响应头获取下一页游标
            nextCursor = response.getHeaders().getFirst("X-Next-Cursor");
            hasMore = (nextCursor != null && !nextCursor.isEmpty());
        }

        SyncResult result = new SyncResult();
        result.setTotalRows(allRecords.size());
        result.setRawData(allRecords);
        return result;
    }

    @Override
    public SyncResult incrementalSync(SyncContext context, String lastCursor) {
        // REST 增量通常通过游标分页实现，逻辑同全量但传入 cursor
        return fullSync(context);
    }
}
```

### 8.7.4 S3Connector（文件系统连接器）

```java
/**
 * S3 / 文件系统连接器
 * 支持 S3 对象存储、NFS 共享目录、本地目录监听
 * 增量策略：基于文件 lastModified 时间戳过滤
 */
@Component
@Slf4j
public class S3Connector implements DataSourceConnector {

    @Override
    public String getType() { return "S3"; }

    @Override
    public SyncResult fullSync(SyncContext context) {
        DataSourceConfig config = context.getConfig();
        AmazonS3 s3Client = createS3Client(config);

        List<Map<String, Object>> files = new ArrayList<>();
        ListObjectsV2Request request = new ListObjectsV2Request()
                .withBucketName(config.getBucket())
                .withPrefix(config.getPrefix());

        ListObjectsV2Result listResult;
        do {
            listResult = s3Client.listObjectsV2(request);
            for (S3ObjectSummary obj : listResult.getObjectSummaries()) {
                if (isSupportedFormat(obj.getKey())) {
                    Map<String, Object> fileInfo = new HashMap<>();
                    fileInfo.put("key", obj.getKey());
                    fileInfo.put("size", obj.getSize());
                    fileInfo.put("lastModified", obj.getLastModified());
                    fileInfo.put("bucket", config.getBucket());
                    // 下载文件内容
                    S3Object s3Obj = s3Client.getObject(config.getBucket(), obj.getKey());
                    fileInfo.put("content", IOUtils.toString(s3Obj.getObjectContent(), "UTF-8"));
                    files.add(fileInfo);
                }
            }
            request.setContinuationToken(listResult.getNextContinuationToken());
        } while (listResult.isTruncated());

        SyncResult result = new SyncResult();
        result.setTotalRows(files.size());
        result.setRawData(files);
        return result;
    }

    @Override
    public SyncResult incrementalSync(SyncContext context, String lastCursor) {
        // 基于 lastModified > lastCursor 过滤新增/修改文件
        // lastCursor 格式: ISO 8601 时间戳
        DataSourceConfig config = context.getConfig();
        LocalDateTime cursorTime = LocalDateTime.parse(lastCursor);

        // ... 过滤逻辑：只拉取 lastModified > cursorTime 的文件
        // ... 返回新文件和最新文件时间戳作为 newCursor
        return fullSync(context); // 简化示例
    }

    private boolean isSupportedFormat(String key) {
        String lower = key.toLowerCase();
        return lower.endsWith(".pdf") || lower.endsWith(".docx")
            || lower.endsWith(".txt") || lower.endsWith(".md")
            || lower.endsWith(".csv") || lower.endsWith(".json");
    }
}
```

### 8.7.5 SyncScheduler（同步调度器）

```java
/**
 * 数据源同步调度器
 * 根据各数据源配置的 Cron 表达式定时触发同步
 * 同时支持管理员手动触发立即同步
 */
@Component
@Slf4j
public class SyncScheduler {

    @Autowired
    private DataSourceMapper dataSourceMapper;

    @Autowired
    private Map<String, DataSourceConnector> connectorMap; // 自动注入所有 Connector

    @Autowired
    private ApplicationEventPublisher eventPublisher;

    /**
     * 按数据源配置的 Cron 定时触发
     * 使用 Spring @Scheduled + TaskScheduler 动态注册
     */
    public void scheduleSync(Long datasourceId) {
        DataSourceConfig config = dataSourceMapper.selectById(datasourceId);
        if (config == null || !"ACTIVE".equals(config.getStatus())) {
            return;
        }

        DataSourceConnector connector = connectorMap.get(config.getSourceType().toLowerCase());
        if (connector == null) {
            log.error("未找到对应连接器: type={}", config.getSourceType());
            return;
        }

        try {
            // 测试连通性
            connector.testConnection(config.getConnectionConfig());

            // 执行同步（全量或增量）
            SyncResult result;
            if ("FULL".equals(config.getSyncStrategy()) || config.getLastCursor() == null) {
                result = connector.fullSync(buildContext(config));
            } else {
                result = connector.incrementalSync(buildContext(config), config.getLastCursor());
            }

            // 更新数据源状态
            dataSourceMapper.updateSyncResult(datasourceId,
                    result.getNextCursor(), result.getTotalRows(), LocalDateTime.now());

            // ★ 发布数据同步完成事件，触发后续质检+ETL管道
            DataSyncCompletedEvent event = new DataSyncCompletedEvent();
            event.setDatasourceId(datasourceId);
            event.setRawData(result.getRawData());
            event.setSyncBatchId(UUID.randomUUID().toString());
            eventPublisher.publishEvent(event);

            log.info("数据同步完成: source={}, rows={}, batch={}",
                    config.getSourceName(), result.getTotalRows(), event.getSyncBatchId());

        } catch (Exception e) {
            log.error("数据同步失败: source={}", config.getSourceName(), e);
            dataSourceMapper.updateStatus(datasourceId, "ERROR");
        }
    }
}
```

***

## 8.8 数据质量检查模块详细设计（方案四：质检引擎） `[P2]`

> ⚠️ 本模块在 P2 阶段独立为 Maven 模块 `agent-qr-data-quality`，完整包结构见 [12. 数据质量检查模块详细设计](#12-数据质量检查模块详细设计-agent-qr-data-quality)
>
> 以下为详细类设计，类文件位于 `org.example.agent_qr.dataquality` 包路径下。

### 8.8.1 DataQualityChecker（质检规则链）

```java
/**
 * 数据质量检查引擎
 * 使用规则链模式，按顺序执行：完整性 → 唯一性 → 格式 → 编码 → 长度
 * 任一维度异常记录到质检报告，合格率<50%自动阻断整批
 */
@Component
@Slf4j
public class DataQualityChecker {

    @Autowired
    private List<QualityRule> rules; // Spring 自动注入所有 QualityRule 实现

    @Value("${dataquality.block-threshold:0.5}")
    private double blockThreshold; // 阻断阈值（合格率），默认 50%

    /**
     * 对同步批次执行全维度质量检查
     * @param batchId 批次号
     * @param rawData 原始数据
     * @return 质检报告
     */
    public QualityReport check(String batchId, List<Map<String, Object>> rawData) {
        QualityReport report = new QualityReport();
        report.setBatchId(batchId);
        report.setTotalCount(rawData.size());

        Set<String> contentHashes = new HashSet<>();
        int totalFailCount = 0;

        // ★ 规则链顺序执行
        for (int i = 0; i < rawData.size(); i++) {
            Map<String, Object> record = rawData.get(i);

            for (QualityRule rule : rules) {
                RuleResult result = rule.evaluate(record);
                if (!result.isPassed()) {
                    report.addFailure(rule.getName(), i, result.getReason());
                    totalFailCount++;
                }
            }

            // 唯一性检查（跨记录）
            String contentHash = hashRecord(record);
            if (!contentHashes.add(contentHash)) {
                report.addFailure("唯一性", i, "内容重复（与已有记录哈希冲突）");
                totalFailCount++;
            }
        }

        int passCount = rawData.size() - (int) report.getFailures().stream()
                .map(QualityFailure::getRecordIndex).distinct().count();
        report.setPassCount(passCount);
        report.setFailCount(rawData.size() - passCount);
        report.setPassRate((double) passCount / rawData.size());

        // ★ 合格率低于阈值 → 阻断
        report.setBlocked(report.getPassRate() < blockThreshold);

        log.info("质检完成: batch={}, total={}, pass={}, fail={}, rate={:.2f}, blocked={}",
                batchId, rawData.size(), passCount,
                rawData.size() - passCount, report.getPassRate(), report.isBlocked());

        return report;
    }

    private String hashRecord(Map<String, Object> record) {
        String content = record.values().stream()
                .map(v -> v != null ? v.toString() : "").collect(Collectors.joining("|"));
        return DigestUtils.md5Hex(content); // MD5 仅用于哈希计算，非密码场景
    }
}
```

### 8.8.2 质检规则实现示例

```java
/** 质检规则接口 */
public interface QualityRule {
    String getName();
    RuleResult evaluate(Map<String, Object> record);
}

/** 完整性检查：必填字段非空 */
@Component
public class CompletenessRule implements QualityRule {
    @Override public String getName() { return "完整性"; }

    @Override
    public RuleResult evaluate(Map<String, Object> record) {
        // 检查 title/content 等关键字段是否为空
        Object content = record.getOrDefault("content", record.getOrDefault("text", null));
        if (content == null || content.toString().trim().isEmpty()) {
            return RuleResult.fail("内容字段为空，无法生成有效知识片段");
        }
        return RuleResult.pass();
    }
}

/** 编码检查：自动检测并标记非 UTF-8 */
@Component
public class EncodingRule implements QualityRule {
    @Override public String getName() { return "编码"; }

    @Override
    public RuleResult evaluate(Map<String, Object> record) {
        for (Map.Entry<String, Object> entry : record.entrySet()) {
            if (entry.getValue() instanceof String text) {
                Charset detected = CharsetDetector.detect(text);
                if (detected != StandardCharsets.UTF_8) {
                    return RuleResult.fail(
                        String.format("字段 '%s' 编码为 %s，需转UTF-8",
                            entry.getKey(), detected.displayName()));
                }
            }
        }
        return RuleResult.pass();
    }
}
```

***

## 8.9 ETL 标准化管道详细设计（方案二：ETL 标准化） `[P2]`

> ⚠️ 本模块在 P2 阶段独立为 Maven 模块 `agent-qr-etl`，完整包结构见 [13. ETL 标准化管道模块详细设计](#13-etl-标准化管道模块详细设计-agent-qr-etl)
>
> 以下为详细类设计，类文件位于 `org.example.agent_qr.etl` 包路径下。

### 8.9.1 DataNormalizer（数据标准化引擎）

```java
/**
 * 数据标准化引擎
 * 将多源异构数据统一为 Canonical 格式
 * 管道：分类 → 字段映射 → 字典翻译 → 格式统一 → 结构化转换 → Canonical 输出
 */
@Component
@Slf4j
public class DataNormalizer {

    @Autowired
    private FieldMappingEngine fieldMappingEngine;

    @Autowired
    private StructuredDataConverter structuredConverter;

    /**
     * 对质检合格的原始数据进行标准化
     * @param rawData 质检合格的原始数据
     * @param config 数据源配置（含字段映射、字典等）
     * @return Canonical 标准记录列表
     */
    public List<CanonicalRecord> normalize(List<Map<String, Object>> rawData,
                                            DataSourceConfig config) {
        List<CanonicalRecord> records = new ArrayList<>();

        for (Map<String, Object> raw : rawData) {
            // 1. 数据分类
            DataType dataType = classify(raw, config);

            // 2. 字段映射 + 字典翻译 + 格式统一
            Map<String, Object> mapped = fieldMappingEngine.apply(raw, config);

            // 3. 转换为 Canonical 格式
            CanonicalRecord record = new CanonicalRecord();
            record.setSourceSystem(config.getSourceName());
            record.setDomain(config.getDomain());
            record.setDataType(dataType);
            record.setMetadata(buildMetadata(mapped, config));

            // 4. 根据数据类型生成 canonical_text
            switch (dataType) {
                case STRUCTURED -> // ★ 结构化 → 自然语言段落
                    record.setCanonicalText(structuredConverter.convert(mapped, config));
                case SEMI_STRUCTURED -> // 半结构化 → 提取关键字段 + 原文
                    record.setCanonicalText(extractSemiStructured(mapped));
                case UNSTRUCTURED -> // 非结构化 → 直接透传原始文本
                    record.setCanonicalText((String) mapped.getOrDefault("content", ""));
            }

            records.add(record);
        }
        return records;
    }

    /** 自动分类：检查数据特征判断类型 */
    private DataType classify(Map<String, Object> record, DataSourceConfig config) {
        if ("JDBC".equals(config.getSourceType())) return DataType.STRUCTURED;
        if (record.containsKey("_file_type")) return DataType.UNSTRUCTURED;
        if (record.containsKey("_json_path") || record.containsKey("_xml_path"))
            return DataType.SEMI_STRUCTURED;
        return DataType.UNSTRUCTURED;
    }
}
```

### 8.9.2 StructuredDataConverter（结构化→自然语言转换）

```java
/**
 * 结构化数据 → 自然语言文本转换器
 * 核心功能：将数据库表行/CSV行转为语义完整的自然语言描述段落
 *
 * 输入：{emp_name:"张三", dept_code:"D01", salary:15000, hire_date:"2024-01-15"}
 * 输出："员工张三，所属部门为研发部，月薪15000元，入职日期为2024年1月15日。"
 */
@Component
public class StructuredDataConverter {

    /**
     * 将结构化行记录转为自然语言段落
     * @param mappedRecord 字段映射后的记录（key=canonical_field）
     * @param config 数据源配置（含字段模板和字典）
     * @return 自然语言描述
     */
    public String convert(Map<String, Object> mappedRecord, DataSourceConfig config) {
        StringBuilder sb = new StringBuilder();
        sb.append("【").append(config.getSourceName()).append("】");

        // 添加表/实体注释作为段落标题
        if (mappedRecord.containsKey("_table_comment")) {
            sb.append(" - ").append(mappedRecord.get("_table_comment"));
        }
        sb.append("\n");

        // 按优先级排序字段
        List<FieldMapping> sortedFields = config.getFieldMappings().stream()
                .filter(f -> "ACTIVE".equals(f.getStatus()))
                .sorted(Comparator.comparingInt(FieldMapping::getPriority))
                .collect(Collectors.toList());

        for (FieldMapping field : sortedFields) {
            Object rawValue = mappedRecord.get(field.getCanonicalField());
            if (rawValue == null) continue;

            // 应用字典翻译
            String displayValue = applyDictionary(rawValue.toString(), field.getDictMapping());

            // 应用格式转换（日期、数字等）
            displayValue = applyFormat(displayValue, field);

            // 使用自然语言模板生成文本
            // 模板示例："{字段中文名}为{值}{单位}"
            String template = field.getTemplate() != null
                    ? field.getTemplate()
                    : field.getDisplayName() + "为{值}";

            String text = template.replace("{字段中文名}", field.getDisplayName())
                                  .replace("{值}", displayValue)
                                  .replace("{单位}", field.getUnit() != null ? field.getUnit() : "");
            sb.append(text).append("，");
        }

        sb.append("\n");
        return sb.toString();
    }

    /**
     * 应用字典翻译
     * 将源系统的编码值翻译为可读的自然语言
     * 示例："D01" → "研发部", "M" → "男"
     */
    private String applyDictionary(String value, Map<String, String> dictMapping) {
        if (dictMapping == null || dictMapping.isEmpty()) return value;
        return dictMapping.getOrDefault(value, value);
    }

    /**
     * 应用格式转换
     * 日期: 2024-01-15 → 2024年1月15日
     * 金额: 15000.00 → 15,000
     */
    private String applyFormat(String value, FieldMapping field) {
        if (field.getTransformRule() == null) return value;

        return switch (field.getTransformRule()) {
            case "DATE_TO_CHINESE" -> convertDateToChinese(value);
            case "MONEY_FORMAT" -> String.format("%,.0f", Double.parseDouble(value));
            case "PERCENTAGE" -> String.format("%.1f%%", Double.parseDouble(value) * 100);
            default -> value;
        };
    }
}
```

### 8.9.3 CanonicalRecord（统一标准记录）

```java
/**
 * Canonical 统一标准记录
 * 所有数据源的数据经 ETL 管道处理后输出的统一格式
 */
@Data
public class CanonicalRecord {

    /** 来源系统标识 */
    private String sourceSystem;

    /** 业务域 */
    private String domain;

    /** 数据类型：STRUCTURED / SEMI_STRUCTURED / UNSTRUCTURED */
    private DataType dataType;

    /** 标准化后的文本内容（可直接进入切片管道） */
    private String canonicalText;

    /** 业务元数据（用于检索过滤和来源引用） */
    private Map<String, Object> metadata;

    /** 数据源配置ID */
    private Long datasourceId;

    /** 同步批次号 */
    private String syncBatchId;
}
```

***

## 8.10 知识目录模块详细设计（方案三：知识目录） `[P2 关键词路由 + P3 语义路由]`

> ⚠️ 本模块在 P2 阶段独立为 Maven 模块 `agent-qr-catalog`，完整包结构见 [14. 知识目录模块详细设计](#14-知识目录模块详细设计-agent-qr-catalog)
>
> 以下为详细类设计，类文件位于 `org.example.agent_qr.catalog` 包路径下。

### 8.10.1 KnowledgeCatalogService（目录管理）

```java
/**
 * 知识目录管理服务
 * 按三级层级组织：业务域 → 数据源 → 数据实体
 * 数据完成 ETL 后自动更新目录索引
 */
@Service
@Slf4j
public class KnowledgeCatalogService {

    @Autowired
    private DataSourceMapper dataSourceMapper;

    @Autowired
    private DocumentMapper documentMapper;

    @Autowired
    private ChunkMapper chunkMapper;

    /**
     * 获取知识目录树
     * @return 三级目录结构
     */
    public CatalogTree getCatalogTree() {
        CatalogTree tree = new CatalogTree();

        // 按域分组
        List<DataSourceConfig> allSources = dataSourceMapper.selectAll();
        Map<String, List<DataSourceConfig>> byDomain = allSources.stream()
                .filter(s -> "ACTIVE".equals(s.getStatus()))
                .collect(Collectors.groupingBy(DataSourceConfig::getDomain));

        for (Map.Entry<String, List<DataSourceConfig>> domainEntry : byDomain.entrySet()) {
            DomainNode domainNode = new DomainNode();
            domainNode.setDomainName(domainEntry.getKey());
            domainNode.setSourceCount(domainEntry.getValue().size());

            for (DataSourceConfig source : domainEntry.getValue()) {
                SourceNode sourceNode = new SourceNode();
                sourceNode.setSourceId(source.getId());
                sourceNode.setSourceName(source.getSourceName());
                sourceNode.setSourceType(source.getSourceType());
                sourceNode.setLastSyncAt(source.getLastSyncAt());
                sourceNode.setTotalSynced(source.getTotalSynced());

                // 获取该数据源下的数据实体
                sourceNode.setEntities(getEntities(source.getId()));

                domainNode.addSource(sourceNode);
            }
            tree.addDomain(domainNode);
        }
        return tree;
    }

    /**
     * 更新知识目录（ETL完成后触发）
     */
    @EventListener
    @Async
    public void onDataETLed(DataETLedEvent event) {
        log.info("知识目录更新: domain={}, source={}, entityCount={}",
                event.getDomain(), event.getSourceName(), event.getEntityCount());
        // 重建域索引（可优化为增量更新）
    }
}
```

### 8.10.2 DomainRouter（业务域路由器）

```java
/**
 * 业务域路由器 —— RAG 检索时的域裁剪
 *
 * 用户提问 → NLP 提取关键实体 → 匹配知识目录 → 筛选检索域 → 精准检索
 * 将全库盲搜变为域内精准搜索，大幅提升检索精度和速度
 */
@Component
@Slf4j
public class DomainRouter {

    @Autowired
    private KnowledgeCatalogService catalogService;

    /**
     * 根据用户问题路由到相关业务域
     * @param query 用户原始问题
     * @return 匹配的业务域列表 + 权重（用于检索过滤和加权）
     */
    public DomainRoutingResult route(String query) {
        CatalogTree catalog = catalogService.getCatalogTree();

        // 1. NLP 实体词提取（可集成 IK Analyzer 或 jieba 分词）
        List<String> keywords = extractKeywords(query);

        // 2. 匹配目录中的域和数据实体
        Map<String, Double> domainScores = new HashMap<>();
        List<String> matchedEntities = new ArrayList<>();

        for (DomainNode domain : catalog.getDomains()) {
            double score = 0.0;

            // 域名匹配
            if (containsAny(query, domain.getDomainName())) {
                score += 0.5;
            }

            // 数据源名匹配
            for (SourceNode source : domain.getSources()) {
                if (containsAny(query, source.getSourceName())) {
                    score += 0.3;
                }
                // 实体名匹配
                for (EntityNode entity : source.getEntities()) {
                    if (containsAny(query, entity.getEntityName())) {
                        score += 0.4;
                        matchedEntities.add(entity.getEntityName());
                    }
                }
            }

            if (score > 0) {
                domainScores.put(domain.getDomainName(), score);
            }
        }

        // 3. 返回路由结果（用于检索时的 metadata 过滤）
        DomainRoutingResult result = new DomainRoutingResult();
        result.setMatchedDomains(domainScores);
        result.setMatchedEntities(matchedEntities);

        // 若未匹配到任何域，回退到全库检索
        result.setFallbackToGlobal(domainScores.isEmpty());

        log.info("检索路由: query='{}', matchedDomains={}, entities={}, fallback={}",
                query, domainScores.keySet(), matchedEntities, result.isFallbackToGlobal());

        return result;
    }

    /**
     * 构建 ChromaDB 检索的 metadata 过滤条件
     * 将路由结果转换为向量数据库的过滤表达式
     */
    public Map<String, String> buildRetrievalFilter(DomainRoutingResult routing) {
        if (routing.isFallbackToGlobal()) {
            return Collections.emptyMap(); // 全库检索，不过滤
        }

        // 只检索匹配域的数据
        Map<String, String> filter = new HashMap<>();
        filter.put("domain", String.join(",", routing.getMatchedDomains().keySet()));
        return filter;
    }
}
```

***

## 8.11 MySQL 前置结构化过滤（修正方案一） `[P2]`

> ⚠️ 本功能归属于 RAG 问答模块 `agent-qr-rag`，类文件位于 `org.example.agent_qr.rag.filter` 包路径下。
> 完整包结构见 [6. RAG 问答模块详细设计 - filter 子包](#6-rag-问答模块详细设计-agent-qr-rag)

### 8.11.1 StructuredFilterService

```java
/**
 * MySQL 前置结构化过滤服务
 * 将 LLM 提取的过滤条件转为 MySQL B+树查询，产出 chunk_id 候选集
 * ChromaDB 仅在候选集（≤ 500 条）内做语义检索，延迟从 3-5s 降至 50-100ms
 */
@Service
@Slf4j
public class StructuredFilterService {

    @Autowired
    private ChunkStructuredMapper structuredMapper;

    /**
     * 根据结构化条件在 MySQL 中过滤出候选 chunk_id 集合
     * @param domain 业务域
     * @param conditions LLM 提取的过滤条件列表
     * @return chunk_id 候选集（上限 500 条）
     */
    public List<Long> filterChunkIds(String domain, List<FilterCondition> conditions) {
        if (conditions == null || conditions.isEmpty()) {
            return Collections.emptyList();
        }

        List<Set<Long>> idSets = new ArrayList<>();
        for (FilterCondition cond : conditions) {
            Set<Long> ids = switch (cond.getFieldType()) {
                case NUMBER -> structuredMapper.selectChunkIdsByNumberRange(
                    domain, cond.getFieldName(),
                    cond.getNumericMin(), cond.getNumericMax());
                case DATE -> structuredMapper.selectChunkIdsByDateRange(
                    domain, cond.getFieldName(),
                    cond.getDateMin(), cond.getDateMax());
                case ENUM, STRING -> structuredMapper.selectChunkIdsByStringValue(
                    domain, cond.getFieldName(), cond.getStringValue());
                default -> Set.of();
            };
            if (!ids.isEmpty()) {
                idSets.add(ids);
            }
        }

        // ★ 多条件交集（AND 语义）
        if (idSets.isEmpty()) return Collections.emptyList();
        Set<Long> result = new HashSet<>(idSets.get(0));
        for (int i = 1; i < idSets.size(); i++) {
            result.retainAll(idSets.get(i));
        }

        // ★ 截断到 500 条，防止候选集过大
        return result.stream().limit(500).collect(Collectors.toList());
    }
}
```

### 8.11.2 ChunkStructuredMapper

```java
@Mapper
public interface ChunkStructuredMapper extends BaseMapper<ChunkStructured> {

    /** ★ 数值范围过滤 —— 使用 B+树索引 idx_domain_field_number */
    @Select("""
        SELECT DISTINCT chunk_id FROM kb_chunk_structured
        WHERE domain = #{domain}
          AND field_name = #{fieldName}
          AND numeric_value >= #{minValue}
          AND numeric_value <= #{maxValue}
        LIMIT 500
    """)
    Set<Long> selectChunkIdsByNumberRange(@Param("domain") String domain,
        @Param("fieldName") String fieldName,
        @Param("minValue") BigDecimal minValue,
        @Param("maxValue") BigDecimal maxValue);

    /** ★ 日期范围过滤 —— 使用 B+树索引 idx_domain_field_date */
    @Select("""
        SELECT DISTINCT chunk_id FROM kb_chunk_structured
        WHERE domain = #{domain}
          AND field_name = #{fieldName}
          AND date_value >= #{minDate}
          AND date_value <= #{maxDate}
        LIMIT 500
    """)
    Set<Long> selectChunkIdsByDateRange(@Param("domain") String domain,
        @Param("fieldName") String fieldName,
        @Param("minDate") LocalDate minDate,
        @Param("maxDate") LocalDate maxDate);

    /** 枚举/字符串精确匹配 */
    @Select("""
        SELECT DISTINCT chunk_id FROM kb_chunk_structured
        WHERE domain = #{domain}
          AND field_name = #{fieldName}
          AND field_value = #{value}
        LIMIT 500
    """)
    Set<Long> selectChunkIdsByStringValue(@Param("domain") String domain,
        @Param("fieldName") String fieldName,
        @Param("value") String value);
}
```

### 8.11.3 HybridRetriever 集成

```java
// HybridRetriever.hybridSearch() 中新增结构化过滤分支：
public List<RetrievedDocument> hybridSearch(String query, float[] queryEmbedding,
                                              DomainRoutingResult routing,
                                              List<FilterCondition> filterConditions) {
    // ★ Step 0: MySQL 前置过滤
    List<Long> candidateChunkIds = null;
    if (filterConditions != null && !filterConditions.isEmpty()) {
        candidateChunkIds = structuredFilterService.filterChunkIds(
            routing.getPrimaryDomain(), filterConditions);
        if (candidateChunkIds.isEmpty()) {
            return Collections.emptyList(); // 无匹配数据
        }
    }

    // Step 1-3: 在候选集内（或全库）执行语义+BM25+Rerank
    // 若 candidateChunkIds 非空，ChromaDB 检索时传入 ID 过滤
    Map<String, Object> chromaFilter = new HashMap<>();
    if (candidateChunkIds != null) {
        chromaFilter.put("chunk_id", Map.of("$in",
            candidateChunkIds.stream().map(String::valueOf).toList()));
    }
    // ... 后续混合检索逻辑 ...
}
```

***

## 8.12 文档删除一致性设计 v2（修正方案二） `[P2]`

> ⚠️ 本功能归属于数据一致性补偿模块 `agent-qr-compensation`，类文件位于 `org.example.agent_qr.compensation.service.DocumentDeleteServiceV2`。
> 完整设计见 [§10.4 DocumentDeleteServiceV2](#104-documentdeleteservicev2物理删除服务-p2)。
> ★ 注意：P2 最终方案中，MySQL 逻辑删除由 `DocumentDeleteListener` 完成，此类只负责 ChromaDB 物理删除。

```java
/**
 * 文档删除服务 v2 —— 只负责 ChromaDB 物理删除 + 重试队列
 *
 * ★ P2 最终架构变更：
 * - MySQL 逻辑删除（softDelete/softDeleteByDocumentId）由 DocumentDeleteListener 完成
 * - 本类只负责 ChromaDB 物理 DELETE API 调用
 * - 调用链：KnowledgeController → DocumentCommandService.requestDeleteDocument()
 *          → [事件] DocumentDeleteRequestedEvent
 *          → DocumentDeleteListener.handleDocumentDeleteRequested()
 *             → MySQL 逻辑删除 (chunkMapper.softDeleteByDocumentId + documentMapper.softDelete)
 *             → this.asyncPhysicalDelete(documentId, chromaIds)  ← 本类方法
 */
@Service
@Slf4j
public class DocumentDeleteServiceV2 {

    @Autowired private ChromaVectorStore chromaVectorStore;
    @Autowired private DeleteTaskMapper deleteTaskMapper;
    @Autowired private DeadLetterQueue deadLetterQueue;

    /**
     * ★ 异步物理删除 ChromaDB 向量（由 DocumentDeleteListener 调用）
     * MySQL 逻辑删除已在 Listener 中完成，此处只负责 ChromaDB 物理删除
     *
     * @param documentId 文档ID
     * @param chromaIds ChromaDB 向量ID列表
     */
    @Async("deleteExecutor")
    public void asyncPhysicalDelete(Long documentId, List<String> chromaIds) {
        // 1. 创建 DeleteTask 追踪记录
        DeleteTask task = new DeleteTask();
        task.setDocumentId(documentId);
        task.setChromaIds(JSON.toJSONString(chromaIds));
        task.setStatus("PENDING");
        deleteTaskMapper.insert(task);

        // 2. 调用 ChromaDB DELETE API 物理删除
        try {
            chromaVectorStore.deleteByIds(chromaIds);
            deleteTaskMapper.updateStatus(task.getId(), "DONE");
            log.info("ChromaDB 物理删除成功: docId={}, count={}", documentId, chromaIds.size());
        } catch (Exception e) {
            log.error("ChromaDB 物理删除失败: taskId={}, docId={}", task.getId(), documentId, e);
            deleteTaskMapper.incrementRetryCount(task.getId());
            deleteTaskMapper.updateStatus(task.getId(), "FAILED");
            // ★ 进入 DLQ 重试（指数退避：3s→9s→27s→81s）
            deadLetterQueue.enqueue("DELETE", documentId,
                JSON.toJSONString(chromaIds), e.getMessage());
        }
    }

    // ============ 以下为旧版方法（已废弃，仅供参考） ============

    /**
     * @deprecated P2 最终方案中，MySQL 逻辑删除移至 DocumentDeleteListener，
     *             物理删除统一使用 asyncPhysicalDelete(Long, List<String>)
     */
    @Deprecated
    @Transactional
    public void deleteDocument(Long documentId) {
        // ... 旧版逻辑（仅供理解演进过程）
    }

    /**
     * @deprecated 使用 asyncPhysicalDelete(Long, List<String>) 替代
     */
    @Deprecated
    @Async("deleteExecutor")
    public void asyncPhysicalDelete(DeleteTask task) {
        // ... 旧版逻辑（仅供理解演进过程）
    }
}

// ★ 检索时自动过滤已删除切片
// SELECT * FROM kb_chunk WHERE document_id = ? AND deleted = 0
```

***

## 8.13 CQRS 读写分离路由（修正方案三） `[P3]`

> ⚠️ 本功能归属于公共模块 `agent-qr-common` (P3)，类文件位于 `org.example.agent_qr.common.datasource` 包路径下。
> 完整包结构见 [2. 公共模块详细设计 - datasource 子包](#2-公共模块详细设计-agent-qr-common)

### 8.13.1 ReadWriteRoutingDataSource

```java
/**
 * CQRS 读写分离数据源路由器
 * 根据 @Transactional(readOnly) 自动路由到主库或从库
 */
@Component
public class ReadWriteRoutingDataSource extends AbstractRoutingDataSource {

    private static final ThreadLocal<Boolean> READ_ONLY_HOLDER = new ThreadLocal<>();

    public static void setReadOnly(boolean readOnly) { READ_ONLY_HOLDER.set(readOnly); }
    public static void clear() { READ_ONLY_HOLDER.remove(); }

    @Override
    protected Object determineCurrentLookupKey() {
        return Boolean.TRUE.equals(READ_ONLY_HOLDER.get()) ? "read" : "write";
    }

    @PostConstruct
    public void initDataSources() {
        Map<Object, Object> dataSources = new HashMap<>();
        dataSources.put("write", writeDataSource());  // 主库
        dataSources.put("read", readDataSource());    // 从库（初期可与主库同实例）
        this.setTargetDataSources(dataSources);
        this.setDefaultTargetDataSource(writeDataSource());
    }
}
```

### 8.13.2 ReadWriteDataSourceAspect

```java
/**
 * CQRS 读写分离切面
 * 在 @Transactional 方法执行前根据 readOnly 属性切换数据源
 * @Order(-1) 确保在 Spring 事务拦截器之前执行
 */
@Aspect
@Component
@Order(-1)
public class ReadWriteDataSourceAspect {

    @Around("@annotation(transactional)")
    public Object routeDataSource(ProceedingJoinPoint pjp, Transactional transactional)
            throws Throwable {
        try {
            ReadWriteRoutingDataSource.setReadOnly(transactional.readOnly());
            return pjp.proceed();
        } finally {
            ReadWriteRoutingDataSource.clear();
        }
    }
}
```

### 8.13.3 Service 层约束

```java
// ★ CommandService —— 写事务，路由主库
@Service
public class DocumentCommandService {
    @Transactional(readOnly = false)  // 写事务 → 主库
    public Document uploadDocument(...) { ... }
}

// ★ QueryService —— 只读事务，路由从库
@Service
public class DocumentQueryService {
    @Transactional(readOnly = true)   // 只读 → 从库/无锁
    public IPage<Document> listDocuments(...) { ... }
}
```

***

## 8.14 批量向量化聚合（缺失拼图） `[P2]`

> ⚠️ 本功能归属于 RAG 问答模块 `agent-qr-rag`，类文件位于 `org.example.agent_qr.rag.embedding.BatchEmbeddingService`。
> 完整包结构见 [6. RAG 问答模块详细设计 - embedding 子包](#6-rag-问答模块详细设计-agent-qr-rag)

### 8.14.1 BatchEmbeddingService

```java
/**
 * 批量向量化聚合服务
 * BlockingQueue 攒批 + 多消费者并发
 * 50 万切片从 27 小时（逐条调用）→ 16 分钟（批量+并发，100倍提升）
 */
@Component
@Slf4j
public class BatchEmbeddingService {

    @Autowired private ProviderFactory providerFactory;

    /** ★ 攒批队列 */
    private final BlockingQueue<ChunkEmbedTask> batchQueue =
        new LinkedBlockingQueue<>(2000);

    @Value("${embedding.batch.size:32}")
    private int batchSize;

    @Value("${embedding.batch.timeout-ms:100}")
    private int batchTimeoutMs;

    @PostConstruct
    public void startConsumers() {
        int consumers = Runtime.getRuntime().availableProcessors();
        for (int i = 0; i < consumers; i++) {
            new Thread(this::consumeBatch, "embed-batch-" + i).start();
        }
        log.info("批量向量化消费者启动: {} 个线程, batchSize={}", consumers, batchSize);
    }

    /** 提交单个切片到攒批队列 */
    public CompletableFuture<float[]> submit(Chunk chunk) {
        CompletableFuture<float[]> future = new CompletableFuture<>();
        batchQueue.offer(new ChunkEmbedTask(chunk, future));
        return future;
    }

    /** 消费者循环：攒批 → 批量 API → 分发结果 */
    private void consumeBatch() {
        List<ChunkEmbedTask> batch = new ArrayList<>(batchSize);
        long deadline = System.currentTimeMillis() + batchTimeoutMs;

        while (true) {
            try {
                long remaining = deadline - System.currentTimeMillis();
                // ★ 条件触发：攒满 OR 超时
                if (remaining <= 0 || batch.size() >= batchSize) {
                    if (!batch.isEmpty()) {
                        executeBatch(batch);
                        batch.clear();
                        deadline = System.currentTimeMillis() + batchTimeoutMs;
                    }
                }
                ChunkEmbedTask task = batchQueue.poll(
                    Math.max(remaining, 1), TimeUnit.MILLISECONDS);
                if (task != null) batch.add(task);
            } catch (InterruptedException e) { break; }
        }
    }

    /** ★ 一次 API 调用处理一批 */
    private void executeBatch(List<ChunkEmbedTask> batch) {
        List<String> texts = batch.stream()
            .map(t -> t.chunk.getContent()).toList();
        try {
            List<float[]> embeddings = providerFactory.getEmbeddingProvider()
                .embedBatch(texts);
            for (int i = 0; i < batch.size(); i++) {
                batch.get(i).future.complete(embeddings.get(i));
            }
            log.debug("批量向量化: {}条完成", batch.size());
        } catch (Exception e) {
            // ★ 批量失败 → 降级为逐条重试
            for (ChunkEmbedTask task : batch) {
                try {
                    task.future.complete(providerFactory.getEmbeddingProvider()
                        .embed(task.chunk.getContent()));
                } catch (Exception ex) {
                    task.future.completeExceptionally(ex);
                }
            }
        }
    }

    private record ChunkEmbedTask(Chunk chunk, CompletableFuture<float[]> future) {}
}
```

***

## 8.15 其他关键优化设计 `[P2 除标注外 + P3]`

> ⚠️ 本节的优化改进分属不同模块，各归属如下:
>
> - BM25Retriever v2 → `agent-qr-rag`
> - PdfParser 流式解析 → `agent-qr-knowledge`
> - EmbeddingDimensionManager → `agent-qr-rag` (P3)
> - TraceIdFilter + MdcTaskDecorator → `agent-qr-common` (P2)
> - RefreshTokenService → `agent-qr-auth` (P2)
> - DomainRouter v2 (Embedding语义) → `agent-qr-catalog` (P3)
> - 线程池三池隔离 → `agent-qr-web` | Caffeine 缓存 → `agent-qr-common`

### 8.15.1 BM25Retriever v2 — Lucene 磁盘索引 + 异步构建 `[P2]`

```java
@Component
@Slf4j
public class BM25RetrieverV2 {
    private Path indexDir = Paths.get("./data/lucene_index");
    private volatile IndexSearcherManager searcherManager;

    /** ★ 容器就绪后异步构建，不阻塞 Spring 启动 */
    @Async("indexBuilderExecutor")
    @EventListener(ApplicationReadyEvent.class)
    public void buildIndex() {
        int pageSize = 1000, offset = 0;
        while (true) {
            List<Chunk> batch = chunkMapper.selectReadyChunksPaged(offset, pageSize);
            if (batch.isEmpty()) break;
            addBatchToIndex(batch);
            offset += pageSize;
        }
        searcherManager.commit();
        log.info("Lucene 磁盘索引构建完成: {} 条", offset);
    }

    /** ★ 增量更新：文档处理完成后触发 */
    @EventListener @Async
    public void onEmbeddingCompleted(EmbeddingCompletedEvent event) {
        List<Chunk> newChunks = chunkMapper.selectByDocumentId(event.getDocumentId());
        addBatchToIndex(newChunks);
    }
}
```

### 8.15.2 PdfParser — 流式解析 + 内存保护 `[P2]`

```java
@Component
public class PdfParser {
    @Value("${parser.pdf.max-memory-mb:256}") private int maxMemoryMb;

    public String parse(String filePath) {
        long fileSize = new File(filePath).length();
        if (fileSize > maxMemoryMb * 1024 * 1024) {
            return parseStreaming(filePath);  // ★ 流式
        }
        try (PDDocument doc = Loader.loadPDF(new File(filePath))) {
            // ... 原逻辑 ...
        }
    }

    private String parseStreaming(String filePath) {
        StringBuilder sb = new StringBuilder();
        try (PDDocument doc = Loader.loadPDF(new File(filePath),
                new MemoryUsageSetting().setMaxMemoryUsage(maxMemoryMb * 1024 * 1024))) {
            PDFTextStripper stripper = new PDFTextStripper();
            stripper.setSortByPosition(true);
            for (int page = 1; page <= doc.getNumberOfPages(); page++) {
                stripper.setStartPage(page);
                stripper.setEndPage(page);
                sb.append(stripper.getText(doc));  // ★ 逐页释放内存
            }
        }
        return sb.toString();
    }
}
```

### 8.15.3 EmbeddingDimensionManager — Collection 隔离 + 维度检测 `[P3]`

```java
@Component
public class EmbeddingDimensionManager {
    @Autowired private ProviderFactory providerFactory;

    /** ★ Collection 命名含模型标识，切换 Provider 自动使用新 Collection */
    public String getCollectionName() {
        return "kb_" + providerFactory.getEmbeddingProviderType() + "_" +
               providerFactory.getEmbeddingModelName().replace(":", "-");
    }

    @EventListener(ApplicationReadyEvent.class)
    public void checkDimension() {
        String col = getCollectionName();
        if (!chromaClient.collectionExists(col)) {
            log.warn("新 Embedding 模型: collection={} 不存在，需全量重建向量", col);
        }
    }
}
```

### 8.15.4 TraceIdFilter + MdcTaskDecorator — 全链路追踪 `[P2]`

```java
@Component
public class TraceIdFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest req, HttpServletResponse resp,
                                     FilterChain chain) throws ServletException, IOException {
        String traceId = Optional.ofNullable(req.getHeader("X-Trace-Id"))
            .orElse(UUID.randomUUID().toString().replace("-", "").substring(0, 16));
        MDC.put("traceId", traceId);
        resp.setHeader("X-Trace-Id", traceId);
        try { chain.doFilter(req, resp); } finally { MDC.clear(); }
    }
}

/** ★ 异步线程自动继承 MDC */
public class MdcTaskDecorator implements TaskDecorator {
    @Override
    public Runnable decorate(Runnable runnable) {
        Map<String, String> context = MDC.getCopyOfContextMap();
        return () -> {
            if (context != null) MDC.setContextMap(context);
            try { runnable.run(); } finally { MDC.clear(); }
        };
    }
}
```

### 8.15.5 RefreshTokenService — 双 Token 机制 `[P2]`

```java
@Service
public class RefreshTokenService {
    @Autowired private TokenRefreshMapper tokenRefreshMapper;

    /** 登录时签发双 Token */
    public TokenPair issueTokens(SysUser user) {
        String accessToken = jwtUtil.generateToken(user.getId(), user.getUsername(),
            user.getRole(), 30 * 60);          // 30 分钟
        String refreshToken = jwtUtil.generateToken(user.getId(), user.getUsername(),
            user.getRole(), 7 * 24 * 3600);    // 7 天
        tokenRefreshMapper.insert(new TokenRefresh(user.getId(), refreshToken));
        return new TokenPair(accessToken, refreshToken, 1800);
    }

    /** 刷新 Access Token */
    public TokenPair refresh(String refreshToken) {
        if (!jwtUtil.validateToken(refreshToken)) throw new BusinessException(401, "Token 已过期");
        TokenRefresh stored = tokenRefreshMapper.selectByToken(refreshToken);
        if (stored == null || stored.getRevoked()) throw new BusinessException(401, "Token 已撤销");
        SysUser user = jwtUtil.getUserFromToken(refreshToken);
        tokenRefreshMapper.deleteById(stored.getId()); // 旧 Refresh 失效
        return issueTokens(user);  // 签发新令牌对
    }

    /** 强制登出 */
    public void revoke(Long userId) { tokenRefreshMapper.revokeByUserId(userId); }
}
```

### 8.15.6 DomainRouter v2 — Embedding 语义路由 `[P3]`

```java
@Component
public class DomainRouterV2 {
    @Autowired private EmbeddingProvider embeddingProvider;
    private final Map<String, float[]> domainEmbeddings = new ConcurrentHashMap<>();

    /** ★ 用域描述文本预计算 Embedding（启动时执行一次） */
    @PostConstruct
    public void init() {
        Map.of(
            "HR", "人力资源管理、员工信息、薪资福利、绩效考核、招聘培训、组织架构、考勤制度",
            "FINANCE", "财务管理、会计核算、预算控制、报销审批、税务申报、审计合规",
            "RD", "研发管理、产品设计、技术规范、代码标准、测试用例、项目迭代管理",
            "SALES", "销售管理、客户关系、合同管理、报价体系、市场分析、渠道管理"
        ).forEach((domain, desc) -> domainEmbeddings.put(domain, embeddingProvider.embed(desc)));
    }

    /** ★ query 向量与各域向量做余弦相似度匹配 */
    public DomainRoutingResult route(String query) {
        float[] queryEmb = embeddingProvider.embed(query);
        Map<String, Double> scores = new TreeMap<>();
        domainEmbeddings.forEach((domain, emb) -> {
            double sim = cosineSimilarity(queryEmb, emb);
            if (sim > 0.3) scores.put(domain, sim);
        });
        if (scores.isEmpty()) return DomainRoutingResult.fallback();
        return DomainRoutingResult.routed(scores.entrySet().stream()
            .sorted(Map.Entry.<String, Double>comparingByValue().reversed())
            .limit(2).collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue)));
    }
}
```

### 8.15.7 线程池三池隔离 + Caffeine 缓存配置 `[P2]`

```java
@Configuration @EnableAsync
public class AsyncConfigV2 {
    @Bean("parseExecutor")    // CPU密集型 → cores threads
    public Executor parseExecutor() { ... }

    @Bean("chunkExecutor")    // CPU密集型 → cores threads
    public Executor chunkExecutor() { ... }

    @Bean("embedExecutor")    // IO密集型 → 8~20 threads
    public Executor embedExecutor() { ... }

    @Bean("deleteExecutor")   // 轻量级 → 4 threads
    public Executor deleteExecutor() { ... }
}

@Configuration
public class CacheConfig {
    @Bean public Cache<String, String> llmResponseCache() {
        return Caffeine.newBuilder().maximumSize(10000)
            .expireAfterWrite(1, TimeUnit.HOURS).build();
    }
}
```

***

## 9. Web 启动模块详细设计 (agent-qr-web)

> Maven 模块: `agent-qr-web`
> 基础包路径: `org.example.agent_qr` / `org.example.agent_qr.web.config`
> 模块依赖: **所有业务模块** (`agent-qr-common`, `agent-qr-auth`, `agent-qr-user`, `agent-qr-knowledge`, `agent-qr-rag`, `agent-qr-statistics`)

### 9.0 包结构（P1 / P2 / P3 全量）

```
agent-qr-web/
├── pom.xml
└── src/
    └── main/
        ├── java/org/example/agent_qr/
        │   └── [P1] AgentQrApplication.java          // @SpringBootApplication + @ComponentScan
        │       (启动类: org.example.agent_qr.AgentQrApplication)
        │
        ├── java/org/example/agent_qr/web/config/
        │   ├── [P1] GlobalExceptionHandler.java       // @RestControllerAdvice (BusinessException/Validation/AccessDenied/Exception)
        │   ├── [P1] CorsConfig.java                   // 跨域配置 (WebMvcConfigurer)
        │   ├── [P1→P2] SecurityConfig.java            // [P1] 基础角色路由 [P2] @EnableMethodSecurity + ABAC 路由
        │   ├── [P1] AsyncConfig.java                  // [P1] docProcessExecutor + statExecutor 双池
        │   └── [P2] AsyncConfigV2.java               // [P2] parseExecutor/chunkExecutor/embedExecutor/deleteExecutor 四池隔离
        │
        └── resources/
            ├── [P1] application.yml                   // 公共配置（数据源/MyBatis-Plus/文件上传）
            ├── [P1] application-p1.yml                // P1 阶段配置（JWT/DeepSeek/ChromaDB/rag参数）
            ├── [P2] application-p2.yml                // P2 阶段配置（Ollama/混合检索/熔断/批量/多源接入）
            ├── [P3] application-p3.yml                // P3 阶段配置（读写分离从库/语义路由）
            └── db/
                └── [P1] p1-schema.sql                 // P1 建表语句（sys_user/kb_document/kb_chunk/chat_conversation/chat_message/stat_daily）

agent-qr-web/pom.xml 依赖:
  [P1] agent-qr-common, agent-qr-auth, agent-qr-user, agent-qr-knowledge, agent-qr-rag, agent-qr-statistics
  [P1] spring-boot-starter-web, spring-boot-starter-security, mysql-connector-j, spring-boot-starter-validation
  [P2] agent-qr-compensation, agent-qr-datasource, agent-qr-data-quality, agent-qr-etl, agent-qr-catalog (新增模块依赖)
  [P3] (无新增模块依赖)
```

### 9.1 关键设计说明

| 组件                       | 阶段    | 说明                                                                                           |
| ------------------------ | ----- | -------------------------------------------------------------------------------------------- |
| `AgentQrApplication`     | P1    | 启动类，`@ComponentScan(basePackages = "org.example.agent_qr")` 确保跨模块扫描                          |
| `GlobalExceptionHandler` | P1    | 统一处理 BusinessException / MethodArgumentNotValidException / AccessDeniedException / Exception |
| `CorsConfig`             | P1    | 开发阶段允许所有来源，生产需收紧                                                                             |
| `SecurityConfig`         | P1→P2 | P1 基础角色路由（hasRole），P2 升级 `@EnableMethodSecurity(prePostEnabled = true)` 支持 ABAC              |
| `AsyncConfig`            | P1→P2 | P1 双池（docProcess + stat），P2 升级为四池隔离（parse/chunk/embed/delete）                                |
| `application.yml`        | P1→P3 | 各阶段配置拆分到 `application-p{N}.yml`，通过 `spring.profiles.active` 切换                               |

***

## 10. 数据一致性补偿模块详细设计 (agent-qr-compensation) ★

> Maven 模块: `agent-qr-compensation` (P2 新增)
> 基础包路径: `org.example.agent_qr.compensation`
> 模块依赖: `agent-qr-common`、`agent-qr-knowledge`（★ 单向依赖，通过事件驱动解耦，无循环依赖）
> 详细设计: 参见下方 [10.1 依赖设计分析](#101-依赖设计分析循环依赖解耦方案) / [10.2 OrphanVectorScanner](#102-orphanvectorscanner) / [10.3 DocumentDeleteListener](#103-documentdeletelistener) / [10.4 DocumentDeleteServiceV2](#104-documentdeleteservicev2)

### 10.0 包结构（P2 全量）

```
agent-qr-compensation/
└── src/main/java/org/example/agent_qr/compensation/
    ├── scanner/
    │   └── [P2] OrphanVectorScanner.java              // 孤儿向量扫描器 (@Scheduled 每30分钟)
    ├── listener/
    │   └── [P2] DocumentDeleteListener.java           // ★ 文档删除事件监听器（@TransactionalEventListener + @Async）
    ├── service/
    │   └── [P2] DocumentDeleteServiceV2.java          // v2 物理删除服务（ChromaDB DELETE API + DLQ 重试）
    ├── entity/
    │   └── [P2] DeleteTask.java                       // 删除任务实体
    └── mapper/
        └── [P2] DeleteTaskMapper.java                 // 删除任务 Mapper

agent-qr-compensation/pom.xml 依赖:
  [P2] agent-qr-common, agent-qr-knowledge, langchain4j-chroma, spring-boot-starter
  ★ 关键：compensation 单向依赖 knowledge（访问 ChunkMapper/DocumentMapper），
    knowledge 不依赖 compensation（只发布 common 中定义的事件），无循环依赖
```

***

### 10.1 依赖设计分析：循环依赖解耦方案 ★

#### 问题描述

若按直觉设计，`agent-qr-knowledge` 和 `agent-qr-compensation` 之间会形成 Maven 循环依赖：

```
┌──────────────────────────────────────────────────────────────────┐
│  ★ 错误设计（循环依赖）                                           │
│                                                                   │
│  knowledge ──需要调用──→ compensation (DocumentDeleteServiceV2)    │
│  compensation ──需要调用──→ knowledge (ChunkMapper/DocumentMapper) │
│                                                                   │
│  形成循环：knowledge → compensation → knowledge                    │
│  Maven 编译失败！                                                  │
└──────────────────────────────────────────────────────────────────┘
```

#### 解耦方案：事件驱动架构

```
┌──────────────────────────────────────────────────────────────────┐
│  ★ 正确设计（事件驱动解耦）                                        │
│                                                                   │
│  ┌──────────────┐    发布 DocumentDeleteRequestedEvent    ┌──────────────────┐  │
│  │  knowledge   │────────────────────────────────────────→│  compensation    │  │
│  │              │    (事件定义在 agent-qr-common)           │                  │  │
│  │  Controller  │                                         │  DeleteListener  │  │
│  │  .requestDel │  ← 不注入 compensation 的任何类          │  .handleDelete   │  │
│  │  eteDocument │                                         │  Requested()     │  │
│  │  ()          │                                         │                  │  │
│  │       │      │                                         │        │         │  │
│  │       │      │                                         │        ▼         │  │
│  │       ▼      │                                         │  MySQL 逻辑删除   │  │
│  │  发布事件     │                                         │  +               │  │
│  │  立即返回     │                                         │  ChromaDB 物理删 │  │
│  └──────────────┘                                         └────────┬─────────┘  │
│       │                                                            │             │
│       │                      依赖关系                               │             │
│       │           compensation ──单向依赖──→ knowledge              │             │
│       │           (访问 ChunkMapper, DocumentMapper)               │             │
│       │                                                            │             │
│       └──────────→ 都依赖 common（DocumentDeleteRequestedEvent） ←──┘             │
│                                                                   │
│  依赖链：compensation → knowledge → common ← compensation          │
│  通过 common 事件类解耦 —— 无循环依赖！                             │
└──────────────────────────────────────────────────────────────────┘
```

#### 关键设计决策

| # | 决策 | 说明 |
|---|------|------|
| 1 | `DocumentDeleteRequestedEvent` 定义在 `agent-qr-common` | 双方都依赖 common，不产生直接依赖 |
| 2 | knowledge 只发布事件，不注入 compensation 类 | `DocumentCommandService.requestDeleteDocument()` 仅调用 `eventPublisher.publishEvent()` |
| 3 | compensation 使用 `@TransactionalEventListener(phase = AFTER_COMMIT)` | 确保 knowledge 事务提交后才执行，保证数据一致性 |
| 4 | compensation 使用 `@Async("deleteExecutor")` | 异步执行，不阻塞 Controller HTTP 响应 |
| 5 | 异常进入 DLQ | 逻辑删除/物理删除失败时写入死信队列，由 `DlqRetryScheduler` 重试 |
| 6 | `OrphanVectorScanner` 作为最终兜底 | 每 30 分钟扫描 ChromaDB 孤儿向量，处理 DLQ 耗尽后的残留数据 |

#### 整体一致性保证链

```
用户删除请求
    │
    ▼
KnowledgeController.delete(id)
    │
    ▼
DocumentCommandService.requestDeleteDocument(id)     ← knowledge 模块
    │ 1. 标记 DELETING
    │ 2. 收集 chromaIds
    │ 3. publishEvent(DocumentDeleteRequestedEvent)
    │ 4. return 200（立即响应）
    ▼
DocumentDeleteListener.handleDocumentDeleteRequested() ← compensation 模块
    │ @TransactionalEventListener(phase = AFTER_COMMIT)
    │ @Async("deleteExecutor")
    │
    ├─→ MySQL 逻辑删除（chunkMapper.softDeleteByDocumentId + documentMapper.softDelete）
    │   ★ 检索立即过滤：SELECT ... WHERE deleted = 0
    │
    └─→ ChromaDB 物理删除（DocumentDeleteServiceV2.asyncPhysicalDelete）
        │ 成功 → deleteTaskMapper.updateStatus(DONE)
        │ 失败 → 重试 3 次 → deadLetterQueue.enqueue("DELETE", ...)
        │
        └─→ DlqRetryScheduler 定时重试（指数退避：3s→9s→27s→81s）
            │ 成功 → 删除 DLQ 记录
            │ 4 次全失败 → 标记 DEAD + 告警
            │
            └─→ OrphanVectorScanner 最终兜底（每 30 分钟扫描清理）
```

***

### 10.2 OrphanVectorScanner（孤儿向量扫描清理） `[P2]`

> 详细设计同 [§8.5.1](#851-orphanvectorscanner孤儿向量扫描清理)（原 §8.5 内容已迁移至此处）
>
> `OrphanVectorScanner` 依赖 `ChunkMapper` 和 `DocumentMapper`（来自 `agent-qr-knowledge`），
> 这是 compensation → knowledge 单向依赖的合理体现。

```java
/**
 * 孤儿向量扫描器
 * 定时扫描 ChromaDB 中存在但 MySQL 中已逻辑删除/物理删除的切片向量，自动清理
 * 作为最终一致性兜底机制，保证 MySQL 与 ChromaDB 间的最终一致性
 */
@Component
@Slf4j
public class OrphanVectorScanner {

    @Autowired
    private ChunkMapper chunkMapper;           // 来自 agent-qr-knowledge
    @Autowired
    private ChromaVectorStore chromaVectorStore;
    @Autowired
    private DocumentMapper documentMapper;      // 来自 agent-qr-knowledge

    /**
     * 每 30 分钟执行一次孤儿向量扫描
     */
    @Scheduled(fixedDelay = 1800000)
    public void scanAndCleanOrphanVectors() {
        log.info("开始扫描孤儿向量...");

        // 1. 获取 ChromaDB 中所有向量的 document_id 元数据
        List<String> chromaDocIds = chromaVectorStore.getAllDocumentIds();

        // 2. 在 MySQL 中查询哪些文档已不存在或已逻辑删除
        List<String> orphanDocIds = chromaDocIds.stream()
                .filter(docId -> {
                    try {
                        Long id = Long.parseLong(docId);
                        Document doc = documentMapper.selectById(id);
                        // 文档不存在 或 已逻辑删除 → 孤儿向量
                        return doc == null || doc.getDeleted() == 1;
                    } catch (NumberFormatException e) {
                        return true; // 异常数据也清理
                    }
                })
                .collect(Collectors.toList());

        // 3. 批量删除孤儿向量
        if (!orphanDocIds.isEmpty()) {
            for (String docId : orphanDocIds) {
                chromaVectorStore.deleteByMetadata("document_id", docId);
            }
            log.warn("清理孤儿向量完成: 共清理 {} 个文档的向量数据", orphanDocIds.size());
        } else {
            log.info("未发现孤儿向量");
        }
    }
}
```

### 10.3 DocumentDeleteListener（文档删除事件监听器）★ `[P2]`

> 完整类设计见 [§7.2.2b DocumentDeleteListener](#722b-documentdeletelistener--p2--compensation-模块监听器)
>
> 核心要点：
> - `@TransactionalEventListener(phase = AFTER_COMMIT)` — knowledge 事务提交后执行
> - `@Async("deleteExecutor")` — 独立删除线程池，不阻塞 HTTP 响应
> - 先 MySQL 逻辑删除（`softDeleteByDocumentId` + `softDelete`），再 ChromaDB 物理删除
> - 异常写入 DLQ，由 `DlqRetryScheduler` 定时重试

### 10.4 DocumentDeleteServiceV2（物理删除服务） `[P2]`

> 完整类设计见 [§8.12 文档删除一致性设计 v2](#812-文档删除一致性设计-v2修正方案二-p2)
>
> 核心要点：
> - 只负责 ChromaDB 物理删除（MySQL 逻辑删除由 `DocumentDeleteListener` 完成）
> - 创建 `DeleteTask` 记录追踪删除状态
> - 失败时写入 DLQ（指数退避重试：3s→9s→27s→81s，最多 4 次）
> - `OrphanVectorScanner` 作为最终兜底清理

**方法签名更新**：

```java
@Service
@Slf4j
public class DocumentDeleteServiceV2 {

    @Autowired private ChromaVectorStore chromaVectorStore;
    @Autowired private DeleteTaskMapper deleteTaskMapper;
    @Autowired private DeadLetterQueue deadLetterQueue;

    /**
     * ★ 异步物理删除 ChromaDB 向量（由 DocumentDeleteListener 调用）
     * MySQL 逻辑删除已在 Listener 中完成，此处只负责 ChromaDB 物理删除
     *
     * @param documentId 文档ID
     * @param chromaIds ChromaDB 向量ID列表
     */
    @Async("deleteExecutor")
    public void asyncPhysicalDelete(Long documentId, List<String> chromaIds) {
        // 1. 创建 DeleteTask 记录
        DeleteTask task = new DeleteTask();
        task.setDocumentId(documentId);
        task.setChromaIds(JSON.toJSONString(chromaIds));
        task.setStatus("PENDING");
        deleteTaskMapper.insert(task);

        // 2. 调用 ChromaDB DELETE API 物理删除
        try {
            chromaVectorStore.deleteByIds(chromaIds);
            deleteTaskMapper.updateStatus(task.getId(), "DONE");
            log.info("ChromaDB 物理删除成功: docId={}, count={}", documentId, chromaIds.size());
        } catch (Exception e) {
            log.error("ChromaDB 物理删除失败: taskId={}, docId={}", task.getId(), documentId, e);
            deleteTaskMapper.incrementRetryCount(task.getId());
            deleteTaskMapper.updateStatus(task.getId(), "FAILED");
            // ★ 进入 DLQ 重试（指数退避）
            deadLetterQueue.enqueue("DELETE", documentId,
                JSON.toJSONString(chromaIds), e.getMessage());
        }
    }
}
```

***

## 11. 多源数据接入模块详细设计 (agent-qr-datasource) ★

> Maven 模块: `agent-qr-datasource` (P2 新增)
> 基础包路径: `org.example.agent_qr.datasource`
> 模块依赖: `agent-qr-common`
> 详细设计: 参见下方 [11.1 DataSourceConnector 策略接口](#111-datasourceconnector-策略接口) / [11.2 JdbcConnector](#112-jdbcconnector) / [11.3 RestApiConnector](#113-restapiconnector) / [11.4 S3Connector](#114-s3connector) / [11.5 SyncScheduler](#115-syncscheduler)

### 11.0 包结构（P2 全量）

```
agent-qr-datasource/
└── src/main/java/org/example/agent_qr/datasource/
    ├── connector/
    │   ├── [P2] DataSourceConnector.java             // 数据源连接器策略接口
    │   ├── [P2] JdbcConnector.java                   // JDBC 数据库连接器（MySQL/PG/Oracle/SQLServer）
    │   ├── [P2] RestApiConnector.java                // REST API 连接器（分页游标拉取）
    │   └── [P2] S3Connector.java                     // S3/文件系统连接器（对象存储/NFS/本地目录）
    ├── service/
    │   └── [P2] DataSourceService.java               // 数据源管理服务（CRUD + 连通性测试 + 手动触发同步）
    ├── scheduler/
    │   └── [P2] SyncScheduler.java                   // 同步调度器（Cron 定时 + 手动触发）
    ├── entity/
    │   └── [P2] DataSourceConfig.java                // 数据源配置实体
    ├── mapper/
    │   └── [P2] DataSourceMapper.java                // 数据源配置 Mapper
    └── dto/
        ├── [P2] ConnectionTestResult.java            // 连通性测试结果
        ├── [P2] SyncResult.java                      // 同步结果（含游标）
        └── [P2] SyncContext.java                     // 同步上下文

agent-qr-datasource/pom.xml 依赖:
  [P2] agent-qr-common, spring-boot-starter-web (RestTemplate), aws-sdk-s3 (S3连接器), mysql-connector-j
```

***

## 12. 数据质量检查模块详细设计 (agent-qr-data-quality) ★

> Maven 模块: `agent-qr-data-quality` (P2 新增)
> 基础包路径: `org.example.agent_qr.dataquality`
> 模块依赖: `agent-qr-common`
> 详细设计: 参见下方 [12.1 DataQualityChecker 规则链](#121-dataqualitychecker-规则链) / [12.2 质检规则实现](#122-质检规则实现示例)

### 12.0 包结构（P2 全量）

```
agent-qr-data-quality/
└── src/main/java/org/example/agent_qr/dataquality/
    ├── checker/
    │   └── [P2] DataQualityChecker.java              // 数据质量检查引擎（规则链顺序执行 + 阻断判定）
    ├── rule/
    │   ├── [P2] QualityRule.java                     // 质检规则接口
    │   ├── [P2] CompletenessRule.java                // 完整性检查（必填字段非空）
    │   ├── [P2] EncodingRule.java                    // 编码检查（自动检测非UTF-8并标记）
    │   └── [P2] FormatRule.java                      // 格式检查（日期/数字/百分比格式校验）
    ├── entity/
    │   ├── [P2] QualityReport.java                   // 质检报告（passCount/failCount/passRate/blocked）
    │   ├── [P2] RuleResult.java                      // 单条规则检查结果
    │   └── [P2] QualityFailure.java                 // 质检失败明细
    └── util/
        └── [P2] CharsetDetector.java                 // 字符集自动检测工具（juniversalchardet）

agent-qr-data-quality/pom.xml 依赖:
  [P2] agent-qr-common, juniversalchardet, spring-boot-starter
```

***

## 13. ETL 标准化管道模块详细设计 (agent-qr-etl) ★

> Maven 模块: `agent-qr-etl` (P2 新增)
> 基础包路径: `org.example.agent_qr.etl`
> 模块依赖: `agent-qr-common`、`agent-qr-datasource`
> 详细设计: 参见下方 [13.1 DataNormalizer 标准化引擎](#131-datanormalizer-标准化引擎) / [13.2 StructuredDataConverter](#132-structureddataconverter) / [13.3 CanonicalRecord](#133-canonicalrecord)

### 13.0 包结构（P2 全量）

```
agent-qr-etl/
└── src/main/java/org/example/agent_qr/etl/
    ├── normalizer/
    │   └── [P2] DataNormalizer.java                  // 数据标准化引擎（分类→字段映射→字典翻译→格式统一→结构化转换）
    ├── converter/
    │   └── [P2] StructuredDataConverter.java         // 结构化数据→自然语言文本转换器
    ├── engine/
    │   └── [P2] FieldMappingEngine.java              // 字段映射引擎（源字段→Canonical字段）
    ├── entity/
    │   ├── [P2] CanonicalRecord.java                 // Canonical 统一标准记录
    │   └── [P2] FieldMapping.java                    // 字段映射配置
    └── enums/
        └── [P2] DataType.java                        // 数据类型枚举：STRUCTURED / SEMI_STRUCTURED / UNSTRUCTURED

agent-qr-etl/pom.xml 依赖:
  [P2] agent-qr-common, agent-qr-datasource, spring-boot-starter
```

***

## 14. 知识目录模块详细设计 (agent-qr-catalog) ★

> Maven 模块: `agent-qr-catalog` (P2 新增)
> 基础包路径: `org.example.agent_qr.catalog`
> 模块依赖: `agent-qr-common`、`agent-qr-datasource`
> 详细设计: 参见下方 [14.1 KnowledgeCatalogService](#141-knowledgecatalogservice) / [14.2 DomainRouter](#142-domainrouter)

### 14.0 包结构（P2 / P3 全量）

```
agent-qr-catalog/
└── src/main/java/org/example/agent_qr/catalog/
    ├── service/
    │   └── [P2] KnowledgeCatalogService.java         // 知识目录管理（三级：业务域→数据源→数据实体）
    ├── router/
    │   ├── [P2] DomainRouter.java                    // [P2] 业务域路由器（关键词匹配方式）
    │   └── [P3] DomainRouterV2.java                  // [P3] 业务域路由器（Embedding 语义匹配方式）
    ├── entity/
    │   ├── [P2] CatalogTree.java                     // 目录树根节点
    │   ├── [P2] DomainNode.java                      // 业务域节点
    │   ├── [P2] SourceNode.java                      // 数据源节点
    │   └── [P2] EntityNode.java                      // 数据实体节点
    └── dto/
        └── [P2] DomainRoutingResult.java             // 域路由结果（matchedDomains + filter + fallback）

agent-qr-catalog/pom.xml 依赖:
  [P2] agent-qr-common, agent-qr-datasource, spring-boot-starter-web
  [P3] (无新增依赖，EmbeddingProvider 通过 agent-qr-rag 提供)
```

***

## 15. 核心时序图

### 15.1 文档上传异步处理时序图 `[P1 基础 + P2 DLQ分支]`

```
用户        KnowledgeCtrl    DocCommandSvc    EventBus    DocParseListener  ChunkEmbListener  ChromaDB
 │              │                │               │               │                │              │
 │  POST upload │                │               │               │                │              │
 │─────────────→│                │               │               │                │              │
 │              │ uploadDocument │               │               │                │              │
 │              │───────────────→│               │               │                │              │
 │              │                │  保存文件+记录 │               │                │              │
 │              │                │──┐            │               │                │              │
 │              │                │←─┘            │               │                │              │
 │              │                │ publishEvent  │               │                │              │
 │              │                │──────────────→│               │                │              │
 │              │   return 200   │               │               │                │              │
 │←─────────────│←───────────────│               │               │                │              │
 │              │                │               │ @Async 调度    │                │              │
 │              │                │               │──────────────→│                │              │
 │              │                │               │               │ 解析文档        │              │
 │              │                │               │               │──┐             │              │
 │              │                │               │               │←─┘             │              │
 │              │                │               │  publishEvent │                │              │
 │              │                │               │←──────────────│                │              │
 │              │                │               │ @Async 调度                    │              │
 │              │                │               │───────────────────────────────→│              │
 │              │                │               │               │                │ 切片文本     │
 │              │                │               │               │                │──┐          │
 │              │                │               │               │                │←─┘          │
 │              │                │               │               │                │ 向量化      │
 │              │                │               │               │                │──┐          │
 │              │                │               │               │                │  │          │
 │              │                │               │               │                │  │ store    │
 │              │                │               │               │                │  │─────────→│
 │              │                │               │               │                │  │←─────────│
 │              │                │               │               │                │←─┘          │
 │              │                │               │               │                │ 更新READY   │
```

### 15.2 RAG 问答时序图（混合检索 + Rerank + 流式 SSE） `[P2]`

```
用户    ChatCtrl  ChatQuerySvc  EmbedProv  HybridRetr  BM25+Chroma  Reranker  LLMCB    LLMProv   MySQL
 │         │          │             │           │            │          │        │         │        │
 │POST ask │          │             │           │            │          │        │         │        │
 │(SSE)───→│          │             │           │            │          │        │         │        │
 │         │askStream │             │           │            │          │        │         │        │
 │         │─────────→│             │           │            │          │        │         │        │
 │         │          │保存提问      │           │            │          │        │         │        │
 │         │          │──────────────────────────────────────────────────────────────────────→│
 │         │          │embed(query) │           │            │          │        │         │        │
 │         │          │────────────→│           │            │          │        │         │        │
 │         │          │←────────────│           │            │          │        │         │        │
 │         │          │           向量         │            │          │        │         │        │
 │         │          │hybridSearch(q,vec)     │            │          │        │         │        │
 │         │          │───────────────────────→│            │          │        │         │        │
 │         │          │              │ 语义检索 │            │          │        │         │        │
 │         │          │              │─────────→│            │          │        │         │        │
 │         │          │              │←─────────│ Top-20     │          │        │         │        │
 │         │          │              │ BM25检索  │            │          │        │         │        │
 │         │          │              │─────────────────────→│          │        │         │        │
 │         │          │              │←─────────────────────│ Top-20   │        │         │        │
 │         │          │              │ RRF 融合   │           │          │        │         │        │
 │         │          │              │─────────────────────────────────→│        │         │        │
 │         │          │              │←─────────────────────────────────│ Top-5  │         │        │
 │         │          │←─────────────│ 精排后结果   │           │          │        │         │        │
 │         │          │              │            │           │          │        │         │        │
 │         │          │ 构建Prompt + getActiveProvider()                           │         │        │
 │         │          │───────────────────────────────────────────────────────────→│         │        │
 │         │          │←───────────────────────────────────────────────────────────│         │        │
 │         │          │                                                     provider│         │        │
 │         │          │ generateStream(messages)                                       │        │        │
 │         │          │────────────────────────────────────────────────────────────────→│        │
 │  SSE←───│←─────────│ token1, token2, ... (逐字流式推送)                                │        │
 │ token   │  token   │              │           │            │          │        │         │        │
 │ 渲染    │  推送    │              │           │            │          │        │         │        │
 │  SSE←───│←─────────│ ...tokenN   │           │            │          │        │         │        │
 │ SSE←───│←─────────│ done + sources + conversationId                                 │         │
 │ done    │  done    │              │           │            │          │        │         │        │
 │         │          │ 保存完整答案 + 来源引用                                            │         │        │
 │         │          │──────────────────────────────────────────────────────────────────────→│
 │         │          │ 发布统计事件                                                          │        │
```

### 15.3 多源数据接入与 ETL 处理时序图 `[P2]`

```
定时器    SyncScheduler  Connector   DataQuality   DataNormalizer  Catalog    ChromaDB
 │            │             │         Checker                     Service
 │ Cron触发   │             │            │           │              │           │
 │───────────→│             │            │           │              │           │
 │            │fullSync/    │            │           │              │           │
 │            │incremental  │            │           │              │           │
 │            │Sync()       │            │           │              │           │
 │            │────────────→│            │           │              │           │
 │            │             │ 拉取原始数据│           │              │           │
 │            │             │──┐         │           │              │           │
 │            │             │←─┘         │           │              │           │
 │            │ SyncResult  │            │           │              │           │
 │            │←────────────│            │           │              │           │
 │            │             │            │           │              │           │
 │            │ publishEvent│            │           │              │           │
 │            │ DataSyncCompleted         │           │              │           │
 │            │─────────────────────────→│           │              │           │
 │            │             │            │ 规则链检查 │              │           │
 │            │             │            │──┐        │              │           │
 │            │             │            │←─┘        │              │           │
 │            │             │            │           │              │           │
 │            │             │      合格率≥50%?        │              │           │
 │            │             │     ┌──────┴──────┐    │              │           │
 │            │             │     │              │    │              │           │
 │            │             │     ▼              ▼    │              │           │
 │            │             │  阻断+告警     publishEvent│              │           │
 │            │             │              DataQualityPassed         │           │
 │            │             │            │──────────→│              │           │
 │            │             │            │           │ 分类+映射+转换│           │
 │            │             │            │           │──┐           │           │
 │            │             │            │           │←─┘           │           │
 │            │             │            │           │ Canonical    │           │
 │            │             │            │           │ Records      │           │
 │            │             │            │           │              │           │
 │            │             │            │           │ publishEvent │           │
 │            │             │            │           │ DataETLed    │           │
 │            │             │            │           │──────────────→           │
 │            │             │            │           │              │ 更新目录   │
 │            │             │            │           │              │──┐        │
 │            │             │            │           │              │←─┘        │
 │            │             │            │           │              │           │
 │            │             │            │           │   切片+向量化  │           │
 │            │             │            │           │──────────────────────────→│
```

***

## 16. 异常处理设计

### 16.1 异常分类

| 异常类型                    | HTTP 状态码 | 处理方式                                     |
| ----------------------- | -------- | ---------------------------------------- |
| `BusinessException`     | 400      | 返回业务错误提示                                 |
| `AccessDeniedException` | 403      | 返回"权限不足"                                 |
| 资源不存在                   | 404      | 返回"资源不存在"                                |
| 参数校验失败                  | 400      | 返回具体校验错误                                 |
| 外部服务调用失败                | 500      | 返回友好提示 + 记录 ERROR 日志                     |
| DLQ 重试耗尽                | -        | 标记 DEAD + 通知管理员 + 记录 ERROR 日志            |
| Ollama 不可用              | 503      | 熔断器自动降级到 DeepSeek API，对用户透明              |
| SSE 推送中断                | -        | 前端自动重连（3次），token 偏移续传；3 次失败后从 MySQL 回退加载 |
| PDF 超大文件                | 413      | 流式逐页解析 + 内存阈值保护，拒绝超过 500MB 的单文件          |
| 批量向量化失败                 | -        | 降级为逐条重试，仍失败进入 DLQ                        |
| ChromaDB 物理删除失败         | -        | 重试队列（3次退避）→ DLQ → 补偿任务兜底                 |
| Embedding 维度不一致         | -        | 启动时检测，日志告警 + 提示管理员重建                     |
| Access Token 过期         | 401      | 前端拦截器自动用 Refresh Token 刷新，用户无感知          |
| 未捕获异常                   | 500      | 返回"服务器内部错误" + 记录 ERROR 日志 + TraceId      |

### 16.2 关键异常场景处理

```java
// RAG 检索异常处理
try {
    retrievedDocs = chromaRetriever.similaritySearch(queryEmbedding, 5);
} catch (Exception e) {
    log.error("ChromaDB 检索异常", e);
    // 降级：告知用户检索服务暂时不可用
    return ChatResponseVO.fallback("知识库检索暂时不可用，请稍后重试", conversationId);
}

// LLM 调用异常处理
try {
    answer = llmProvider.generate(messages);
} catch (Exception e) {
    log.error("LLM 调用异常", e);
    return ChatResponseVO.fallback("AI 回答生成失败，请稍后重试", conversationId);
}

// 异步文档处理异常处理
try {
    // 文档处理...
} catch (Exception e) {
    log.error("文档处理失败: id={}", event.getDocumentId(), e);
    // 更新状态为 FAILED，记录错误原因
    documentMapper.updateStatusAndError(
        event.getDocumentId(),
        DocumentStatus.FAILED.name(),
        e.getMessage()
    );
}
```

***

## 17. 关键算法说明

### 17.1 递归字符分割算法 `[P1]`

```
输入：长文本 text，目标切片大小 chunkSize=500，重叠大小 overlap=50
输出：文本片段列表

算法步骤：
1. 按 "\n\n" 将文本初步分割为段落列表
2. 遍历每个段落：
   a. 若段落长度 ≤ chunkSize：直接加入结果
   b. 若段落长度 > chunkSize：
      - 从位置 0 开始，每次取 chunkSize 长度的子串
      - 在子串末尾附近寻找断点（。\n！？；，等）
      - 下一段起始位置 = 当前断点 - overlap
      - 直到处理完整个段落
3. 合并过短片段（长度 < 100）到前一个片段
4. 返回切片列表
```

### 17.2 混合检索 + RRF 融合 + Rerank 流程 `[P2]`

```
输入：用户问题 query，查询向量 Vq
输出：精排后的 Top-K 文档片段列表

算法步骤：
1. 双路召回（并行）：
   a. 语义检索：ChromaDB.similaritySearch(Vq, topK=20) → 候选集 A
   b. 关键词检索：BM25Retriever.keywordSearch(query, topK=20) → 候选集 B

2. RRF 加权融合去重：
   对每个文档 d：
     RRF_score(d) = w1/(k+rank_A(d)) + w2/(k+rank_B(d))
     其中：w1=0.6, w2=0.4, k=60（平滑常数）
   按 RRF_score 降序排列，去重合并

3. Rerank 精排：
   对融合后的候选集，使用 bge-reranker-v2-m3 交叉编码器
   逐个计算 [query, doc_i] 的相关性分数
   按分数降序排列，取 Top-K=5

4. 组装返回结果（含 Rerank 分数、文档标题、内容片段）
```

### 17.3 死信队列指数退避重试 `[P2]`

```
输入：失败事件 event，当前重试次数 retryCount（0-based）
输出：重试调度时间

算法步骤：
1. 计算退避延迟：delay = 3^(retryCount+1) 秒
   retry 0 → 3s, retry 1 → 9s, retry 2 → 27s, retry 3 → 81s

2. 判断是否超限：
   - retryCount >= 4 → 标记 DEAD，停止重试，通知管理员
   - retryCount < 4 → 更新 nextRetryAt = now + delay

3. 定时扫描器每 30 秒拉取 nextRetryAt ≤ now 的记录执行重试
```

### 17.4 LLM 熔断器状态机 `[P2]`

```
状态转换规则：
  CLOSED ──(连续失败 ≥ 3 次)──→ OPEN
  OPEN   ──(30 秒超时)───────→ HALF_OPEN
  HALF_OPEN ──(探测成功)─────→ CLOSED
  HALF_OPEN ──(探测失败)─────→ OPEN

降级策略：
  OPEN 期间 → 自动使用 DeepSeek API 备用链路
  对用户完全透明，无感知切换
```

### 17.5 字符集自动检测与转码 `[P2]`

```
输入：原始文本字节流 bytes
输出：UTF-8 编码的文本字符串

算法步骤：
1. 使用 juniversalchardet 库检测字节流编码：
   UniversalDetector.detectCharset(bytes) → detectedCharset
2. 置信度评估：
   - 置信度 ≥ 0.8 → 使用检测到的编码解码
   - 置信度 < 0.8 → 回退尝试常见编码序列：
     [UTF-8, GBK, GB2312, ISO-8859-1, Windows-1252]
3. 解码成功 → 转码为 UTF-8
4. 解码失败 → 标记为编码异常，写入质检报告
```

### 17.6 结构化数据 → 自然语言转换算法 `[P2]`

```
输入：结构化行记录 {field1:value1, field2:value2, ...} + 字段映射配置
输出：语义完整的自然语言段落

算法步骤：
1. 获取表/实体注释作为段落标题：
   title = metadata.table_comment || "数据记录"

2. 按字段优先级（priority）排序，逐字段生成文本：
   对每个字段 field：
   a. 检查值是否为空 → 空值跳过
   b. 应用字典翻译：
      displayValue = dictMapping[rawValue] || rawValue
      （如 "D01" → "研发部"）
   c. 应用格式转换：
      - DATE_TO_CHINESE: "2024-01-15" → "2024年1月15日"
      - MONEY_FORMAT: "15000.00" → "15,000"
      - PERCENTAGE: "0.85" → "85.0%"
   d. 应用自然语言模板：
      template = "{字段中文名}为{值}{单位}"
      text = template.replace(值, displayValue).replace(单位, unit)

3. 将所有字段文本用"，"连接，末尾加"。"
4. 返回完整段落
```

### 17.7 知识目录检索路由算法 `[P2 关键词 + P3 语义]`

```
输入：用户问题 query，知识目录树 catalog
输出：DomainRoutingResult（匹配域+权重+过滤条件）

算法步骤：
1. NLP 分词提取关键实体词：
   keywords = IKAnalyzer.tokenize(query)
   过滤停用词，保留名词和专有名词

2. 遍历目录树计算域匹配分数：
   对每个业务域 domain：
     score = 0.0
     a. 域名匹配：domain.name 与 query 的 TF-IDF 相似度 × 0.5
     b. 数据源匹配：每个 source.name 匹配得分 × 0.3
     c. 实体匹配：每个 entity.name 匹配得分 × 0.4
     若 score > 0，加入 matchedDomains

3. 路由决策：
   - matchedDomains 非空 → 构建 metadata filter，裁剪检索范围
   - matchedDomains 为空 → fallbackToGlobal=true，全库盲搜

4. 构建 ChromaDB filter：
   filter = { "domain": "HR,FINANCE" }
   语义检索 + BM25 仅在 filter 范围内的切片中检索
```

### 17.8 批量向量化攒批算法 `[P2]`

```
输入：切片流（无界），批量大小 B=32，超时 T=100ms
输出：批量 Embedding 向量

算法步骤：
1. 启动 N 个消费者线程（N = CPU 核心数），每个线程维护一个本地批次列表
2. 生产者线程将 ChunkEmbedTask 放入 BlockingQueue
3. 每个消费者循环执行：
   a. 从 BlockingQueue.poll(T, MILLISECONDS) 取任务
   b. 任务放入本地批次列表
   c. 触发条件：批次.size() >= B OR poll 超时
   d. 触发时：调用 embedBatch(texts) 一次处理整批
      → 成功：分发向量到各 CompletableFuture
      → 失败：降级为逐条 embed() 重试
   e. 清空批次，重置超时计时器

时间复杂度：O(N/B) 次 API 调用 vs O(N) 次逐条调用
空间复杂度：O(B) 攒批缓存 + O(N) 结果 CompletableFuture
性能收益：50 万切片从 27 小时 → 16 分钟（100 倍）
```

### 17.9 Provider 切换决策流程 `[P1]`

```
输入：application.yml 中 llm.provider 配置值
输出：LLMProvider 实例

决策流程：
1. 读取配置值 llm.provider
2. switch(provider)：
   case "ollama":    → 注入 OllamaLLMProvider
   case "deepseek":  → 注入 DeepSeekLLMProvider（默认）
3. 若对应 Bean 未注册 → 抛出 BusinessException
```

***

> **文档版本**: v1.0
>
> **编写日期**: 2026-06-03
>
> **依据文档**: 系统概要设计说明书 v1.0、软件需求规格说明书 v1.0

