-- ============================================
-- P1 阶段测试数据
-- 仅在数据库首次初始化时自动执行
--
-- 注意: 密码哈希需通过应用层 PasswordUtil 生成
-- 启动后端后调用 POST /api/auth/register 创建用户
-- 或使用 docker exec 手动插入
-- ============================================

-- 测试会话
INSERT INTO chat_conversation (user_id, title) VALUES
(1, '测试会话-产品功能介绍'),
(1, '测试会话-技术支持文档');
